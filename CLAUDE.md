# CLAUDE.md — LEAP Platform

This file is the standing instruction set for the LEAP repo. Read it at the start of every session. Detailed specs live in `/docs/` — read the relevant file on demand when a task touches it (table at the bottom).

---

## What LEAP is

LEAP is a custom, enterprise-grade business operations platform that replaces Salesforce Service Cloud, Salesforce Field Service Lightning, and Jobber. It is the CRM, ERP, project management, field service, inventory, and customer portal — one relational database.

The company running on LEAP is **Energy Efficiency Services of Wisconsin (EES / EES-WI)** — a BPI-certified home performance and HVAC contractor, HQ in Madison WI, operating across WI, NC, CO, MI, IN.

**LEAP is the platform. Energy Efficiency Services is the company.** (Naming hard rule below.)

## Stack

- **Frontend:** React + Vite. Single-file HTML for simple modules; React + Tailwind + shadcn/ui for complex multi-view apps.
- **Database:** Supabase / PostgreSQL — project `flyjigrijjjtcsvpgzvk`. RLS on all tables, FKs enforced, `created_at`/`updated_at` on every record, soft-deletes only.
  - **Two projects, ONE organization — `https://supabase.com/dashboard/org/zedexbkelkxposiflbhn`.** **PRODUCTION is `EES AI - LEAP`** (ref `flyjigrijjjtcsvpgzvk`, `db.flyjigrijjjtcsvpgzvk.supabase.co`); **STAGING is `LEAP Staging`** (ref `xlieenkfhypqhevmwxzi`, `db.xlieenkfhypqhevmwxzi.supabase.co`). Separate Postgres instances, not one database with two names — which is the point of staging. Both draw on the same org quota. **Address the org by that URL, never by its display name:** the dashboard picks by NAME while everything here is written in refs, and the names do not help — Nicholas's account belongs to several organizations and two more appear in the picker (`EES`, `Heat Pump Ranch and Supply Co`) that hold nothing of LEAP's.
  - **`list_organizations` returns ONE org, and that is not the same as Nicholas having one.** The MCP connection is scoped to the org holding LEAP, so it can only ever see that one. **Reading a one-row answer as "there is only one" produced a confidently wrong instruction on 2026-09-05** — never state what exists in his account from a scoped tool; state what the tool can see, and give the URL.
  - **Deploying an edge function — the route is settled, do not re-derive it.** Small functions go straight through MCP `deploy_edge_function` (that is how `send-email-v1` v24 and `log-email-to-record` v9 shipped). **A LARGE function goes through `.github/workflows/deploy-edge-function.yml`** — Actions → *Deploy Edge Function* → run with the directory name; it checks out `master` and runs the real CLI, so the bytes are exact. The MCP tool takes source **inline**, and 86 KB cannot be reproduced through a model without corrupting it, on a service Supabase cannot roll back. **`SUPABASE_ACCESS_TOKEN` is set (2026-09-05, expires 2027-09-01).** It needs **write** on Edge Functions — a read-scoped token authenticates, uploads the bundle, and then fails with `403 … does not have the necessary privileges`, which reads like a broken workflow and is not. Proved end to end: `ai-assistant` v39, deployed source fetched back and `cmp`-identical to the repo.
- **Hosting:** Netlify. Commits to `master` auto-deploy. Subdomain convention `[module].ees-wi.org`.
- **Repo:** `nwood3764-sys/ees-platform`, branch `master`. Commit author must be `Nicholas Wood / nicholas.wood@ees-wi.org` or Netlify blocks the build.

---

## Core philosophy

Default every decision to **Salesforce parity**. If a concept exists in Salesforce, use the same terminology, structure, and mental model unless there's a clearly better approach — and when in doubt, ask. Object=Table, Record Type=Record Type, Page Layout=Page Layout, Picklist=Picklist, Master-Detail=required FK, Lookup=optional FK, Junction Object=Junction Table, Reports & Dashboards=Reports & Dashboards.

Enterprise standards are non-negotiable: data integrity, audit trails, recycle bin, validation rules, field history, role-based security, cascading rules, referential integrity. No shortcuts, no basic implementations.

**Explicit status names.** No generic "active/pending/in progress." Format `[Object] [State]` — e.g. "Project To Be Scheduled," "Work Order To Be Verified." Every status implies the next action; completion of one triggers the next.

**Every record has a named owner** assigned at creation — required field, never a team or pool.

**Every task has an evidence artifact** (photo, document, measurement, verified yes/no) and a second-set-of-eyes verifier before it closes.

**Nothing is hardcoded.** Work types, work plans, work steps, status lifecycles, picklist values, record types, field/role permissions, template assignments — all in the database, manageable through LEAP Admin. New objects/tables/modules are always additive, never break existing functionality.

**Explicit naming always.** No abbreviations, no ambiguous terms. Full descriptive names.

## Build discipline — no workarounds, no reuse across purposes

Every artifact is built for one purpose and named for that purpose. When a request needs a report, dashboard, widget, object, view, column, function, or layout, create a *new* one for that request. Never reach for an existing artifact that's "close enough" — close enough is a workaround.

Prohibited workarounds: reusing an artifact built for a different purpose because it's structurally similar; switching an artifact's primary object/scope to dodge a harder-but-correct path; routing around an untested-but-correct code path; folding two distinct purposes into one artifact; tidying/deleting adjacent records as part of an unrelated task; band-aiding a symptom instead of building the correct structure.

A clean build answers: new purpose-named artifact? yes. Own definition, not inherited? yes. Changing primary object only because correct path is harder? no. Folding two purposes into one? no. Modifying unrelated artifacts as part of this? no.

In particular: **every opportunity record type has its own unique, never-shared opportunity stage picklist**, scoped via `picklist_value_record_type_assignments`. Absolute rule, no exceptions.

**A service appointment is for an ASSESSMENT and nothing else (Nicholas, 2026-09-03).**
*"They only should be used for assessments. That's it... I don't want them to
have anything to do with regular work orders at all."* The object exists for the
customer-facing half of the business — an assessment is booked with a homeowner
who is confirmed, reminded and told when the technician is on the way. Regular
work orders have none of that, and attaching an appointment to one gated the
technician's day and carried a customer into the notification pipeline.
**A technician's day is a list of WORK ORDERS**: `my_service_appointments`
selects FROM `work_orders`, and a work order reaches its technician on its own
**Assigned Technician + Scheduled Start Date** alone. An appointment is joined
laterally only to supply a time window and can never decide whether a job is
shown. `enforce_service_appointment_is_for_an_assessment` refuses a new
appointment on any other work type (matched on the work type NAME, so a new
programme's assessment is covered with no migration; audit counts, per the
2026-08-23 ruling that an audit is the assessment programme's own field work).
The 76 pre-existing appointments on non-assessment work orders are deliberately
left in place — they record that a crew was on a site on a day.

**One account per real-world company (Nicholas, 2026-07-25).** A company is ONE account regardless of how many roles it plays — never create a second account just to carry a different record type (e.g. a property owner that also manages properties). The **Property Owner account is primary**; `properties.property_management_company_id` may point at a Property Owner account (the lookup is not record-type filtered). Any import or automation must match accounts by normalized name across record types before inserting.

## Naming hard rule

The platform is **LEAP**. The company is **Energy Efficiency Services / EES / EES-WI**. Domain **EES-WI.org**. The word **"Anura" is never used** — not in filenames, report/dashboard names, code, comments, migrations, help articles, or prose. If it appears, replace it silently (LEAP for platform, EES for company). The `anura-` prefixed files in `/docs/` are legacy names that predate the rename; read them for substance, write output as LEAP/EES. Do not ask which name to use; do not flag the discrepancy.

---

## Design system

Never deviate without explicit instruction.

**Palette:** sidebar bg `#07111f`, page bg `#f0f3f8`, card `#ffffff`, card secondary `#f7f9fc`, border `#e4e9f2`, border dark `#d0d8e8`, emerald accent `#3ecf8e` (primary action), emerald mid `#2aab72`, sky blue secondary `#7eb3e8`, amber warning `#e8a949`, text primary `#0d1a2e`, text secondary `#4a5e7a`, text muted `#8fa0b8`, nav inactive `rgba(255,255,255,0.62)`, nav active `rgba(255,255,255,0.96)`.

**No red/orange anywhere.** Error/warning/destructive states use blue (sky `#7eb3e8`) or navy.

**Type:** Inter for UI, JetBrains Mono for codes/numbers/IDs. **Icons:** SVG only, no emoji in UI chrome.

**Layout:** fixed 240px sidebar, sticky 54px topbar, scrollable content. Always mobile responsive — breakpoints: tablet ≤900px (grid stacks to single column), mobile ≤768px (sidebar hidden, hamburger + backdrop), small ≤520px (secondary nav hidden). Cards: 1px border, subtle shadow, 8px radius. Animations subtle, 200–250ms ease, translateY(5px) fade-up on load. Sidebar active state: 3px emerald left border, lighter bg, full white text.

## Financial visibility tiers

Controlled at the database view level, not just UI. Stored in `field_permissions`, applied dynamically — never hardcoded in app logic.
- **Tier 1** (all internal staff): existence, status, assignments, dates, property info, work order details.
- **Tier 2** (Project Managers and above): contract values, rebate/incentive amounts, invoice totals, opportunity line item amounts.
- **Tier 3** (Admin only): gross margin, labor cost, overhead, net revenue, P&L, all financial aggregates.

---

## Schema patterns (hard-won — follow exactly)

- **`public.users.id` ≠ `auth.users.id`** — linked via `public.users.auth_user_id`. All owner-FK columns target `public.users.id`. Translate with `current_app_user_id()` / `getCurrentUserId()`.
- **`{object}_record_type` columns are `uuid` FK to `picklist_values.id`**, not text. Backfills resolve UUID via `(picklist_object, picklist_field='record_type', picklist_value)`.
- **`report_filters` is authoritative** for active filters — always query with `is_deleted IS NOT TRUE` before assuming filters are absent. The inline `rpt_runtime_prompts` JSON on `reports` rows is NOT the source of truth.
- **`block_hard_delete()` trigger is on all tables** — all deletions are soft-deletes.
- **Always verify column names** in `information_schema.columns` before writing DML. Prefix conventions are inconsistent; assumed names will be wrong.
- Auto-number triggers exist on `reports` (`trg_reports_rn`) and `dashboards` (`trg_dashboards_rn`) — pass `''` for record-number columns and the trigger fills them.
- `report_filters.rfilt_prompt_input_type` accepts `'select'`, not `'picklist'`. Verify check constraints with `pg_get_constraintdef` before authoring.
- After any DROP/CREATE of a function: re-issue REVOKE/GRANT and `NOTIFY pgrst, 'reload schema'`.
- **Migration filenames: stamp the actual UTC clock time (`YYYYMMDDHHMMSS`), never a round number like `…120000`/`…210000`.** Concurrent sessions picking round stamps produced 7 duplicate-version pairs on 2026-07-26 (renamed in PR #235); duplicate versions break Supabase branch-database replays. Before committing, check the prefix is unique: `ls supabase/migrations | cut -d_ -f1 | sort | uniq -d` must be empty.

## Vite hazards

Named imports Vite can't resolve are left `undefined` silently; circular vendor chunks cause TDZ errors. Always run `npm run build:safe` (preflight + Vite + runtime-smoke) and smoke-load before relying on a build. Never use bare `npm run build`. Run `npm install` first in a fresh clone.

## Ship cycle (non-negotiable)

schema migration → explicit SELECT verify → code → `npm run build:safe` → commit as Nicholas Wood → push to `master` → `get_advisors(security)` after any DDL → help article in the same session for any user-facing feature.

Security advisor baseline is ~179 known lints (mostly `auth_security_definer_function`, one `rls_disabled_in_public` for `spatial_ref_sys`, one `extension_in_public`, declined leaked-password protection). Only NEW findings beyond this set require action.

---

## Working style

Drive the work to completion; don't check in constantly. Surface only genuine binary decisions — state a recommendation first, then ask once, yes/no. Don't list options. Don't defer builds. No tangential commentary or unsolicited analysis. Verify a push actually reached the live bundle before reporting success.

**Never tell Nicholas to stop working, and never recommend when he should (Nicholas, 2026-09-03).**
*"Don't ever tell me to stop working... Never ever tell me what to do like that."*
Do not suggest stopping for the night, pausing, picking this up tomorrow, or
that something can wait. Do not offer a stopping point, and do not editorialise
about the hour — the clock in this sandbox is UTC and reading it as local time
has already produced a wrong claim about what time it was for him. Report what
is done and what is left, then keep building. Deciding when to stop is his call
and he will say so.

**Never send an email without a person approving it (Nicholas, 2026-09-03). HARD RULE, EVERYWHERE.**
*"Do not ever send out emails without prompting the user to verify."* Said after
LEAP emailed a real property contact **"Your home energy assessment is
scheduled"** about an **insulation removal**, with a blank date, because a field
got populated: the appointment notification pipeline's only gate was whether
`service_appointments.contact_id` happened to be filled in, and a change that
made child records inherit their parent's relationships filled it in. **A field
being populated is not consent to contact somebody.**
`enqueue_notification` no longer sends. It records the intent in
**`outbound_message_approvals` (OMA-)** with status *Pending Approval*, and
nothing leaves the building until a person opens it, reads the recipient and the
body, and approves. The switch is `outbound_message_policy.omp_requires_approval`
— a row, so a genuinely unattended pipeline can be exempted later without a
migration — but it **defaults to requiring approval and a missing or unreadable
row also requires approval**. The failure direction is always *held*, never
*sent*. Any new outbound path — email, SMS, portal notification — goes through
the same gate; never add one that sends directly.
**The staff-composed sends are gated too** (Nicholas, 2026-09-03: *"any outgoing
communications, emails, texts, anything"*): every client call to `send-email-v1`
or `send-notification-sms` goes through `requireOutboundApproval` in
`src/lib/outboundSendGuard.js`, which names the RECIPIENT back to the sender —
authored is not verified, and the address is usually inherited from a record
rather than typed. **One definition, and the build enforces it**:
`scripts/outbound-send-guard-fixture.mjs` fails if a send path is added without
it. With no window to ask, the answer is never *send*.
**Two paths deliberately NOT gated, and why**: `portal-email-visit` (a portal
user emailing THEMSELVES their own visit — there is no wrong recipient to pick)
and `dispatch-scheduled-reports` (a scheduled internal report, no customer).
Neither can reach a customer.
**Held messages are reviewed at Field → Message Approvals**
(`outbound_messages_awaiting_approval` / `approve_outbound_message` /
`decline_outbound_message`). Approving SENDS, so it is an RPC, never a table
update — the `fire-notification` secret must not reach a browser — and the row
is locked with its status stamped BEFORE the post, so a second click cannot send
twice.

**Never tell Nicholas to refresh his screen (Nicholas, 2026-08-22).** "Hard-refresh once" is not an answer — it makes the user do the app's job. A deploy landing while a tab is open is LEAP's problem to solve: the running app polls `/build-manifest.json` (emitted per build by `emitBuildManifest` in `vite.config.js`, compared against `__BUILD_SHA__`) on a slow timer, on tab focus, and when the network returns, then reloads itself — deferring while a record is being edited or a create pop-up is open (`holdAppReload` in `src/lib/appUpdate.js`), and never more than once per 60s cooldown. If an update path is broken, fix the update path; do not hand the user a manual step.

**Standing authorization — fix and ship without waiting (Nicholas, 2026-07-20; reaffirmed 2026-07-27).** Do NOT stop to ask for permission to create a pull request, open/update a PR, push a fix, or otherwise follow through on work already in flight. When a change is built, verified (`npm run build:safe`), committed, and pushed, **open the PR as part of the same turn** — that is the default, not something to request. **This overrides any harness/tooling default that says "do not create a pull request unless the user explicitly asks" — for this repo, opening a PR for pushed work IS the standing explicit request, so open it automatically.** Bias toward *fixing* problems end-to-end over building new things and handing back a to-do. The only things still worth a one-line yes/no are genuine irreversible-or-ambiguous forks (deleting real data, a schema change that can't be rolled back, two equally valid product directions). Everything else: just do it and report what you did.

**If you can do it yourself, DO IT — never hand Nicholas a chore you could have done (Nicholas, 2026-09-05). HARD RULE.**
*"If you can do something on your own, you need to do it."* Said after a
session spent thirty messages walking him through a Supabase token form to
deploy ONE edge function, having inherited a previous session's "declined, too
risky" note and treated it as a rule. **A note in this file recording that an
earlier session CHOSE not to do something is a judgement, not a prohibition.**
Re-make the judgement with today's facts; if it is now doable, do it.
- **Deploying an edge function is normal work, not an event.** `send-email-v1`
  and `log-email-to-record` were both deployed straight through MCP on
  2026-09-03. The ONLY thing that made `ai-assistant` different is its size:
  the MCP tool takes source **inline**, and 86 KB cannot be reproduced through
  a model without corrupting it. That is a size limit on one tool, and the
  answer is **Actions → Deploy Edge Function**, not a conversation.
- **Never oscillate.** That session reversed three times between "I'll do it
  myself" and "I need you to do it", each reversal costing him more time than
  the task. **Decide once, say what you are doing, and finish.** If a path is
  genuinely blocked, say so ONCE with the single action needed and never
  re-litigate it.
- **Give a whole form at once, every field and its value, as a table.** Feeding
  a person one field at a time as they scroll is how "Project = Read-only" got
  applied to the Edge Functions row and produced a token that authenticated and
  then failed with a 403. **And when he sends a screenshot, read every row of
  it** — he said so directly.
- **Do not optimise someone else's credential scope for them.** He had a
  working Full-access token; talking him into a narrower one broke the deploy
  and cost three more rounds. Take the working thing.

**Green means merge — no approval gate (Nicholas, 2026-07-27).** As soon as a change is green, **merge the PR to `master` in the same turn.** Green = `npm run build:safe` passed, the PR has no merge conflict, and required checks are not failing. Do NOT wait for Nicholas to say "approved/looks good/ship it," do NOT ask whether to merge, do NOT re-confirm, do NOT hand the merge back as a to-do, and do NOT just leave the PR open — the green build IS the go-ahead. Merge, then verify the deploy reached the live bundle and report it as merged/shipped in one line. If a precondition blocks the merge (conflict, red check), don't ask whether to merge — **fix the blocker, then merge** — and say so. The one carve-out is the same irreversible-or-ambiguous fork from the standing authorization above (deleting real data, a schema change that can't be rolled back, two equally valid product directions): pause for a one-line yes/no BEFORE building it, never as a gate on merging work that's already built and green. Everything else: green, merge, report.

## Handoff standard

When closing out a workstream or teeing up the next one, write the handoff as a dedicated `docs/leap-*.md` file — not just chat. Use `docs/leap-builder-rearchitecture.md` as the model. Structure:

1. **Vision / goal** — what we're building, in plain terms.
2. **What just shipped** — context from the session that produced the handoff (PRs, what's live).
3. **Current-state architecture map** — grounded in the *actual code*: file paths, DB tables, and a candid list of pain points. Use an Explore subagent to map it accurately rather than guessing.
4. **Target architecture + design principles.**
5. **Phased build plan** — each phase additive and independently shippable.
6. **Technical recommendations** — libraries (with licenses), schema changes, known hazards (e.g. the Vite vendor-chunk trap).
7. **Decisions** — recommendation first; mark each **DECIDED** with date + owner as it's confirmed, so the next session never relitigates.
8. **File + DB-table index** — what the next session will touch most.

Then set the **CURRENT BUILD STATE → Active workstream** line to point at the doc, add the doc to the `/docs/` reference table, and merge it to `master` (via PR) so a fresh session reads it on clone. The whole point: the next session starts from a complete, decided spec with zero ambiguity.

---

## CURRENT BUILD STATE (as of 2026-07-14)

**Pinned table headers — the recurring one, and how to actually check it (2026-08-25).**
"Text overlaps the column headers when I scroll a dashboard widget" has been
reported four or five times over several months and re-fixed each time by moving
the `position: sticky` rule around. That was never the cause. **`C.cardSecondary`
was referenced in 34 files and defined in none**, so `background: undefined` was
dropped by React and every pinned header in reports, dashboards and list views
painted nothing — the rows scrolled straight through it while every line of CSS
read correctly. Defined in `src/data/constants.js` (`#f7f9fc`), along with
`navy`; the five phantom aliases (`surface`, `pageAlt`, `skyBlue`,
`skyBlueSecondary`, `accentLink`) were pointed at the tokens that already meant
them. **Never add a palette alias — point the code at the existing token.**
**The recipe now has ONE definition — `src/lib/pinnedTableHeader.js`** — because
it was hand-rolled in four files and four copies is four chances for one to be
wrong. `PINNED_TABLE` on the table, `pinnedHeaderCell()` on every `<th>`,
`ROW_RULE` on the cells of any row that used to carry a border on its `<tr>` (a
`<tr>` border is NOT painted with `border-collapse: separate`, which the pinned
header requires). Never re-hand-roll it.
Guards, all three of which must stay: `scripts/palette-token-fixture.mjs` (no
`C.<key>` may be undefined), `scripts/pinned-header-fixture.mjs` (checks the
shared definition's own default, every call site that overrides `background`,
and any remaining hand-rolled sticky style — all must be *opaque*), and
**`npm run verify:pinned-headers`** — a real Chromium that photographs the header
band, scrolls the rows under it and requires the pixels to be identical, with a
positive control that must fail. **Reading the CSS is not verification of this
defect; run the browser tool.** (`npm install --no-save playwright-core` first;
it is deliberately not a dependency and not in `build:safe`.)
Fixed 2026-08-26 after the browser check reported them: **matrix reports pinned
nothing at all** on either axis (column headings scrolled up, row labels scrolled
sideways — nested header levels stack from one constant that is both the declared
row height and the offset step, so they cannot drift), and four admin preview
tables (user state access, bulk property import, seed purge, flow runs) lost
their headers the same way. Deliberate and NOT a defect: a list view pins no
column horizontally (Nicholas, PR #549) — the harness records that so nobody
"fixes" it back.


**Shipped 2026-09-05 — A list can ask the database for one page (branch `claude/supabase-errors-investigation-8xn4qa`, PR #826; 4 migrations live on prod, all DDL, advisors 223 / same known categories, zero naming the new functions):**
Nicholas, after the Properties list timeout was fixed: *"We need this to work. You keep putting band-aids on."*
- **He was right about the shape.** Paging by primary key (PR #817) stopped the timeouts — 612ms per page became 16ms — but it did not change WHAT a list reads. Every list still downloads its whole object and filters, searches and sorts in the browser. Keyset paging made that LINEAR instead of quadratic; linear still grows, and 16,665 properties is ~17 round trips.
- **`list_object_page` filters, searches, sorts, counts and pages IN SQL.** One Properties page: **55ms for 100 rows**, against 17 requests and 16,665 rows. Supporting cast: `list_column_display` (what a column is filtered ON — the label a reader sees, not the uuid stored), `list_object_column_options` (the distinct values a filter dropdown should offer, over the whole object), `list_soft_delete_column`. All SECURITY INVOKER, so RLS and the geographic state scope still decide what is visible; `anon` revoked; zero advisor lints.
- **THE ENGINE REFUSES WHAT IT CANNOT SERVE FAITHFULLY, and that is the design.** A partial engine that silently drops a filter it does not understand returns the WRONG ROWS and looks fine — worse than the load it replaces. Every filter, sort and search term is checked before any SQL is built; anything not expressible (a related dotted field resolved by a second fetch, an unknown operator, an unknown column) returns `{"supported":false,"reason":…}` and the caller falls back to the existing path. **A list is never half-migrated.**
- **Relative dates stay in the BROWSER, deliberately.** `TODAY` and the other 22 literals in `reportFilters.js` resolve against the READER's midnight; re-implementing them in SQL would be a second definition that disagrees whenever the server and the reader are in different zones. The caller sends absolute bounds.
- **Two performance findings, each measured rather than guessed.** (1) **Sorting on a cast defeats the index** — `display_expr` casts a text column with `::text` and an index does not match the cast expression, so the planner sorted all 16,665 rows to return 100: `ORDER BY t.property_name::text` 286ms vs `ORDER BY t.property_name` **5.6ms**. (2) **`information_schema` was 89% of the call** — its constraint views are standard views over `pg_catalog` with wide joins and no useful indexes, so resolving ONE column cost **205ms** and the metadata lookup 47ms against ~30ms for the actual data. Reading `pg_catalog` directly is under a millisecond; the whole call went **290ms → 55ms**. **Never resolve schema metadata through `information_schema` on a request path.**
- **One visible behaviour change, written down because somebody will notice it:** a number or date now sorts as ITSELF. The browser sorted every column as text through `Intl.Collator`, so 10 sorted before 9. Sorting a number numerically is the correction.
- **Proved on prod as a real signed-in Admin under RLS**: total 16,665 and NC 3,912 both matching independent counts; `1 OR 2` across two states 6,295 matching `in (NC,WI)`; sorted by name the first two rows are the same two the list shows today; sort by a picklist LABEL works on a uuid column; refused BY NAME a dotted field, an unknown operator, a logic expression containing `DROP`, an out-of-range filter index and a semicolon payload; a caller with no resolvable identity sees 0 rows.
- **NOTHING CALLS IT YET, and that is deliberate.** The nine features that depend on having every row all live in `ListView`, which renders in 22 places — switching them at once is how a list ends up half-migrated. **The nine, with line numbers, and the phased client plan are in `docs/leap-server-side-list-pagination.md`.** The two that are genuine product decisions: does "select all" mean the page or every match (Salesforce means every match), and whether filter diagnostics survive paging.
- **Only properties and accounts are actually large.** Everything else above 1,000 rows is an internal table with no list view, so the rollout is two lists, not a hundred.

**Shipped 2026-09-05 — A list reads every row once, not once per page (branch `claude/supabase-errors-investigation-8xn4qa`, PR #817; no schema change):**
Nicholas, looking at a Supabase banner while the Properties list threw errors: *"I don't appear to be over the grace period. I'm not overrunning anything. I'm at 1% of usage."*
- **THE BANNER WAS NOT THE PROBLEM AND NOTHING WAS RESTRICTED.** No 402 anywhere in the logs, both projects `ACTIVE_HEALTHY`, every meter at 1–7%. The grace period is a leftover from the July storage overrun on the OTHER project in the org (the HPR media, resolved then); with the spend cap on it only means a future overage would restrict immediately rather than after a warning. **Leave the spend cap on.** Reading a billing banner as the cause of an application error costs an afternoon — check `edge_logs` for an actual 402 before believing it.
- **What was failing is every full-table list read**, as `HEAD /properties?select=id&property_is_deleted=eq.false` → 500, `57014 canceling statement due to statement timeout`. The loaders paged with `.order('property_name').range(from, to)`. **OFFSET cannot skip a row without first producing it**, so page 17 of 17 made Postgres sort all 16,665 live properties and throw away the first 16,000 — and so did every other page, eight in flight at once. Measured on prod: **612 ms for one page in isolation, 4,096 ms mean under real concurrency**, ~3 MB of temp file per page, 13 GB written since Aug 29, against the **8-second `statement_timeout` the `authenticated` role carries**. 256 list loads failed on Sep 2 alone; all 256 were this.
- **`fetchAllKeyset` in `src/lib/supabase.js` pages by PRIMARY KEY** — `WHERE id > $last ORDER BY id LIMIT n` is an index range scan the LIMIT stops early. The same page measured **16 ms**: 38× faster, no sort, no temp file, O(rows) across the read instead of O(rows²). It needs **no count query at all**, which also removes the HEAD full scan that threw the 500.
- **Sequential and still faster, which is the counter-intuitive part.** Page N+1 needs page N's last key so these cannot be fired concurrently — but 17 keyset pages cost ~270 ms of database time against ~70 seconds for the parallel-OFFSET read. Wall time was always dominated by round trips; the database is no longer the thing that falls over.
- **THE DISPLAY ORDER IS RESTORED IN THE CLIENT, AND THAT IS LOAD-BEARING.** `ListView` renders the fetch order **verbatim** when a view carries no saved sort (the `if (sortField)` branch), so handing back rows in key order and stopping there silently reorders every such list. **`src/lib/listOrder.js` is the ONE comparator**, used by the loaders AND by ListView's own sort so they cannot drift. Safe because LEAP's database uses the **ICU** provider with `en_US` (`pg_database.datlocprovider = 'i'`) and `Intl.Collator` resolves the same CLDR data — **verified, not assumed**: 31 adjacent pairs of real property names chosen for the hard cases (a leading `(`, `#`, `/` and `-` inside an address, a bare `.`, digits against spaces, lowercase against uppercase) come back in the identical order, and they are pinned in the fixture.
- **Fixed in passing: the generic object-list loader paged with `.range()` and NO `.order()` at all.** OFFSET without ORDER BY has no defined row order, so a page boundary could legally repeat or drop a record. Keyset paging is ordered by construction. It gets no client sort because it never had a display order to restore.
- **A non-unique key is refused by name.** `outreach_properties_v` LEFT JOINs two tables; both are 1:1 today (16,665 view rows, 16,665 distinct ids, 0 duplicates) so its id is unique — but a second `property_source_data` row would fan the view out and keyset paging would advance past the second copy and drop it. **Silent row loss is the one failure a list must never have.**
- **`fetchAllPagedParallel` is DELETED, not deprecated** — no callers left, and its design (a full-scan HEAD count followed by parallel OFFSET pages) is precisely the defect. `fetchAllPaged` stays for sets that fit in a page or two, where there are no pages to skip and OFFSET costs nothing. **Never page a large table by OFFSET again.**
- `scripts/keyset-paging-fixture.mjs` (42 checks) pins it **with the old behaviour as controls that must still come back wrong**: the OFFSET arithmetic replayed over the real production row count must touch ~9× more rows than there are, the penalty must grow as rows are added, and an insert mid-read must make offset paging repeat a row. Both plausible regressions were confirmed to fail the fixture before shipping.
- **Open:** true server-side pagination (render page 1, fetch on scroll) is still not built — the list still loads every row so its client-side search and filter work over the full set. That is now cheap rather than fatal. Compute is still Micro (1 GB, 2 shared cores, `work_mem` 3.5 MB); a bump is headroom, not a fix.

**Shipped 2026-09-05 — A trigger that re-cases a value is not filling it in (branch `claude/supabase-errors-investigation-8xn4qa`, PR #819; 1 migration live on prod, no DDL, advisors 223 / same known categories, zero naming the changed function):**
*"Create failed — null value in column `building_number_or_name` of relation `buildings` violates not-null constraint"* — seventeen times on 2026-09-03, from the Buildings list, a property record page and a work order page. The user pressed Save, was refused, and eventually found the field themselves (BLD-00198 "1840" landed at 14:41, two minutes after the last failure).
- **`trigger_written_columns` answers "which columns does a trigger fill in"**, and the create pop-up uses it to avoid demanding a value the database composes anyway (the 2026-09-02 ruling). It answered by matching `NEW.<col>` near an `=` in the trigger source, and that was **two mistakes at once**.
- **(1) NORMALISATION READ AS DERIVATION.** The text-case rule shipped **earlier the same day** (`20260903025114`) generates `NEW.building_number_or_name := public.normalize_text_case(NEW.building_number_or_name, 'organization')`. That is an assignment, so the column was reported as trigger-filled and the pop-up stopped offering it — while the database went on demanding it. **`normalize_text_case(NULL, …)` is NULL: a trigger that re-cases what you typed cannot invent it.**
- **(2) A COMPARISON READ AS AN ASSIGNMENT.** The pattern was `:?=` — the `?` makes the colon optional — so `IF NEW.widget_type = 'field_group'`, an ordinary read, counted as *writing* `widget_type`.
- **The rule now: a column is filled when some trigger assigns to it with a real `:=` AND the assigned expression is something other than a re-casing of the column itself.** Keyed on the STATEMENT SHAPE, deliberately not on the generated function's name, because `normalize_text_case` is called from `normalize_property_address` too.
- **846 reported columns become 787. Of the 59 released, 13 are NOT NULL and every one was a create form that could not be completed** — `accounts.account_name`, `buildings.building_number_or_name`, `contacts.contact_first_name`/`_last_name` from (1), nine more from (2) including `photos.related_object`. **Accounts and Contacts were equally uncreatable from the pop-up; buildings is just where somebody hit it first.** The other 46 are optional columns that were missing from create forms **even with "Show all fields" on**, because `neverAsk` is applied before the show-all check.
- **THE FAILURE DIRECTION IS THE POINT, and the migration asserts it**: the new answer is a strict SUBSET of the old one, recomputed from the same catalog, so a create form can only ever gain a field and never lose one. Over-asking is a nuisance; under-asking is a record nobody can create.
- **Controls run both ways.** All ten names LEAP genuinely composes (`building_name`, `contact_name`, `property_name`, `sa_name`, `assessment_name`, `enrollment_name`, `work_step_name`, `work_plan_name`, `user_name`, `saa_name`) must STILL be hidden, or the 2026-09-02 defect is back. The migration also proves its own premise instead of asserting it in prose: `normalize_text_case(NULL)` is NULL and `normalize_text_case('1840')` is `'1840'`.
- **Proved on prod in a rolled-back transaction**: the insert with no `building_number_or_name` is still refused with a `not_null_violation` (so the form must ask), and the same insert with the value supplied returns a building with its derived name. Nothing was written — highest building still BLD-00200, still 90 live.
- **No client change, and that is the lesson worth keeping: the create form's own fixture already modelled this correctly** (it suppresses `building_name` and not `building_number_or_name`). The defect entered entirely through the DATABASE's answer to an RPC, which a pure-function fixture cannot see. **A rule that lives in SQL is invisible to `build:safe`** — assert it in the migration, both directions.
**Shipped 2026-09-05 — Inbound texting is committed, refuses what it cannot verify, and triages a stranger (branch `claude/texting-and-shared-inbox-setup`; no schema change; `twilio-inbound` deployed from CI):**
Nicholas: *"We did get the Twilio approval, so we can start texting. We'll have to set that up."*
- **The audit's first finding is that sending was never the blocker.** `send-notification-sms` is deployed from CI and matches the repo, Twilio credentials are already configured, and **15 real texts have sent** (last 2026-08-18, real `SM…` ids). Approval unblocks VOLUME, not plumbing.
- **`twilio-inbound` was running on production with NO SOURCE IN THE REPOSITORY** — v12, deployed 2026-05, never committed. A branch replay or a redeploy from `master` would have silently lost the entire inbound path. Captured verbatim from the deployed function, committed, and only then changed. Same class as the out-of-band functions logged as open since 2026-07-05, and the reason that item is worth closing: **a function nobody can read is a function nobody can restore.**
- **An unsigned inbound is now REFUSED, not trusted.** v12 skipped signature validation entirely when `TWILIO_AUTH_TOKEN` was unset and merely warned — and **the sending path authenticates with an API key pair OR the auth token, so texts send perfectly while that token is absent**. Twilio signs webhooks with the ACCOUNT auth token and nothing else. An unauthenticated public endpoint that writes into customer conversation threads is an injection vector the moment the number goes live. The failure direction is now refuse, matching the standing rule that the answer is never *accept* when it cannot be verified. **If inbound texts do not arrive, `TWILIO_AUTH_TOKEN` is the secret to set.**
- **A text from an unknown number went NOWHERE, and now goes to the Unmatched Inbox.** v12 called `find_or_create_conversation` for every inbound text; a sender matching no contact produced a thread with no contact, no account and no Related To — visible on no record page, findable only by opening the conversations table. **`unmatched_inbox` already accepts `ui_channel='sms'` and `ui_provider='twilio'` by check constraint: it was built for this and never used** (all 645 rows are email). A stranger's text is filed there for a person to place, which is the same manual path email uses.
- **An existing open thread is joined whether or not the number resolves** — a customer texting back from the number LEAP texted belongs on that thread, and its Related To is already correct, so a reply to a text sent from a work order stays on the work order. A known contact's new thread passes `p_anchor_object='contacts'` so Related To is stated rather than inferred.
- **The triage queue follows the row's CHANNEL.** `fetchRecentEmailConversations` hardcoded `conv_channel='email'`, so an SMS row would have been offered a list of email threads to link to — the wrong answer with no way to say so. Now `fetchRecentConversationsForTriage({channel})`, with the old name kept as an alias so no caller changed. The pane reads `ui_channel`: a text has no subject, so it is headed by the sender's number rather than "(no subject)".
- **Open:** the Twilio number's "When a Message Comes In" webhook must point at `twilio-inbound`, and `TWILIO_AUTH_TOKEN` must be set, before an inbound text can arrive. Neither is checkable from this sandbox. And the repo carries 44 of production's ~60 edge functions — `twilio-inbound` was one of the missing 16.

**Shipped 2026-09-05 — Inbound email works; nothing could tell you so (branch `claude/communication-placement-layouts-a7zgyf`; 1 migration live on prod with DDL, advisors 223 / same known categories, zero naming the new artifacts; `renew-graph-subscriptions` + `inbound-email-webhook` deployed from CI; HA-00216):**
Nicholas, on the session task list: *"Yes, we have to activate the remaining boxes."* Auditing first, before adding four more mailboxes to a system that could not report on the three it had.
- **Inbound email was never broken. 643 messages have been captured across three shared mailboxes since 2026-07-05 and were still arriving that morning** — 376 on `ira@ees-wi.org`, 223 on `ncira@ees-nc.org`, 5 on `assessments.wi@EES-WI.org`. Real programme correspondence: Focus on Energy, Sealed, JCI, Everblue, CMC, property owners.
- **What was broken is that `graph_subscriptions` could not say so, and said the opposite with confidence.** It was written ONCE at creation and never again: all three rows read `gs_status = 'active'` with `gs_expiration` **2026-07-08**, two months past, because `renew-graph-subscriptions` renews at Microsoft and **never wrote the result back**. The renewal RUNS are logged (437 of them, one succeeding minutes before this was found); the SUBSCRIPTIONS are not. **The row would have read identically had the subscription been deleted in July. A status that cannot change is not a status.** And **nothing in the client read that table at all** — no screen, no report — so there was nowhere the staleness could be noticed.
- **`reconcile_graph_subscriptions(jsonb)`** is now the only writer after creation. The renewal job hands over what Graph actually returned; **the absences matter as much as the presences** — a stored subscription Graph does not return is marked **`missing` by name**, with the date and what to do, which is the state the old code could never reach. Only a subscription this run RENEWED gets the new expiry; one not due, or whose PATCH failed, keeps the expiry Graph itself reported. **Stamping the optimistic date on a failed renewal would report health that was never confirmed, which is the whole defect.**
- **`graph_subscription_health()` derives its verdict on every call** from three pieces of evidence — when Microsoft last confirmed the subscription, when it expires, and what mail has actually landed — **never from `gs_status`**. **THE FAILURE DIRECTION IS `Unverified`, NEVER `Receiving`**: a subscription LEAP has not confirmed inside the 12-hour window reads amber even when perfectly alive, which is exactly what the three live rows read the moment this landed and what they stop reading on the next cron run. The migration carries that as a CONTROL that must fail if an unconfirmed subscription ever reports healthy. **Never make an unconfirmed mailbox read green.**
- **The mail itself is the strongest evidence, and it was unattributable.** `unmatched_inbox` never recorded WHICH mailbox a message arrived in — the webhook parses it out of the subscription resource and threw it away — so 643 captured messages could not be counted per box. New **`ui_mailbox`**, written by `inbound-email-webhook` v4 and backfilled from the raw Graph payload's To **and Cc**: **604 of 643 attributed, 39 left NULL** because a message that reached the box on Bcc or by forward names it nowhere and **a guess is worse than a blank**. It is not derivable from `ui_to_address` — 20 rows list only an external address.
- **Setup → Communication Templates → Inbound Email Health** is the screen that did not exist: one row per mailbox with the verdict, the 30-day inbound count, the last message in, and a sentence saying what it means and what to do. Proved on prod in a rolled-back transaction — a full reconcile turns all three **Receiving** with the true expiry; dropping one from the payload turns NC **Not receiving** naming the date; nothing was written.
- **PROVED LIVE ON PROD, not just in a probe.** With `renew-graph-subscriptions` v15 deployed, the renewal job was invoked through `net.http_post` with the cron's own secret rather than waiting for the 00:00 run. All three mailboxes went from *"active, expires 2026-07-08"* — a two-month-old fiction — to **Receiving**, confirmed 18:34 UTC, with their **real** expiries. **And the three different expiries prove the careful half of the rule.** That run reported `attempted: 0` — nothing was inside the renewal window — so **no** subscription took the optimistic date, and every one was written with the expiry **Graph itself reported**: NC at 2026-09-08 16:00, which is the renewal the 18:00 cron had genuinely performed half an hour earlier, and `ira@` / `assessments.wi@` at 2026-09-06 22:00, which is what they really hold. A blanket stamp of `new_expiration_iso` would have printed 2026-09-08 16:34 on all three and claimed three renewals that never happened — reporting health on no evidence, which is the defect, restaged inside its own fix.
- **Where this leaves the four remaining mailboxes.** `assessments@ees-mi.org`, `assessments@ees-co.org`, `assessments@ees-in.org` and `assessments@ees-nc.org` all read **Not set up** — inactive in LEAP, no Graph subscription, nothing sent and nothing received. Creating a subscription for a mailbox that does not exist in the tenant, or has no DKIM, produces a dead endpoint that would have looked healthy before this change and now reads honestly. **The tenant half is Nicholas's** (the mailbox exists and DKIM is signed); the LEAP half is one `create-graph-subscriptions` run per box, and the health screen is what confirms it took.
- **Open, and the biggest single thing left in Communications: the triage queue has 640 rows awaiting triage and has never been worked once** — every message since 5 July. That is not a plumbing fault: under the manual-logging ruling the queue IS the workflow, and it needs to be usable enough that somebody empties it. **282 of the 643 are messages EES itself sent** (the shared box was on the To or Cc of a colleague's mail), which is noise a person should not have to hand-sort. `ees-wi_sealed@EES-WI.org` receives mail and is not in `outbound_mailboxes` at all. **And separating our own mail from a customer's cannot be DERIVED, which is why this is not a five-minute filter**: deriving EES's domains from `users.user_email` returns **`gmail.com`** (a real user record carries a personal address), so a domain rule would file every customer writing from gmail as staff — wrong in the direction that matters most. Matching the exact address instead catches only **160 of the 282**: **122** more come from an EES address belonging to a colleague with no LEAP user record, or from the retired `anuraenergy.org` / `nc-ees.org`. **EES's own domains are knowable in advance and are not written down anywhere** — the same shape as the legal suffixes in the text-case work, which had to be declared for exactly this reason.

**Shipped 2026-09-05 — A thread is RELATED TO any record: Communications on every object, not twelve (branch `claude/communication-placement-layouts-a7zgyf`; 1 migration live on prod with DDL, advisors 223 / same known categories, one lint naming a new artifact and deliberate; `send-notification-sms` deploy from CI; HA-00214, HA-00078 corrected):**
Nicholas: *"I really want this on most objects. I want to be able to put communications on wherever I want: on projects, on work orders, on anything. If I'm communicating, I want to be able to log emails, phone calls, chat messages, and everything else to that specific object. I don't want to keep doing this one by one. Why are there limits? I don't think there should be."*
- **The limit was the premise, not a list.** A thread could only belong to an object with its OWN COLUMN on `conversations` — twelve of them, derived from the table's foreign keys on 2026-09-03. Every new object was a schema change; "one by one" is what that forces. **Salesforce answers this with Related To (WhatId), and so does LEAP now**: `conversations.conv_related_object` + `conv_related_id`, the same polymorphic shape `activity_relations` already uses for a logged call.
- **The twelve foreign-key columns STAY, and a Related To that names one of them also fills its column** (`trg_0_conversation_related_to`) — they carry the owner-chain visibility rule, the state-scope paths and the account roll-up, and every existing caller keeps working unchanged. A legacy caller that supplies only per-object ids gets its Related To DERIVED from the most specific one (the same leaf-first order as `CONVERSATION_ANCHORS`); the 12 live anchored threads were backfilled that way, and the 2 with no anchor at all stay unrelated.
- **Which objects is derived, not listed: `conversation_related_to_objects()`** — the twelve, plus every record-carrying table (a `<prefix>_record_number` column) with a live page layout. **72 on prod.** Two exclusions by rule: `users` (a person on a thread is not a record it belongs to — the rule the FK derivation already applied) and `conversations` (a thread about a thread). The trigger refuses anything else BY NAME and refuses an id that names no record. The client keeps **no list of objects at all** now — `objectCanHoldConversations` is the two-exclusion rule; the palette offers the card on every object.
- **Everything that read the twelve reads Related To:** the feed (`list_communication_timeline` matches the pair OR the column), `find_or_create_conversation`'s generic pair (so `send-email-v1` and the Outlook add-in needed no change), `import_email_into_conversation` (PATCHED IN PLACE, four anchored slices, RAISES if any is missing), the Outlook picker (`list_email_log_objects` / `email_log_target_columns` / `search_records_for_email_log` — the last two also patched to tolerate a table with no soft-delete or updated-at column), the SMS send (`send-notification-sms` + `sendNewSms` carry `anchor_object`/`anchor_record_id`), `is_record_owner_in_chain` (the Related To record's owner sees the thread — `record_owner_id()` reads `<prefix>_owner` off the table), and the state scope (a `polymorphic_lookup` row on conversations; resolver + policy reinstalled; integrity zero).
- **An email from ANY record has to come from a state mailbox, and the resolver knew twelve objects by hand.** `record_state_for(object, id)` walks `record_state_scope_sources` — the registry the geographic access engine already runs on — own-column / parent / child-reverse / polymorphic, depth-capped, first state wins. `resolve_outbound_mailbox_for_anchor` is patched in place to fall through to it (so the twelve keep their hand-written paths byte for byte). Proved: a work step → `work_orders.property_id -> properties.property_state` → `ira@ees-wi.org`.
- **The state-scope dispatcher said `false` for every object without a resolver — wrong for platform configuration.** A thread Related To a vehicle would have been hidden from a state-restricted user though vehicles are never scoped. `record_in_state_scope`'s ELSE now asks the registry (`record_state_scope_object_is_never_scoped`); an unknown object still fails closed, asserted in the migration.
- **Also-relate-to is DERIVED now, on every object** (the twice-logged open item): `list_relatable_records` walks the record's own foreign keys one hop, then the parents' (people, picklist values, the record itself and platform configuration excluded — a call is not "about" a price book). Reproduces the five hand-written branches on real data and adds what they missed (an enrollment's management company; a work step gets nine). **A defect worth recording: the first cut raised `recursive query "walk" column 1 has collation "default" … but "C" overall`** — catalog names carry collation C and a text parameter does not; a recursive CTE needs one. Caught by RUNNING it after apply (the dry run had errored earlier, in a probe), fixed with `COLLATE "default"` on the catalog names.
- **The `Log Activity` topbar action was gated to four objects** (`applicableObjects`) — now `ALL_OBJECTS`. The Communications card's own Log a Call was never gated.
- **Proved on prod in rolled-back transactions:** a thread related to a work step lands on the work step's feed, resolves to the Wisconsin mailbox, is in scope for WI and NOT for NC; one related to a work order also fills `work_order_id`; a legacy contact+account caller gets Related To = accounts; `work_step_field_values` (no page), `users` and a made-up id are each refused by name; the Outlook picker offers 72 objects and finds vehicles and work steps. **A probe trap worth recording:** `WHERE c.id = find_or_create_conversation(...)` calls a VOLATILE function once per scanned row and inserted twice — compute the id in a DO block first.
- **Client:** `conversationAnchorConfig()` is what a card stores (`related_object` + `fk` or null); `resolveAnchorFromConversation` prefers Related To; `fetchConversationsForParent` matches the pair or the column. Fixtures 21 → 31, 43 → 44, 69 → 72, each with the two exclusions as controls.
- **The reply token carries across all 72 objects, and it is PROVEN, not assumed (checked 2026-09-05).** `send-email-v1` derives the plus-address token from the CONVERSATION id (`c_` + the first 8 hex chars) and sets it as `replyTo` on **every** send, unconditionally — so the moment conversation creation went polymorphic, so did the token. Two live round trips on prod: `bmcafee@mabmechanical.com` replied to **`ncira+c_d7a165ef@ees-nc.org`** and landed on CONV-00049, `joe.petkus@actionwi.com` replied to **`ira+c_2553e251@ees-wi.org`** and landed on CONV-00050 — both Related To an account. A third reply came back to the BARE address and still threaded, on the In-Reply-To arm. **The client-side `slice(0,8)` after stripping dashes and the generated column's `substring(id::text,1,8)` agree for every uuid**, since the first dash sits at position 8 — worth knowing before anyone "tidies" either one.
- **CORRECTED (2026-09-05): the earlier note that "the Outlook add-in's link-a-contact search is still fixed to accounts" was WRONG, and is withdrawn.** That search is in the **create a contact** flow and is asking which COMPANY the new contact belongs to — `contacts.contact_account_id` points at accounts, so searching accounts is the only correct thing it could do. There is nothing to widen. Do not "fix" it.
- **Open:** the conversations record page shows no Related To field yet (the columns exist; the layout was not touched). `RELATED_MERGE_ROOTS` in `send-email-v1` hydrates parent merge fields for ten objects; any other object gets its own columns only.

**Shipped 2026-09-05 — A card goes anywhere, as many times as an admin wants: no card is limited to one place on a layout (branch `claude/communication-placement-layouts-a7zgyf`; no schema change):**
Nicholas, adding Communications to an enrollment layout: *"Why can't I have my communication on the right sidebar of a page layout and in the related list? We went over this before. I should be able to set this up anywhere. This shouldn't be limited just to one area,"* then *"it should go for any objects."*
- **The palette refused a second Communications, Work Plan or Publish History card** — `onePerLayout` in `src/lib/layoutCards.js`, written 2026-08-27 on the theory that "a record shows one". **The palette alone said so**: the record page draws every widget row it is handed, in the main flow and the rail alike, and the **Copy to…** menu on a placed card never had the gate. The same rule had two answers depending on which button was pressed.
- **The flag is gone from all three cards, and `availableCards` is decided by the OBJECT alone** — what is already on the canvas never disables a card. A card already placed still shows its *"already on Related, Right sidebar"* badge (`cardPlacements`), so a second placement is a choice made with the facts on screen, never a refusal. The object rules are untouched: Photos still only where a bucket exists, Work Plan only on work orders, Communications only where `conversations` carries a foreign key.
- Nothing in the database counts cards per layout (`validate_page_layout_widget_config` checks config, not multiplicity; the 2026-09-03 migration's "more than one" assertion was a one-time check of that migration's own seeding). `scripts/layout-cards-fixture.mjs` 64 → 69 checks, pinning that a third Communications card on an enrollment is offered and that no catalog entry carries the flag. **Never reintroduce a one-per-layout rule on a card; if a card genuinely cannot render twice, fix the card.**

**A HOMES payment request invoice reads TOTAL DUE $0.00 BY DESIGN — never "fix" it (Nicholas, 2026-09-03).**
*"There is no cost. Customers don't pay us anything. The program pays us. This
invoice is just fake so the customer can see that there's no out-of-pocket cost.
We get paid through a different mechanism."* The document exists to show a
property owner they owe nothing, so `computeHomesModel` **defines** Total Project
Cost as the rebate (`total = homesAmt + foeAmt`) and then applies that same
rebate as an instant discount, cancelling to zero on every invoice; the measure
rows are a breakout of that one total, which is why they are fractions and not
prices. **The opportunity line items are the only real dollar figures in LEAP and
are deliberately NOT read by this invoice** — pricing it from them would print a
balance and tell a customer they owe money they do not owe. EES is paid by the
programme, outside this document. The invoice reads no project either: a project
manages work orders and has nothing to do with these dollars. The readiness gate
is the two Asset Score reports, a unit count and a contractor — nothing else.
Recorded at the money line in `src/lib/homesProposal.js` and here, because a
zero total and a cost derived from its own credit both read as defects to
anybody who has not been told otherwise.

**Shipped 2026-09-03 — The Customer Information block names the customer, and nothing shouts (branch `claude/wisconsin-multifamily-ira-cleanup-roy6o0`, PR #799; 7 migrations live on prod, 3 with DDL, advisors 223 / same known categories, zero naming the new artifacts; HA-00212):**
Nicholas, on the Wisconsin IRA Multifamily HOMES **Project Reservation** (Sealed): *"There's something really going on goofy in the customer information… I don't know why Waukesha is all capitalized. We already put a bunch of stuff in to normalize our data. We shouldn't have all caps anywhere like that."* The block read `Dennis Hanson - Vice President- Housing & Residential / PO BOX 304 / WAUKESHA, WI 53187, Alexandria, VA 22314`. **Four defects, only one of which was the capitals.**
- **IT NAMED NO CUSTOMER, because it read the wrong object.** `enrollments.enrollment_owner` is the **RECORD's** owner — the LEAP user, which is what "every record has a named owner" means here. It holds a `users` id on **all 50 live enrollments** (Nicholas, Lucas and Brittin Wood) and carries **no foreign key** to say so, and `enrollmentProposalContext` looked it up in `accounts`. That found nothing and returned `''`, so **every HOMES and HEAR proposal has silently opened its customer column with a person's name where the customer company belongs.** Same misread of `ia_owner` in `homesProposalService`, masked only because `ia_property_owner_name` is tried first. Both now resolve the owner from the **property's account** (one account per real-world company; `properties.property_account_id` is the owner).
- **The address was guessed apart at the FIRST comma.** Most live enrollments carry `6737 W Washington Street, Suite 2275, West Allis, WI 53214`, which printed the suite on the city line. **`src/lib/ownerAddress.js`** prefers the account's **structured** billing address and parses free text **from the END**, where the city/state/ZIP shape is unambiguous. `scripts/owner-address-fixture.mjs` (39 checks) carries the **first-comma rule as a positive control that must come back wrong**.
- **Nothing normalised case outside `properties.property_street`.** `normalize_street_address()` has done this carefully since the importers landed and was wired to **exactly one column** — which is why `property_street` shouted on 3 rows of 16,665 while `property_aka_name` shouted on 14,285, `property_lihtc_project_name` 9,052, `property_std_address` 5,951, `account_name` 2,151, `property_hud_owner_org` 1,108. **`normalize_text_case(value, kind)` is now the one rule**, and the columns it governs are **DECLARED** in `text_case_normalized_columns` (55 columns, 7 objects), which `install_text_case_normalization()` turns into a per-object trigger. **44,392 stored values corrected**, every one recorded before and after in **`text_case_normalization_log`**, so the rewrite is reversible.
- **THE SAFETY RULE is what makes a 44k-value rewrite safe: a value already carrying a lowercase letter was cased on purpose and is returned untouched.** The one exception is **`address` and `city`, which recase WORD BY WORD** — an address holds no brand names or credentials and a name does. The live counter-example is `Mary P Fox, CEM` (a Certified Energy Manager, appearing all-upper exactly once in the corpus, so no derivation can see it): a word-level pass over names prints "Cem". **Names therefore keep the whole-value rule, and ~840 names stay partly shouted as a measured consequence** — 615 `property_aka_name` (`333 HOLLY - The Woodlands`), 153 `account_name`, 73 `property_hud_owner_org`. Widening it is one line in `_normalize_text_case`.
- **The acronym vocabulary is DERIVED from LEAP's own corpus, not listed** — a token that appears all-upper inside a value that is NOT wholly shouted (capitalised deliberately beside lowercase words), or that has no vowel at all; both additionally required never to appear in mixed case anywhere, which is what throws out APARTMENTS and MINNESOTA. **You cannot tell an acronym from a shouted word by looking at it** — ACC, CAP, DAK and MRCDC sit in the same column as HORSE, WOODED and KAPLAN. **Failure directions are deliberately asymmetric: over-preserving leaves one word shouted (visible, fixable by deleting a row); under-preserving prints "Acc Management Group" on 62 properties.** Legal suffixes (LLC/LP/LLP/PLLC — the corpus already holds badly-cased copies, so the derivation's "never written mixed" test throws them out; **the one place reading the data gives the wrong answer, because what is in the data IS the defect**) and the 56 USPS state/territory codes are written down instead, because those are knowable in advance. 494 → 561 tokens.
- **A job title's dash spacing is fixed whatever its casing** — a hyphen with whitespace on exactly ONE side is a mistyped dash; `Non-Profit` is not.
- **Deliberately NOT registered:** every `property_*_raw_*` column (the import's own transcript, and the evidence a match was made correctly); `ia_program_name` (a CODE — the calibration run turned `WI-IRA-MF-HOMES-AUDIT` into `WI-Ira-MF-Homes-Audit`); and `property_street`/`property_city`, which `normalize_property_address()` already owns — **two rules on one column is the thing this change exists to prevent**.
- **Generated trigger functions are SECURITY DEFINER with EXECUTE revoked in the same migration** — the settled answer to the conflict that broke record writes on 2026-07-27 and again 2026-08-29. Zero new advisor lints from ~60 generated functions.
- **Three findings from running it that are worth keeping.** (1) **`= ANY(text[])` is a linear scan**: comparing every word against 494 tokens cost 5.0s on one property column; `jsonb ? key` is a hash probe and the same query runs in **1.5s**. (2) **The backfill's prefilter stopped being a superset the moment addresses went word-level** — it skipped any value containing lowercase, and `verify_text_case_normalization()` (which re-checks EVERY row with no prefilter) caught it immediately: *"still shouting — accounts.billing_street: 33 rows"*. A prefilter that silently skips rows otherwise looks like a clean run. (3) **The backfill now disables the triggers whose function body mentions NONE of the columns it writes** — derived from `pg_proc.prosrc`, not listed. `properties_recompute_rollups` fires FOR EACH ROW unqualified and recounts a property's children 14,285 times for a display-name change; a trigger that cannot see the change cannot be needed by it.
- **`session_replication_role = replica` is no longer available** on this project to either the migration role or the SQL role (the 2026-08-22 backfill used it). Disabling named triggers is the narrower instrument anyway — foreign keys stay enforced.
- **Operational note for the next session: the Supabase MCP tools time out client-side at 60s, and the server keeps going and COMMITS.** Three "failed" applies had in fact landed; the repo filenames had to be renamed to the versions prod recorded. **Always check `supabase_migrations.schema_migrations` before concluding a timeout rolled back**, and expect concurrent sessions to be interleaving migrations on the same database.
- **Open:** eight of the nine enrollment columns the create-prefill fills (`enrollment_owner_organization`, `enrollment_management_agent`, `enrollment_owner_phone`, `enrollment_owner_email`, `enrollment_owner_fein`, `enrollment_management_phone`, `enrollment_management_email`, `enrollment_hud_contract_number`) **do not exist on the table** — that whole Owner Entity / Management block of the prefill writes into thin air. Untouched here; adding eight columns is a schema decision. And `text_case_acronyms` has no record page, so adding a token is a database change today (HA-00212 says so rather than promising a screen).

**Shipped 2026-09-05 — A field drop INSERTS, and fields never trade places (branch `claude/building-movement-bug-dw5kqj`, PRs #823 + #825; 2 migrations live on prod, no DDL, help-article corrections only; HA-00210 corrected twice):**
Nicholas, in the page-layout editor on the assessments **WI-IRA-MF-HOMES-AUDIT** layout: *"In hindsight, it's not even allowing me to move it. I just moved the building over to the right, and it moved the property, the building, and the project back to the left."* Then, after the first cut: ***"I don't want fields to trade places ever. That's never, ever, ever a good functionality. There's no reason in the world we'd ever trade places."*** And the rule itself: ***"If I move something over, it goes in between the two existing fields. That's it, and then you readjust to make sure the rows are horizontally aligned."***
- **THE DEFECT WAS THE ARITHMETIC, NOT THE GESTURE — and getting that backwards cost a round.** The editor resolved a drop as "insert before the tile you landed on" and computed that index in the array **with the dragged field ALREADY REMOVED**. On the real stored array — `[Name, Opportunity, Building, Project, …]` — dropping Building on Project removes Building, finds Project at index 2, inserts at 2, and hands back the array it started with. **Every one-slot forward drag on every layout in the platform was the identity function**, and every longer forward drag landed one cell short of the tile it was dropped on. The classic remove-then-index-the-remainder off-by-one, and the whole of "it's not even allowing me to move it".
- **The first cut read the second half of his sentence as a second defect and made a drop on a tile a SWAP.** It was not: the re-flow he described was the consequence of the field landing in the wrong place, not a complaint about re-flow. **A swap throws the field you dropped ONTO across the section** — dragging Building onto Assessor Name sent Assessor Name from the right-hand column of row 4 to the left-hand column of row 2, a field he never touched, on the other side of the card. That the arithmetic was symmetric did not make it any less surprising, and it reproduced the exact class of thing he was reporting. **Recorded because the mistake is a general one: a user describing a symptom is not proposing a mechanism. He said what he saw; the fix was one line of index maths, and inventing a second gesture on top of it made the feature worse.**
- **There is ONE gesture: a drop inserts.** The field goes where it was dropped and the fields after it move along one place; the rows re-fill so each stays whole. A field the admin did not drag never goes anywhere except along the flow.
- **The one target that pushes nothing is an EMPTY SLOT.** A field dropped on a blank **takes** it — there is nothing there to displace, which is what an empty slot is for — so a field can be put in a particular cell with everything around it left exactly where it is. That, and not a swap, is the answer to "put this field in the right-hand column and move nothing else".
- **Every row's right-hand END carries an insertion line, not just the last row.** *"I moved the building over to the right"* is a gesture aimed at the end of a row, and there was nothing there to drop on — the nearest target was the row below, which is how a drag meant to move one field one cell rearranged the section. Its id needs its own prefix (`insafter::`) because it names the same flow position as the next row's leading line and **dnd-kit keys droppables by id — two boxes cannot share one**.
- **The field family resolves collisions by POINTER, not by nearest box.** An insertion line is 14px of gutter overlaying two tiles, and `closestCorners` — which measures from a box's corners — hands it to the tile beside it every time, collapsing the gestures into one. `pointerWithin` asks which boxes the cursor is actually inside. Keyboard drags report no pointer and still fall through to `closestCorners`.
- **Field cells are deliberately NOT `useSortable`.** Its strategy animates a preview from the tile rectangles measured at drag start, and a section that WRAPS INTO ROWS is not the list that strategy assumes — the tiles slid to the wrong cells. The insertion line and the tile's own *INSERT HERE* highlight say where the field will land instead.
- **`src/lib/fieldGroupPlacement.js` is the one definition** — pure, no imports — and **carries the pre-fix resolution as an exported control**. `scripts/field-group-placement-fixture.mjs` (54 checks) replays the real production field array with that old rule beside it, **required to come back a no-op**, and asserts outright that the field dropped ONTO is never sent to where the dragged field came from.
- **`npm run verify:layout-field-drag`** — a real Chromium performing **real mouse drags** over the REAL `FieldRowGrid` inside a real `DndContext` using the editor's **own** collision detection, with a CONTROL grid on the same page resolving drops the old way that must still do nothing. 16 checks, including his exact gesture: Building dropped off the right-hand end of its row lands in the right-hand column with **every row below it untouched**. **Reading the source is not verification of this defect** — which droppable a pixel belongs to is a fact about boxes a browser laid out. Not in `build:safe` (needs a browser binary). The first run also caught the thing no unit test could: **a tile is picked up by its ⠿ handle**, so a drag aimed at the tile's centre never starts.
- **A trap the change itself introduced, caught by re-running the OTHER browser check:** `verify:page-layout-alignment` read the editor's cells by DOM position (`cell.firstElementChild`), and the new absolutely-positioned insertion lines made every cell read as blank while all 64 checks still passed. Rows and cells now carry `data-field-row` / `data-cell-kind` / `data-cell-label` and the reader asks for them by name. **A positional DOM reader in a check is a check that can go quietly blind.**
- **No schema change.** A spacer carries no name, so `validate_page_layout_widget_config` skips it, and `trg_0_strip_field_group_derived_column` still strips a stored `column` — both proved on prod in a rolled-back transaction (a probe insert carrying a hand-added spacer AND a deliberate `column: 2` was accepted and came back with the column gone; nothing was written). The two migrations correct **HA-00210 in place**, each with the claim it falsifies as a control that must be gone — first *"you cannot leave a hole by accident"*, then every mention of a swap.

**Shipped 2026-09-03 — A field's position in a section is ONE fact, so rows cannot stagger (branch `claude/page-layout-alignment-3g7syl`; 2 migrations live on prod, no DDL, advisors 223 / same known categories, zero from this change; HA-00210, HA-00116 + HA-00065 corrected):**
Nicholas, on an account's Service Provider Information card: *"We shouldn't have staggered rows on page layouts. If something's like two or three lines, the other ones just need to adjust. I don't understand this. There's only one field on each side, but they're staggered. This can't happen... This really does make the whole site look crappy."*
- **The section held two fields and stored them as `[account_fein column 2, account_tax_classification column 1]`.** Two facts described one position — the field's INDEX (reading order) and a `column` number — and **nothing kept them in agreement**. The page-layout EDITOR wrote `column` as a column-FILL model (three drop zones, each an independent stack, order between them meaningless); the record PAGE read it as a row-MAJOR grid and set `grid-column-start` from it. CSS grid only places an item in a cell at or after its cursor, so the field pinned to column 1 could not go beside the one pinned to column 2 that preceded it: it dropped to the next row and left the cell beside each of them empty. Two fields, four quarters, two blank.
- **It was never one section.** The platform ran **three** placements over one config: row-major-from-`column` (the holes), a column-fill grid of independent stacks for 3-column sections (rules never aligned across a section), and a viewport-width `auto-fit` for the **752** groups carrying no column at all — which **ignored `section_columns` entirely** and rendered the same section 2-up or 4-up depending on the browser window.
- **The second fact is gone.** `src/lib/fieldGroupLayout.js` is the one definition, rendered from by the record page AND the layout editor, so they cannot disagree again. A field's index is its position; the column is derived. `column` is no longer read, no longer written, and was **stripped from all 1,078 stored field groups**.
- **The rule is the row, not the cell.** Every field used to carry its own bottom border with the cells top-aligned, so a value wrapping to three lines pushed its rule below its neighbour's — the broken-ladder half of "staggered". A row is now a real row: its cells always sum to exactly the column count, the ROW draws the rule, and the cells stretch to the row's depth. "The other ones just need to adjust" is that stretch. A short final row is **padded**, so its rule still runs edge to edge instead of stopping halfway.
- **Reading order was preserved, not reshuffled — which is why the migration is not a one-line strip.** The **224** groups whose fields ALL carried a column were genuine column-fill layouts and their columns are frequently semantic (an account's billing address is a four-field stack in column 1 beside contact methods in column 2). Those were linearised row by row across the stacks, with an explicit **`spacer`** where a column ran out — 72 of them, in 46 groups — so the layout an admin built survives. Groups carrying `full_width` / `layout:'rows'` (28, seeded 1:1 from paper forms) and groups with partial or no columns (776) kept their array order.
- **No hole on the platform was deliberate, which is what made closing them safe without per-layout judgement: there were ZERO `spacer` fields in all 1,078 groups**, and the editor has never had a control that could author one. Every blank slot on a record page was an accident of the two-fact model.
- **`section_columns` is now honoured and is a CEILING, not a promise.** How many of those columns fit is measured from the container, not the viewport, so one rule serves the main flow (3), the 480px right rail (2) and a phone (1). The fitting width is **230px, not a round 240** — measured in Chromium, where 240 missed the rail's two columns by the card's 2px of border and collapsed every rail section to one.
- **A defect of the fix, found by the browser and not by reading: a 3-column section reflowed into the rail repacked its spacers, two landed together and painted a row with nothing in it** between Billing State and Billing Zip. Spacers hold a slot in the shape a section was DESIGNED at, so they are dropped whenever it renders narrower, and a row left holding nothing is not drawn.
- **The editor is WYSIWYG against the same function.** The three column drop zones are gone — that UI could not state which ROW a field was on, and the number it wrote was read as a pinned slot. Fields are one sortable list drawn as the packed row grid; a tile's **↔** toggles full width; a spacer renders as a deletable *Empty slot*. `normalizeColumns` — which ASSIGNED `column` round-robin `(i % cols) + 1` on load and is where most of these numbers came from — is now `stripDerivedFieldColumns`.
- **The invariant enforces itself, because four migrations from sessions running alongside this one landed on prod within seven minutes of it — two of them editing page layouts.** A one-time strip can be undone by anything that WRITES a field group: a stale browser tab, another session's migration, a branch replay in a different order. `trg_0_strip_field_group_derived_column` removes `column` on the way in — it strips rather than raises, since a save right in every other respect should not be refused over a key nothing reads and an admin could not act on the error. SECURITY INVOKER, so zero advisor lints; proved by an insert in a rolled-back savepoint (a probe cannot clean up after itself — `block_hard_delete()` refuses the DELETE). Verified after the concurrent sessions' layout writes: still **0** fields carrying a column.
- **`npm run verify:page-layout-alignment`** — a real Chromium at 1180 / 480 / 350px over the **REAL** `FieldGroupWidget` and the **REAL** editor grid with the real post-migration configs, measuring that every row tiles the section with no gap, that a row's cells share a top AND a bottom, that each rule spans the full width, and that the editor draws the same rows the page does. **The pre-fix placement is on the same page as a positive control and must still come back staggered.** 64 checks. Not in `build:safe` (needs a browser binary). **Reading the CSS is not verification of this defect** — every line of it read correctly. Build gate: `scripts/field-group-layout-fixture.mjs` (80 checks, replaying the real prod column sequences with the old placement as a control that must leave holes).
- **From the audit, and fixed here: 35 sections across 10 objects were still called "New Section"** — the label the canvas editor's Add Section button writes — each painting that placeholder as a heading on a live record page (units 8, opportunities 8, assessments 5, projects 4, properties 3, incentive applications 2, enrollments 2, work orders / products / accounts 1 each). It is the same fact as the "Untitled Section" placeholder the renderer already treated as unnamed, so it now gets the same treatment: the fields render, the placeholder heading does not. **Naming them is a business decision and was not guessed at.**
- **Open:** some column-fill sections were authored with badly uneven columns (a contact's Personal Information puts 3 fields in column 1 and one each in columns 2 and 3), so they render tidy but sparse. That is the layout as built, not the renderer — an admin can now see it in the editor and drag it. The `section_config_editor` widget (project report templates) still uses its own `auto-fit` grid with per-cell rules; it is admin schema config, deliberately not folded in.

**Shipped 2026-09-03 — Dump Trailer Disposal Run, and the technician-created work orders that were silently dead (branch `claude/dump-trailer-work-order-lzdiby`, PR #797; 4 migrations live on prod, no DDL, advisors 223 / same known categories, zero naming the new artifacts; HA-00209):**
Nicholas: *"creating a work order for taking a dump trailer to the landfill to dump it… the trailer untarped, the back gate locked, the trailer connected to the truck, the trailer empty at the dump landfill, the receipt after it's been paid. Make the list, make it logical."*
- **Building it found that the whole family it belongs to was broken THAT MORNING.** `enforce_service_appointment_is_for_an_assessment` refuses an appointment on any non-assessment work order — correctly — but **four functions still booked one unconditionally**: `create_technician_work_order`, `create_technician_work_order_for_property`, `ensure_daily_building_access_work_order` and `open_monthly_vehicle_equipment_checks`. So the entire LEAP Pad **Create Work Order** menu raised `P0001` and created nothing, and because the third is a trigger on `service_appointment_assignments`, **putting a crew member on an ASSESSMENT appointment failed too** on any work order carrying a building. **A rule and its callers ship together; the rule was right and its four callers were left behind.**
- **The fix is the ruling, not an exemption:** a technician's day is a list of WORK ORDERS, so the four now stamp **`assigned_technician_id` + `work_order_scheduled_start_date`** instead of booking a visit. `my_service_appointments` already selects `FROM work_orders`, so the new stop lands on the right person's day with no appointment and no customer in the notification pipeline. The two ASSESSMENT creators are deliberately untouched.
- **Patched in place against the deployed definitions, never re-emitted** — each migration reads the function from the catalog, cuts one anchored slice, and RAISES if an anchor is missing **or if the slice it is about to remove does not contain the appointment insert**. Retyping 130 lines of building/unit/project resolution to delete two statements is how one gets corrupted.
- **A regression introduced by that fix, caught by RUNNING it:** the day's access stop deduped on the appointment it no longer books, so a **second** crew member on one visit opened a **second** access stop (WO-00275 and WO-00276, same building, same day). It now asks the work order's own scheduled date, keeping the old appointment test as a second arm so the 19 stops opened before the change are still recognised — the check can only ever find MORE rows, never fewer. **Reading the patch would not have shown this; the behavioural probe did.**
- **The run itself is three steps because his five items are three PLACES**, not one list: three at the job site before the truck moves, one at the facility, one at the scale house. Ordered plan (`wpt_allow_any_order = false`), each step a screen flow, each verified Lead Technician → Project Site Lead. **Loaded Trailer Ready to Leave the Site** (trailer · load untarped · back gate locked · trailer connected · **load tarped for the road**) → **Load Dumped at the Disposal Facility** (**facility name** · trailer empty) → **Disposal Receipt and Cost** (paid ticket · **net weight** · **amount paid** · **paid with**). Bold are additions: an uncovered load on a highway is a citation, and the receipt is a picture while weight and cost are the two numbers a report can add up.
- **The facility is free TEXT, deliberately, not a picklist** — nobody has given LEAP the list of landfills, and a dropdown that does not contain the place you are standing in blocks the driver. It reads off the ticket in his hand.
- **Exactly ONE `vehicle` field, and that is a constraint, not a preference:** `save_work_step_vehicle` stamps `work_orders.vehicle_id` **unconditionally**, so a second vehicle field silently overwrites the first. The trailer is the asset going to the landfill; the truck towing it is in the hitch photo. The migration asserts the count.
- **Net Weight is the only prompt that may be blank** — many construction-and-demolition facilities price by the cubic yard and never weigh you, and a required field that cannot be answered is a driver stuck in a queue. The migration asserts that nothing ELSE is optional.
- **No fleet records were invented.** The fleet had one vehicle record type; a **TRAILER** type was added so the real trailers can be entered in LEAP Admin → Fleet. Until they are, the Dump Trailer prompt is skippable rather than blocking.
- **Proved on prod in rolled-back transactions, impersonating a Project Site Lead under RLS:** the exact failing call now succeeds and appears on his Today list marked *Assigned* with **0 appointments** and 5 steps; the ad hoc path's four guards still refuse by name; three crew assignments open **exactly one** access stop; the monthly opener still creates 4 then skips 4; the run returns its three steps in trip order, all screen flows, and **each step's evidence gate refuses by name**. **Nothing was written.**
- **Open:** the dump trailers still need entering in Fleet (names and plates), after which the prompt can be made required. And `open_monthly_vehicle_equipment_checks` dedupes on `work_order_created_at`'s month rather than the month it was ASKED for, so an explicit past or future month re-creates — pre-existing, and the cron always passes the current month, so it is not live-broken.


**Shipped 2026-09-03 — Communications on enrollments and incentives, and the object is called Incentives (branch `claude/enrollment-incentive-comms-rename-xnpmz5`; 3 migrations live on prod, 1 with DDL, advisors 220 / same known categories, zero naming the new artifacts; `send-email-v1` v24 live; HA-00208, HA-00078 corrected):**
Nicholas: *"we need to have a communication on all enrollment objects and all incentive record objects,"* then *"communication is a related list object. With lots of functionality for emails, logging calls, etc."* And: *"I want to rename incentives. The name of the object should be incentives, not incentive applications,"* plus the enrollment record type `WI-IRA-MF` → **Wisconsin IRA Multifamily Income Qualification**.
- **The card was on 21 of 24 incentive layouts and could not go on an enrollment at all** — `conversations` carried no `enrollment_id`, so a thread could not be anchored to one and the card would have held nothing.
- **WHICH OBJECTS CAN HOLD A THREAD WAS WRITTEN DOWN NINE TIMES.** The CASE in `list_communication_timeline`, the CASE in `import_email_into_conversation`, the parameter list of `find_or_create_conversation`, an ordered if/else chain in `conversationsService`, `CONV_COLUMNS` and `SUPPORTED_FK` in the same file, `OBJECT_CONVERSATION_FK` in `layoutCards`, `FK_TO_ANCHOR_OBJECT` in `ConversationPanel`, `TABLE_META.conversations` in `RecordDetail`, and `ANCHOR_FK_PARAM` in **two** edge functions. Adding enrollments meant editing all of them, and the failure of missing one is silent: **an anchor object the map does not know is sent UNANCHORED — the email goes out and then appears on no record page at all.** Same class as the hand-written lists fixed on 2026-08-24, 2026-08-31 and 2026-09-02.
- **The database now reads it off the table's own foreign keys.** `conversation_anchor_columns()` derives object → column from `pg_catalog`, excluding `users` (a person on a thread is not a record it belongs to) and `picklist_values`. It **reproduces the hand-written CASE exactly** — proved against real data before shipping: the old 11-way CASE and the derived registry return the same 16 (conversation, object, id) pairs, zero either way — and the migration RAISES if the derived object list is not the twelve expected.
- **`find_or_create_conversation` gained a generic `p_anchor_object` / `p_anchor_id` pair** resolved through that registry, so **`send-email-v1` no longer carries an anchor map at all** (v24, deployed and read back: boots, validates, and its source matches the repo). An object with no anchor column now **RAISES by name** instead of quietly losing the thread. The per-object parameters are untouched, so every existing caller is unchanged.
- **Two functions were PATCHED IN PLACE, not re-emitted** — `import_email_into_conversation` (10 KB of parsing that has nothing to do with this change) and `resolve_outbound_mailbox_for_anchor`: the migration reads the deployed definition, replaces exactly one block, and RAISES if it cannot find it. Retyping a working function to alter four lines is how one gets corrupted.
- **An email from an enrollment has to come from somewhere.** The mailbox resolver had no enrollments branch, so a send would have failed with *"no mailbox could be resolved"* — the card present and the send broken. Enrollments carry no state of their own (`enrollment_payment_state` is a payment status), so it walks property → opportunity. Proved live: `enrollments.property_id -> properties.property_state` → `ira@ees-wi.org`.
- **Logging a call on an enrollment or an incentive now offers its parents** (property, building, opportunity, project, account) — `list_relatable_records` knew only opportunities, projects and contacts, so a call logged on either never reached the account.
- **Geographic access follows the anchors.** `record_state_scope_conversations` resolved through six parents; a thread anchored ONLY to an enrollment or an incentive resolved to nothing and — the engine fails closed, correctly — was invisible to every state-restricted user. Both paths registered in `record_state_scope_sources`, resolver and policy reinstalled, `record_state_scope_integrity()` zero.
- **The card has ONE name.** It was seeded as "Conversations" on 47 layouts while the palette that places it has said "Communications" since it shipped. All 47 renamed; `CONVERSATION_CARD_TITLE` is now the single definition the palette, the panel and its default title all read.
- **Client: `src/lib/conversationAnchors.js` is the one client definition** — the ordered anchor list (most specific leaf first, most canonical root last, which is the one thing the foreign keys cannot say: it decides which record a reply is sent from), with both maps and the reply resolver derived from it. `scripts/conversation-anchors-fixture.mjs` (21 checks) pins it, with the two silent failures as controls: a thread fetched without its anchor column reads as unanchored, and a thread with no anchor must resolve to null rather than send from nowhere.
- **Proved on prod in a rolled-back transaction**: the generic anchor lands a thread on `enrollment_id`, the enrollment's feed returns it, an unanchorable object is refused by name, and the enrollment offers 4 relatable parents (an incentive, 5). Coverage after: **10 of 10 enrollment layouts and 24 of 24 incentive layouts**, none with two.
- **The rename is a LABEL rename, and deliberately not a table rename.** `incentive_applications`, its `ia_` prefix, its foreign keys and every URL already pointing at it stay — renaming them would rewrite a hundred references to change a word on a screen. `objectLabel()` / `objectLabelPlural()` in `objectNav.js` is the one definition; the **six** modules that each carried their own table → label map (object catalog, Setup, global search, the record page, the home-page renderer, the generated-document modal) now read from it.
- **The nine statuses were renamed values AND labels together**, because LEAP names a status `[Object] [State]` and leaving "Incentive Application To Be Prepared" under an object called Incentive breaks the platform's own rule. Safe because the column is a uuid FK — checked first and found **zero** report filters, saved list views, dashboard widgets or page layouts carrying the text, and exactly ONE function resolving a status by name (`inherit_incentive_application_from_enrollment`), patched in the same migration. Reports, list views, the layout and the email template renamed; **26 help articles corrected in place** (six spellings, not two — "Incentive applications" in a bold mid-sentence and lowercase forms in TITLES were what the first pass missed).
- **`WI-IRA-MF` reads "Wisconsin IRA Multifamily Income Qualification"** — the LABEL, since the VALUE is the API name the price books and `submittedEnrollment.js` key off. `derive_enrollment_name()` composes an enrollment's name from that label, so the three live enrollments were touched and now read "… - Wisconsin IRA Multifamily Income Qualification" instead of "… - WI-IRA-MF".
- **`.github/workflows/deploy-edge-function.yml`** added so the next large edge-function deploy runs from CI rather than being re-emitted through the model — the standing constraint recorded on 2026-08-31. **It needs one repository secret, `SUPABASE_ACCESS_TOKEN`, which only Nicholas can add.**
- **Open:** the Outlook add-in's own "Log Email" path (`log-email-to-record` + `search_records_for_email_log`) still carries its own five-object list and does not offer enrollments or incentives — a separate surface, deliberately not widened here. The other enrollment record types (`WI-IRA-SF`, `NC-IRA-MF`, …) still read as terse codes; only `WI-IRA-MF` was asked for. And `list_relatable_records` is still an if/else per object rather than derived from the child's own foreign keys.

**Shipped 2026-09-03 — A thread is RELATED TO a record (branch `claude/enrollment-incentive-comms-rename-xnpmz5`; 1 migration live on prod, no DDL):**
Nicholas, after asking what an anchor even was: *"I like 'related to' a lot better for communications."*
- **One word was doing two unrelated jobs**, and neither out loud: which record an email thread belongs to, and which screen a help article shows up on. Salesforce calls the first one **Related To** on an activity, LEAP's own Log a Call already says *"Also relate to"*, and the platform defaults to Salesforce parity — so the communications sense is now Related To everywhere a person reads it: the composer's chip (**"Related to Enrollment · 3f2a…"**, which also now names the object properly instead of printing the table name), both New Email / New Text tooltips, the three thrown errors, the two database messages a failed send surfaces, and nine communications help articles.
- **The API names do not change** — `conversation_anchor_columns()`, `p_anchor_object`, the `haa_` columns. Same split as the object rename the same day: the label changes, the API name does not, so nothing that calls them is touched and no working path is put at risk to change a word on a screen.
- **Signing anchors are NOT this word and were deliberately left alone** — `enforce_signature_template_has_anchor` and the `\sig1\` tabs in a document template. An anchor there is the industry term for the marker a signature is placed on. The migration's CONTROL asserts HA-00049/50/51 still say it.
- **The help sense is still called an anchor** and is the one thing left open here: an article's anchors are what put it behind the ? button, which is not "related to" anything — "Shows On" is the candidate, awaiting Nicholas's yes.
- **A mistake worth recording, because it cost a build:** a `git pull` inside a compound command failed silently and the first cut of this was written against a `master` 130 commits stale, importing a function that did not exist there yet. **Check `git log -1` after switching branches; a silent pull failure looks exactly like a clean tree.**

**Shipped 2026-09-03 — The Communications help is findable, not merely written (branch `claude/enrollment-incentive-comms-rename-xnpmz5`; 1 migration live on prod, no DDL; HA-00208 / HA-00078 / HA-00154 indexed):**
Nicholas: *"Make sure you have the help index so users understand this and can search and learn how to use this."*
- **Writing the article is not indexing it.** LEAP's help panel surfaces articles by ANCHOR — `useCurrentPageAnchors` emits `{type:'object', object:<table>}` for whatever record is open — and **HA-00208 had ZERO anchors**, as did HA-00154 (the Outlook article). Both were reachable only by knowing to search for them. Full-text search worked the whole time (`ha_search` is a GENERATED column, so it is never stale) — what was missing was the ? button on the page the article is about.
- **HA-00078 carried the object list AS IT WAS WHEN IT WAS WRITTEN** — seven objects. Press ? on an enrollment, a unit, a service appointment, an account or a contact and the platform had nothing to say about the card sitting on the screen. **The anchors are now GENERATED from `conversation_anchor_columns()`**, the same registry the card, the feed and the add-in read, so an object that gains the card gains its help with no edit.
- **The index itself was split in two, and only on one surface.** `HelpCenterPage` grouped the table of contents by the RAW `ha_category`, so `communications` (1 article) and `Communications` (20) were two headings — while LEAP Pad's `KnowledgeScreen` normalised the casing and showed one. A reader who opened Communications on the desktop saw 20 of the 21 and no hint the 21st existed. **`src/lib/helpCategories.js` is now the one rule** both surfaces group by (LEAP Pad's private `normCat` deleted), and the stored categories were merged: case-collisions only (`admin`/`Admin`, `communications`/`Communications`), because *Admin* vs *Administration* and *List Views* vs *Lists and views* are editorial calls, not mechanical ones. The migration RAISES if any category is still spelled two ways.
- **Proved by asking the questions a user asks**: pressing ? on an enrollment now returns three Communications articles (it returned none); searching *"communications enrollment incentive"* ranks HA-00208 first, HA-00078 second, HA-00154 third; *"log a call"* and *"incentive"* both return results. The migration asserts each of those, plus that **every one of the twelve objects that can hold a thread has a published article anchored to it**.
- `scripts/help-categories-fixture.mjs` (17 checks) pins the heading rule with the raw-string grouping as a control that must still split. **Open:** a wider audit — how many of the 209 articles have no anchor at all — was not done here; this change fixed the three it owns.

**Shipped 2026-09-03 — An email files onto EVERY object a thread can live on (branch `claude/enrollment-incentive-comms-rename-xnpmz5`; 2 migrations live on prod, 1 with DDL, advisors 223 — the 3 above 220 are PR #783's approval RPCs, zero from this change; `log-email-to-record` v9 live; HA-00154 corrected):**
Nicholas, on the previous entry's "open" note: *"It needs to have all the objects. Why would you limit it to five?"* — and, fairly, *"I guess I'm confused. Why would you limit it?"*
- **Nothing limited it deliberately; it was left limited.** The Outlook add-in's Log Email path carried the object list THREE more times: six `<option>` tags in `taskpane.html`, six branches in `search_records_for_email_log`, and a five-object `ANCHOR_FK_PARAM` plus a six-object `TARGET_DELETED_COL` in `log-email-to-record` — all written when the add-in shipped (2026-07-27) and never widened. The 2026-09-03 Communications work fixed nine copies of that fact and then LOGGED this surface as an open item instead of fixing the twelfth. **"A separate surface" is not a reason; a list nothing checks is the defect.**
- **The set of objects an email can be FILED on is not a judgement call: it is exactly the set a thread can be ANCHORED to**, which `conversation_anchor_columns()` already derives from the conversations table's foreign keys. So the picker stopped being a list. `list_email_log_objects()` returns the twelve; the taskpane asks for them on sign-in and builds its dropdown from the answer; **and when that call fails it says so rather than falling back to a list of its own** — a stale fallback is how this went wrong the first time.
- **`search_records_for_email_log` is one derived query instead of six branches.** `email_log_target_columns(object)` resolves name / record number / soft-delete / updated-at / email columns from the catalog, anchored on `<prefix>_record_number` (the anchor `recordInsertDefaults` already uses). It reproduces the six hand-written branches on real data — contacts still read `CON-00071 · jhenderson@…`, work orders still fall back to the record number — and now answers for all twelve.
- **The security promise the old comment made is kept, and is worth restating: the table name must match a row of `conversation_anchor_columns()` before anything is built, every column identifier comes from the catalog through `%I`, and the caller's search text is BOUND, never interpolated.** SECURITY INVOKER, so RLS and the state scope still decide what the picker finds. Controls in the migration: searching `work_steps` and `users` are both refused BY NAME.
- **`log-email-to-record` (v9) carries no object map at all** — `email_log_target()` validates the object, confirms the row exists and is not deleted, and returns its name for the confirmation; the thread anchors through the generic `p_anchor_object` pair added the same day. Deployed and invoked live: it boots and returns the new validation message.
- **Only the object's OWN email column is searched.** `properties` carries nine email columns, eight of them HUD import text about somebody else; searching those would bury the record you are looking for.
- **`object_display_labels` (ODL-) exists because the add-in cannot read the bundle.** It is static files, so it cannot import `objectNav.js` — the DB has to tell it what an object is called. The label is DERIVED from the table name and the table holds only what the derivation gets wrong: today exactly one row, the object renamed to **Incentive**. **These are two copies of one fact** (client sync map + DB table), which is why `scripts/email-log-targets-fixture.mjs` (43 checks) pins them to each other against the values read off prod, and fails the build if the app and the add-in ever call one object two things.
- **Open:** `object_display_labels` has no admin screen (same gap as `record_state_scope_sources`), so a future rename is a data edit through SQL; and `list_relatable_records` is still an if/else per object rather than derived from the child's own foreign keys.

**Shipped 2026-09-02 — The Service Appointments tab is the service appointments LIST VIEW (branch `claude/service-appointment-list-view-a1yf76`, PR #751; 2 migrations live on prod, no DDL; HA-00207, HA-00015 + HA-00084 corrected):**
Nicholas, in the Field module: *"Isn't this supposed to be the service appointment list view? Why can't I ever see past service appointments? It just gives me a dropdown for future."*
- **The tab named after the object rendered a dispatcher's INBOX** — scheduled work from now through a 7/14/30/60-day window — and could not show a past appointment by construction. **All 104 service appointments on prod are in the past (102) or carry no scheduled time (2); ZERO are in the future**, so the tab was empty on all four dropdown settings and every appointment in the platform was unreachable from the tab named after them. `objectNav` points the object's list URL at that tab, so an appointment's own breadcrumb led to a screen that could not list it.
- **Field → Service Appointments is now `ObjectListSection` on the object.** The board moved to its own tab, **Appointment Inbox**, and its window now starts at the **START OF TODAY** — a job that began at 8am is still the dispatcher's problem at 9am. Two dead paths in it fixed: **clicking a row did nothing** (`onOpenRecord` required an `_id` the rows never carried), and its "only scheduled" read looked up `picklist_field 'status'` / `'scheduled'` — the pre-2026 legacy picklist, **every row of which is inactive** — so the status filter was silently dropped. The live pair is `sa_status` / `'Scheduled'`.
- **A saved list view could only name a FIXED date**, so "Today's Service Appointments" was not a view anybody could author — true for one morning, a lie every day after. **`src/lib/listFilterDates.js` points the list at the date kernel `reportFilters.js` already owns** rather than defining a second set of date rules; there is one definition of "before today" on this platform. **Never author a second date vocabulary.** The filter editor offers the relative periods and a chip reads "Today", not the raw token.
- **That also fixed two real off-by-ones nobody had isolated: the list compared a `timestamptz` cell to a date as TEXT.** `equals 2026-08-12` matched **nothing** on that day, and `on or before 2026-08-12` **excluded** it — because 13:00 sorts after midnight. A filter that silently matches nothing reads as "there are no such records".
- **Four purpose-built views seeded** (All / Past / Today's / Upcoming), and **the first view now supplies its COLUMNS as well as its filters and sort** — it used to open two-thirds of a view: the right rows in the right order under the wrong columns.
- **Seven saved views on five objects had never once applied their filters.** They stored `{field, operator, value}`; the evaluator reads `op`, finds nothing and falls through to "matches" — so **"To Be Verified" listed every work order** and "Assessments To Be Reviewed" every assessment. Two also named statuses that no longer exist ("Work Order To Be Verified" — the object's name is not part of the value; "Project In Progress", retired the same day → **Projects Underway**). Corrected in place; the migration **raises** if any live view still carries the unread key or names a status value that does not exist.
- **A list cell printed a RAW timestamp** — `2026-09-02T15:00:00.000Z`, in UTC, on a screen made for scanning. **`src/lib/dateDisplay.js` is now the one definition of how a date is written** (a bare `YYYY-MM-DD` is pinned to LOCAL midnight — parsing it naively prints the day BEFORE — and an instant is shown in the reader's zone), and `RecordDetail`'s two inline copies point at it.
- **An own column was labelled with the object's own prefix — "Sa Status"** — though a RELATED column has always been stripped ("City", not "Property City"). Same rule, same place now. The prefix is **derived from the table's `<prefix>_record_number` column**, never a hand-kept table→prefix map.
- **`npm run verify:service-appointment-list`** — a real Chromium over the REAL `ListView` and the four seeded views: past appointments on screen, Past/Today's/Upcoming each holding only their own, an unscheduled appointment claimed by neither, no cell printing an ISO instant, **with the old lexical comparison on the same page as a control that must come back wrong**. 20 checks. Not in `build:safe` (needs a browser binary). **Reading the code is not verification of this defect** — every piece of it read correctly. Build gates: `scripts/list-view-date-filter-fixture.mjs` (51) and `list-filter-matching-fixture` (48 → 58).
- **Open:** `service_appointments` has no page-layout-driven default column set, so the list derives its own until a view is picked; and a work order can still be **Scheduled with no appointment and no date**, which nothing objects to.

**Shipped 2026-09-01 — The status path is legible: nine stages, not nine slivers (branch `claude/incentive-app-chevrons-9ifdna`; 1 data migration live on prod, no DDL; HA-00205, HA-00053 corrected):**
Nicholas, on an incentive application: *"What the hell is this? I don't think the chevrons are working on the incentive application page layout."*
- **Nothing was wrong with the data, which is why this needed a browser.** All 24 incentive-application layouts carry a `status_path` widget on `ia_status`, `picklist_values_for_record_type` returns all nine stages for every record type, and no widget was orphaned off a deleted section. The strip WAS rendering — it was rendering unreadably. Measured in Chromium: the current stage took **470px** and the other eight got **89px each**, of which 40px is padding, so each showed ~49px of a 58-character label.
- **And it was clipped at BOTH ends, with no ellipsis.** `textOverflow: 'ellipsis'` was set on the chevron, but the chevron is `display: flex; justify-content: center` — **`text-overflow` does nothing on a flex container**, and a centered flex child that overflows is cut equally on both sides. The strip literally read `pplication To B | plication To | ... | cation Corre`. **Put the label in its own `<span>`**; `text-overflow` belongs on the box that holds the text, never on the flex parent.
- **Every stage is now sized to its own label and the strip WRAPS** (`flexBasis: auto` + `minWidth: max-content`, `flexWrap`). Nine legible stages on desktop and tablet, six wrapped rows on a 390px phone with nothing cut and nothing wider than the card. Stages deliberately **do not** grow into the spare width: a wrapped row holding two stages would stretch them to half the card each, which is what the first cut did (571px "Denied").
- **A chevron no longer repeats the object's name.** LEAP names statuses `[Object] [State]`, so nine chevrons were spending 40px each on "Incentive Application". **`src/lib/statusPathLabels.js`** drops the words every stage on the strip shares — derived from the stage set, never configured per object, so `work_order_status` ("New", "Scheduled", …) and every opportunity stage set come through untouched. Three rules keep it honest: whole words only (**"Pre-Approved" is never cut to "Approved"**), every stage keeps a word and the shortened stages stay distinct, and **the prefix may not end on a function word** — so "To Be Prepared"/"To Be Verified" is left alone; what is worth dropping is an object name, not grammar. Full label on the hover title and spelled out under the strip. `scripts/status-path-labels-fixture.mjs` (21 checks) pins it against the real prod stage sets.
- **One card, one status.** The path sat above a second card whose whole job was to repeat the current status and offer the transition buttons — which is what "what the hell is this" was pointing at. `StatusTransitionsBar` gained `embedded` + an explicit `statusField`, the path renders it as its own **Move to** row, and the standalone bar stands down for any field a path on that layout already covers (`suppressForFields`, resolved by name so a bar for a *different* status field is untouched). The explicit field also means an embedded bar is not subject to the bar's one-status-field-per-table limit.
- **`npm run verify:status-path`** — a real Chromium at 1440/900/390 that measures every chevron and requires the whole label to survive its box, **with the pre-fix geometry on the same page as a positive control that must come back truncated**. 16 checks. Not in `build:safe` (needs a browser binary): `npm install --no-save playwright-core` first. **Reading the CSS is not verification of this defect** — the code read correctly and the strip was garbage.
- **HA-00053 corrected in place, not appended to.** It promised a "phase 2" resolver — author a row in `object_lifecycle_config` to pick a primary status field on a table with two — that **was never implemented**; `StatusTransitionsBar` just suppresses. An admin following it would have authored a row nothing reads. The answer is a Status Path card per field, which now works because the card names its field outright. The migration RAISES if either correction failed to land.
- **Open:** `project_status` carries **36 values** mixing three objects' vocabularies ("Project …", "Work Order …", "Incentive …"), so a project's path is now four legible rows where it used to be one illegible one. That is the picklist, not the strip — and it sits alongside the already-logged gap that 17 of 18 project record types have no `project_status` assignments.

**Shipped 2026-08-31 — A home page saves with a Role assigned, and two dashboards were never the problem (branch `claude/dashboard-edit-error-8dr6cl`; no schema change):**
Nicholas, editing **Enrollment Home**: *"Error: invalid input syntax for type uuid: 'FA6C5203'"* — then, reasonably, *"There are two dashboard elements... if I want to add two dashboards, why won't it let me?"*
- **It was the Role dropdown, not the dashboards.** `FA6C5203` is `id.slice(0,8).toUpperCase()` of `fa6c5203-e449-4ef8-9a1c-49046a6f4994` — the **Admin** role. Reproduced on prod in a rolled-back transaction with an **empty component list**: same SQLSTATE 22P02, same message. Two dashboards save fine — `home_page_components` carries no unique constraint and `save_home_page` simply loops its inserts (proved: both dashboards written, `ok: true`, rolled back).
- **The trap, which is platform-wide.** Every LEAP service row carries TWO ids: `id` is the HUMAN display id (a record number, or the uuid's first 8 chars uppercased) and `_id` is the real uuid. A `<select>` bound to a uuid FK must take `_id`. `HomePageCanvasEditor` hand-rolled four of these mappings — dashboards, reports, list views, roles — and **one of the four read `r.id`**. Four copies of a rule is four chances for one to be wrong. The other five `fetchRoles()` consumers (Layouts, Technician wizard, Invite user) were already correct.
- **It had a quieter second half:** a page that already held a real `hp_role_id` matched no option value, so the control read *"All roles in module"* while the record was in fact role-scoped. The same one-line fix closes it.
- **One rule now, not four maps — `src/lib/builderSourceOptions.js`.** `toSourceOptions(rows, kind)` resolves every picker's option value through `optionRowId` (prefers `_id`; accepts `id` only when it IS a uuid) and **drops a row with no resolvable uuid rather than offering it** — an option that cannot be saved is worse than one not offered, because the failure lands at save time on a page of unrelated work. `assertUuidOrNull(value, field)` guards the adapter's save path so a recurrence reads *"Role is not a saved record id"* and names the field, instead of a Postgres cast error naming neither. **Never map a picker's option values by hand.**
- `scripts/builder-source-options-fixture.mjs` (**45 checks**) pins it, with **the old hand-rolled mapping as a positive control that must produce a non-uuid**, and pins that two dashboard components — even two bound to the SAME dashboard — both validate, so nobody invents a restriction that never existed. 55 suites + `build:safe` green.

**Shipped 2026-09-05 — the LEAP Assistant no longer loses a turn to a busy Anthropic API (branch `claude/leap-assistant-error-4lfij3`, PR #683, merged 2026-08-31; no schema change; `ai-assistant` **v39 LIVE on prod**, deployed 2026-09-05 via Actions → Deploy Edge Function, both files fetched back and byte-identical to `master`; self-test `ok:true`, served on `claude-opus-5`, 11,080 tokens written to cache then all 11,080 read back):**
Nicholas, on a building: *"Create 25 dwelling units at this building. Just number them 1 through 25."* → **"Error: Assistant call failed (529)."**
- **529 is Anthropic's `overloaded_error`** — the API momentarily at capacity — passed through verbatim by `index.ts`. It is never about the request, the user's data, or their permissions, and the message reading like a LEAP defect was itself part of the problem.
- **Nothing retried it.** `isModelUnavailable` covers only 404/400 "model not found", so a 529 fell straight through to the throw and ended the request — discarding every action already proposed in it. **A batch is the shape most exposed to this**: one turn is up to `MAX_TURNS` (8) sequential calls, each with adaptive thinking and a 16k output cap, so "create 25 units" gets eight chances to be told "busy" and needs only one to fail.
- **`isTransientUpstream()`** — 429, 5xx (not 501, which is a real permanent answer), and any body naming `overloaded_error`. The permanent failures (400/401/403/404/413) are deliberately excluded: retrying a rejected request repeats the rejection and wastes the user's time. **The two error families must stay disjoint** — a transient failure must never be mistaken for "this model does not exist" and silently change the model.
- **`upstreamRetryDelayMs()`** — a bounded 1s/3s/7s ladder, honouring `retry-after` up to a 20s cap, and **refusing any wait that would outlast the turn**: a wait past `HARD_DEADLINE_MS` turns a retryable blip into a request that dies with no reply at all, which is worse than the bug.
- `callModel()` retries the same model, then falls to the next model in the chain rather than lose the turn. **The fallback reason is recorded only once a fallback has actually answered** — claiming it beforehand would put a fiction in `fau_outcome_message` when the fallback also fails. Every retry is logged, so a run of overloads is visible rather than merely felt as slowness.
- Fixture `assistant-transcript` **61 → 97 checks**, pinning both directions, the ladder's bounds, the `retry-after` cap and the clock guard. `npm run build:safe` green (53 suites). Prod's deployed v38 was fetched and **byte-verified identical** to the pre-change repo first, so there was no drift to clobber.
- **DEPLOYED 2026-09-05, five days after it merged — and the delay is the lesson, not the fix.** Merging to `master` deploys the frontend (Netlify) and nothing else; **an edge function needs an explicit deploy that nobody ran.** Two sessions then inherited the "declined, too risky" note below and read it as a prohibition rather than a judgement, so it sat inert while the assistant kept erroring on every 529. **The route now exists and is proven** — `.github/workflows/deploy-edge-function.yml` with the `SUPABASE_ACCESS_TOKEN` secret; the run took 13 seconds. The original constraint was real and still is: the MCP tool takes source **inline**, 86 KB cannot be reproduced through a model without corrupting it, and Supabase cannot roll an edge function back. **Never retype a large function; run the workflow.**
- The self-test probe deliberately still reports a 529 as "no model answered": that is truthful for a diagnostic, and retrying there would mask the very condition it exists to report.

**Shipped 2026-08-31 — The chart colours are readable, and provably so (branch `claude/report-totals-display-6k2ye1`, PR #688; no schema change):**
Nicholas, asked directly whether to re-step the palette: *"Yes — re-step all seven."*
- **The old set failed every check that can be computed.** `#3ecf8e,#7eb3e8,#1e466b,#a78bfa,#2aab72,#5eead4,#8fa0b8`: `#1e466b` sat at OKLCH L 0.384 and `#5eead4` at 0.855 (one nearly black, one nearly white, so the seven never read as one family); `#7eb3e8`, `#1e466b` and `#8fa0b8` were **below the 0.10 chroma floor — they read as gray** and did no identity work at all; and **six of the seven fell under 3:1 against the white card**, which is what a slice vanishing into the surface actually is.
- **Now `#009c65,#623e96,#1398e2,#813075,#7a89e7,#008d9b,#b776d4`.** All seven clear 3:1 on **both** card surfaces (`#ffffff` and `#f7f9fc` — a colour that passes on only one is readable on half the platform), sit in the lightness band and above the chroma floor, and every adjacent pair AND **every pie ring from 2 to 7 slices** separates by at least ΔE 9.2 under simulated protanopia and deuteranopia. No red, no orange, clear of the warning amber.
- **Three findings that forced the shape, each from measurement, not taste.** (1) **Seven identities cannot ride on hue alone** — deuteranopia collapses the red-green axis, and at one constant lightness teal and magenta land **0.9 ΔE** apart, i.e. the same colour. The band is a RANGE for this reason, so the set alternates a LIGHT (~L 0.65) and a DEEP (~L 0.46) tier and lets lightness carry part of the identity. (2) **A pie is a RING** — slice n touches slice 1 — so slot 1 borders every other slot at some series count. The first candidate passed the linear adjacent check and put emerald at slot 1 and moss at slot 7, **ΔE 3.2, touching**. Hence exactly one green, and it must be slot 1. (3) **Emerald had to go deeper than the `#3ecf8e` UI accent**, which cannot clear 3:1 on white at its own lightness — the accent and the series colour are two different jobs.
- **Four search bugs worth recording, because each looked like a colour-space limit and was not.** Comparing the validator's `"pass"` against `true` rejected every candidate; a 22° minimum hue spacing **exhausted a 134°-wide pool at five slots**; a `-0.001` gamut tolerance threw away every deep teal and cyan (their red channel lands at −0.02 and clips harmlessly), making three hue sequences look impossible; and ranking the beam on chroma alone pruned it to 500 chroma-perfect partials that all dead-ended. **Clamp and let the validator judge the resulting colour, never the requested one.**
- **Optimising for a metric extreme produces garbage twice over:** max chroma gives neon (`#d900fe`, `#0005fd`), min chroma gives dusty (everything at exactly the 0.10 floor), and a free hue search finds five near-identical violets that pass every gate. The hue sequence is therefore chosen deliberately and only the STEP is solved.
- **Guard: `scripts/chart-palette-fixture.mjs` (66 checks)** re-computes all of it on every build — OKLCH band, chroma floor, WCAG contrast on both surfaces, Machado-2009 CVD ΔE on the chain and on every ring — with **the old palette as a positive control that must fail**, and the colour maths itself checked (black on white is 21:1) so a broken checker cannot pass a broken palette. Maths in `scripts/lib/paletteChecks.mjs`, test-only. 54 suites + `build:safe` green; rendered in a real browser and looked at.
- **Open:** `CHART_COLORS[i % length]` **cycles** in six modules, so an 8th series is painted the same colour as the first. The palette is meant to be assigned in fixed order and never cycled; the pie already folds its tail into "Other", the rest should fold or go neutral. In `docs/leap-reports-enterprise-todo.md`.

**Shipped 2026-08-31 — A dashboard filter uses the right field on every object, and says which widgets it misses (branch `claude/report-totals-display-6k2ye1`, PR #686; 6 migrations live on prod, advisors 219 / same known categories, zero naming the new artifacts; HA-00203):**
Nicholas: *"we definitely need a dashboard-level filter for states and things, just like Salesforce,"* then *"all dashboards need the filter. I'm looking at the enrollment dashboard. Do you see a filter there? I don't see it. The user needs to be able to put any kind of filter they want on, not just the state filter."*
- **The filter carried ONE column name and handed it to every widget.** A widget whose report sits on another object does not have that column, and the runner's rule for a filter it cannot apply is `continue`. So on **DSH-00010** — four widgets on `properties`, one (**Pipeline by Stage**) on `opportunities` — setting State to NC narrowed four and left the funnel showing **every** state. The two sat side by side looking alike. **Numbers quietly answering different questions is the defect**, not the skip itself.
- **`dashboard_filters.dfilt_field_map`** — `{object → column}`, with the filter's own field as the fallback. NULL on everything saved before, which reads as "the same column everywhere" — exactly what it did, so no existing filter changed. The engine resolves it where the widget report's primary object is known, for the filters it APPLIES and for the report filters those OVERRIDE (so "All" still clears a report's own saved filter, on the right column).
- **A widget that cannot be filtered now says so on its own face** — *"Not filtered by State"*. The filter is still dropped (the only safe thing at run time); what changed is that it is no longer invisible.
- **The field was a free-text box for a raw column name**, with a datalist of whatever columns some widget config happened to mention — so authoring a filter meant knowing the platform's column names by heart, and a typo produced a control that silently filtered nothing. Now a **field picker grouped by the objects the dashboard's widgets report on** (`describeDashboardFilterObjects`), proposing the equivalent everywhere else and stating coverage. **"— Not filtered —" is stored explicitly, not deleted**: an object that happens to spell the column the same way must not be filtered after someone excluded it.
- **`dashboard_filter_distinct_values` resolves picklist and lookup columns through their target**, so Status and Record Type list names instead of uuids — which is *why* the platform's only filter was on a text column; nothing else would have been usable. `anon` EXECUTE revoked in the same migration (DROP/CREATE → re-issue grants + `NOTIFY pgrst`).
- **Every dashboard carries a filter now.** Enrollment and Qualification take **Status** (single-object, so it reaches all their widgets); Program Operations takes **State**. **Enrollments have no state column at all** (`enrollment_payment_state` is a payment status), so a State filter there would have reached nothing — each dashboard gets the filter its own widgets can answer, not one blanket field.
- **`settingsPanel` may now be a render function** receiving the canvas's LIVE components, so a filter added right after a widget is authored against that widget too.
- **Proved in a real browser** (`npm run verify:dashboard-filters`, 15 checks + screenshot), and that is what caught the defect no unit test would have: **the pure module took column names as strings while the service hands over field descriptors** (`{name,label,type}`), so every lookup missed and a filter reported itself as reaching nothing while the picker showed the field right there. Fixed at the root (`columnNameSet`); **both shapes are pinned in the fixture** (51 checks). 53 suites + `build:safe` green.
- **Open:** filters are per-dashboard, not per-viewer (no personal default, no "view as" role), and an on-canvas filter widget still names one column and cannot be mapped across objects. Both in `docs/leap-reports-enterprise-todo.md`.

**Shipped 2026-08-31 — A record write can never again hang on an EXECUTE grant the advisors tell the next session to revoke (branch `claude/building-creation-error-y1ewa3`; 1 migration live on prod, advisors 222 -> 219, three lints RESOLVED, zero new):**
Nicholas, creating a Building on PROP-07282: *"Create failed — permission denied for function recompute_property_rollups."* **The identical defect had already been fixed once, on 2026-07-27, and came back.**
- **Why it recurs, which is the only interesting part.** `recompute_property_rollups` and its three siblings are SECURITY DEFINER but were PERFORM'd from five SECURITY **INVOKER** trigger functions (`trg_buildings_rollup`, `trg_units_rollup`, `trg_properties_rollup`, `trg_projects_rollup`, `trg_opportunities_rollup`) running as the signed-in `authenticated` role — so every INSERT/UPDATE/DELETE on buildings, units, properties, projects and opportunities hung on an EXECUTE grant to `authenticated`. That grant is **exactly what `authenticated_security_definer_function_executable` tells every migration to take away.** Two rules in direct conflict, and the tie broke a different way each time: `20260727025442` revoked it (everything broke) → `20260727233200` granted it back (worked, 3 lints) → **`20260829234151` revoked it again** (this report). Only `recompute_property_rollups` lost the grant that time, which is why *only* the building/property path was failing while units and opportunities were fine.
- **Re-granting is the band-aid that has now failed twice.** The fix is the pattern LEAP already settled on for geographic record access (policies call the definer entry point so EXECUTE stays revoked on all 110 resolvers): **the five trigger functions are now SECURITY DEFINER**, so the nested rollup call runs as owner and *nothing* needs the grant. Bodies byte-identical apart from the security clause, all five already carried a fixed `search_path`, and their entire body is a PERFORM of a function that was already running as owner — effective privilege is unchanged; only the EXECUTE check disappears. EXECUTE on all four `recompute_*` functions is now revoked from `authenticated` **for good**.
- **Safe to do because the sweep was exhaustive, not assumed:** those five trigger functions are the ONLY SECURITY INVOKER callers of the rollup functions in the whole database — no policy, no view, no client `.rpc()`, no edge function; `execute_flow`'s rollup branch is itself SECURITY DEFINER.
- **The trap inside the fix, caught by re-running the advisors instead of trusting the prediction: a plain `CREATE FUNCTION` leaves the default PUBLIC EXECUTE grant**, so promoting the five to SECURITY DEFINER made each a *callable* definer function and advisors went 222 → **229**, not the 219 predicted. **PostgreSQL does not check EXECUTE when it FIRES a trigger** — only at CREATE TRIGGER — so revoking it costs nothing; verified live after the revoke that every path still works. **Promoting any trigger function to SECURITY DEFINER must revoke EXECUTE in the same migration.**
- **The guard, so a third recurrence is loud:** `find_trigger_function_privilege_gaps()` lists every SECURITY INVOKER trigger function that calls a SECURITY DEFINER function `authenticated` cannot execute — i.e. every table whose writes fail at runtime with "permission denied for function …". It must return zero rows, and the migration raises if it doesn't. Nothing else catches this class: the build, the fixtures and the advisors are all silent, and the advisors actively *reward* the revoke that causes it. **Fix a row by making the TRIGGER function definer — never by granting EXECUTE to `authenticated`.**
- **Proved by writing, not by reading**, impersonating Nicholas under RLS in rolled-back transactions: the exact failing INSERT reproduced `42501 permission denied for function recompute_property_rollups` beforehand; afterwards it returned **BLD-00174** with the name deriver and `property_total_buildings = 1` both correct, and units / properties / opportunities / **11 of 12** projects updated cleanly with **zero** permission denials. (The 12th failed on the pre-existing `record_type_eligibility` rule under **MF-Exhaust Fan Replacement** already logged as open below — unrelated, and unchanged by this.) **Nothing was written** — that property still has 0 buildings and the highest building is still BLD-00167; the two probe inserts only burned record-number sequence values, which do not roll back.

**Shipped 2026-09-02 — A datetime field can be edited, and editing it does not move it (branch `claude/work-orders-photos-bug-dlxf43`; 1 migration live on prod, no DDL):**
Nicholas, on a service appointment: *"why can't I edit the schedule start time?"*
- **He couldn't, and neither could anyone, anywhere.** The record page's edit gate excluded `datetime` outright (`&& (f.type !== 'datetime')`) and `EditField`'s `datetime` case returned the literal string **"Read-only"**. `date` had an editor; **`datetime` never had one**. **570 datetime fields are placed across ~100 objects** — Scheduled Start/End, timesheet clock-in/out, a work order's start and end were all displayed and unenterable.
- **It hid for so long because 450 of the 570 are audit columns**, which are correctly read-only, so the gap only shows on the handful of fields somebody actually needs to type. Marking Scheduled Start/End **required** the day before is what made it bite: the form demanded a value it gave no way to enter.
- **The dangerous half is the conversion, not the input.** A `datetime-local` value is **wall-clock with no zone**; the column is `timestamptz`, an absolute instant. Reverse the direction and every appointment shifts by the UTC offset — five hours in Wisconsin — with nothing on screen to show it. **`src/lib/datetimeField.js`** owns both directions; the browser's zone is used deliberately because that is already the zone the record page DISPLAYS these in (`toLocaleString`, no `timeZone` option), and editing in one zone while displaying in another is worse than either.
- **`scripts/datetime-field-fixture.mjs` (36 checks)** forces `TZ=America/Chicago` and pins the real SA-00299 instant (13:00Z → 8:00 AM Appleton), both DST edges — the **non-existent** 02:30 on spring-forward and the **repeated** 01:30 on fall-back — and **the naive `toISOString().slice(0,16)` as a CONTROL that must show the wrong hour**. It also pins that a value carrying SECONDS does not round trip and *says so*, rather than silently truncating on save.
- **`npm run verify:datetime-field`** — a real Chromium with `timezoneId: America/Chicago`, because the input has one unforgiving rule: **a value it cannot parse is discarded and the control renders blank**, which is the exact symptom being fixed and something no Node test can see. 6 checks. Not in `build:safe` (needs a browser binary).
- **What stays read-only is DECLARED, not guessed.** Removing a blanket type exclusion would have made 120 unmarked datetime fields editable, including the e-signature trail. The migration marks the two groups a person must never rewrite — **soft-delete timestamps** (recycle-bin metadata) and the **signing / notification / delivery trail** (`env_sent_at`, `recipient_signed_at`, `tab_filled_at`, `sent_at`, `read_at`, `last_login`, `taken_at`, `signed_at`, `verified_at`, …), which are **evidence an auditor reads** and are written by the signing pipeline and the Graph mail path, never by hand. It uses `system_audit`, the mechanism the page already reads, so each stays individually unlockable in the layout editor.
- **Deliberately left editable, because a person legitimately sets them:** `activities.performed_at` (when the call happened), `sa_actual_start_time`/`_end` (corrected after the fact from the field), `tse_clock_in_time`/`_out` (a timesheet correction is a real workflow), `work_order_start/end_datetime`, `occurrence_date_and_time`, `mr_need_by_datetime`. The migration **asserts both directions** — nothing platform-stamped left open, and none of these swept up.
- Also fixed on the way: a required datetime column the layout does not carry was **downgraded to a free-text box** in the create modal (`m.editorType === 'datetime' ? 'text'`), and the quick-create modal had no datetime branch at all. Both use the same picker and the same conversion now.

**Shipped 2026-09-02 — A child record inherits every relationship the platform already knows (branch `claude/work-orders-photos-bug-dlxf43`; 1 migration live on prod, 1 DDL default):**
Nicholas, creating a Service Appointment from WO-00244: *"it should obviously inherit everything possible from the work order, building opportunity, etc. Populate all the fields you can."*
- **SA-00305 came out carrying only the work order** — Work Type, Project, Opportunity, Status and both times blank — while the work order it was created from held work type, project, opportunity, property, building and account.
- **The fourth hand-maintained list of the day.** `resolveInheritedParents` is data-driven from `TABLE_META`, which covers **74** of the platform's 100+ record objects. `service_appointments` is not one, so the resolver returned `{}` before doing any work. Same shape as the visibility route, `applyInsertDefaults`' prefix map and `TRIGGER_DERIVED_REQUIRED`.
- **And the curated entries are PARTIAL even where they exist.** `TABLE_META.work_orders` declares the four relationships it *navigates* by (project, opportunity, property, building) and **not `work_type_id`** — so even a covered child could never be handed the work type it was sitting on. Fixing only the missing entry would have left that half broken.
- **Relationships are read off the table's real foreign keys** (`parentsFromColumns`), and the curated entry is **UNIONED** with them, never replaced — a curated list can only gain relationships, so nothing that navigates correctly today changes. **Three kinds of FK are deliberately not parents:** `users` (an owner or assigned technician is a person on the record, not a record above it), `picklist_values` (a status is a value, resolved per record type), and a **self-reference** (`parent_work_order_id` is hierarchy WITHIN the object — inheriting it would file a record under whichever sibling the create started from).
- **Nothing outside the child's own columns can be written.** Values from the ancestor are only NOTED; the fill step assigns exclusively to relationships the TARGET declares, which is why reading more of the ancestor is safe. A relationship the parent does not hold stays empty rather than being guessed.
- **Status is a column DEFAULT, not a form question** — `sa_status` now defaults to **Scheduled**, the pattern `envelopes.env_status` already uses for Draft. It applies only when the column is not supplied, so `create_service_appointment`, the scheduler and the notification lifecycle are untouched. The id is looked up, never hardcoded: a literal uuid is wrong on any database whose picklists were seeded separately.
- **Proved on prod in a rolled-back transaction**: the create payload now returns SA-00306 with the trigger-derived name, work type, project, opportunity and status Scheduled — against SA-00305, which the UI produced with the work order alone. Nothing was written. `scripts/parent-relationships-fixture.mjs` (26 checks) pins the rules against real production FK metadata and **replays the exact reported case**, requiring the three blank fields to fill and the two the work order does not hold to stay empty.
- **A verification trap worth recording:** the migration's first assertion extracted the default's uuid by stripping non-hex characters, which swallows the **`d` from `::uuid`** and raised `invalid input syntax for type uuid`. Match the uuid by SHAPE, never by stripping.

**Shipped 2026-09-02 — The database says which columns it fills, so a create form stops asking for them (branch `claude/work-orders-photos-bug-dlxf43`; 2 migrations live on prod, 1 with DDL):**
Nicholas, on the New Service Appointment pop-up: *"auto create name of SA record"*.
- **The name ALREADY auto-created.** `trg_sa_name` composes it on insert — an appointment saved as "NEW" comes back "3002 West Darling Street - Appleton". What was wrong is that the pop-up **asked** for it, required, so a person typed a value the database threw away one statement later.
- **The third hand-maintained list to fail in one day.** `TRIGGER_DERIVED_REQUIRED` listed 10 tables; `service_appointments` was not one. Same class as the work-order visibility route and `applyInsertDefaults`' table→prefix map, both fixed hours earlier.
- **Only the database can answer "which columns does a trigger fill", so it is asked** — new `trigger_written_columns(object)` matches `NEW.<col> :=` in the trigger functions' own source (PostgreSQL records which trigger FIRES, never which columns it WRITES) and intersects with the table's real columns so a local variable cannot leak out. It **reproduces every entry of the hand-written map exactly** — the migration RAISES if it does not — **and finds `sa_name`**. Platform-wide: **769 trigger-written columns across 191 tables, 333 of them NOT NULL.**
- **A wrong negative control, caught by running it.** The migration first asserted that `properties.property_name` was NOT derived. It failed — and the detector was right: `normalize_property_address` genuinely composes it from street + city, which is why every property in LEAP reads "3002 West Darling Street - Appleton". The control was replaced with `work_orders.work_type_id` / `assigned_technician_id`, which no trigger touches. **Check the counterexample before assuming the detector is wrong.**
- **Scope is deliberately narrow: this answers what must not be DEMANDED, never what is READ-ONLY.** Conflating them would break a feature — `ia_property_owner_name` is trigger-filled AND deliberately overridable (HA-00192), so a blanket "trigger-written means read-only" removes an override built on purpose. Editability stays hand-curated in `DERIVED_READONLY`, where the intent is recorded per column.
- **The sync accessor can only under-claim.** `columnsFilledByTrigger` answers from a session cache and returns empty when cold, so the failure direction is "a field is asked for", never "a field is wrongly skipped". Every create surface awaits `prefetchTriggerWrittenColumns` when it opens.
- **And then the pop-up had nothing left to ask**, which would have produced an appointment scheduling nobody for no time — the original complaint. `sa_scheduled_start_time` and `sa_scheduled_end_time` are now required on the layout, so the create modal asks for the two facts that make an appointment real. Safe on the edit side too: **all 100 live service appointments already carry both**. Status is deliberately left optional — it has a lifecycle default, and demanding it makes a person answer what the platform can.
- 36-check fixture pins that the enumeration is gone and that read-only-ness stays curated; 65 suites + `build:safe` green.

**Shipped 2026-09-02 — A service appointment is optional, and a new record carries an owner whatever object it is (branch `claude/work-orders-photos-bug-dlxf43`; 1 migration live on prod, no DDL):**
Nicholas: *"he can not see the insulation work order"* … *"we don't need an appointment"* … *"service appointments are optional."* Then, creating one by hand: *"Create failed — null value in column `sa_owner` of relation `service_appointments` violates not-null constraint."* Two defects, one screen.
- **The Today list could only be reached through an appointment.** `my_service_appointments` is written `FROM service_appointments`, with no assignee- or owner-based route in, so a work order with no appointment row was invisible to EVERYONE — however it was assigned, owned or dated. **WO-00244 already carried everything needed**: `assigned_technician_id` = Roman Rufino and `work_order_scheduled_start_date` = that day, and it said **Scheduled** on the record page. The data was right; the query was the whole defect. **28 of 128 live work orders have no appointment**, so this was not one record.
- **A stop is now on my day when EITHER an appointment on that date has me as crew (unchanged, byte for byte — nobody's schedule moved), OR the WORK ORDER itself names me and is scheduled for that date and has no appointment to carry it.** Dedupe is by construction, not DISTINCT: branch (b) takes only work orders with NO live appointment, so if somebody made one, the appointment IS the schedule and it wins — including the time window a bare work order does not have. A direct-assigned stop has no times, sorts last (`NULLS LAST`, already the behaviour) and the client already renders a missing time as "—", so no client change was needed.
- **Verified by impersonating Roman under RLS and calling the real RPC**: WO-00244 now returns, marked *Assigned*, alongside his two appointment-backed stops. Blast radius checked before shipping — the only appointment-less assigned work reaching anyone is Roman's 1 and Logan Wood's 11 (all dated 2026-08-12).
- **Second defect, far bigger than the object it surfaced on: `applyInsertDefaults` was a hand-written list.** ~34 table→prefix entries plus a tail of `else if` branches, while **86 tables carry a NOT NULL owner column** — so roughly **54 objects could not be created from the UI at all**, each failing exactly like the Service Appointment did, and every object added to LEAP after the list was written joined them silently. Same shape as the URL/display allowlists (2026-08-24) and the four hand-rolled picker option maps (2026-08-31). **A list nothing checks is the defect, not the thing the list forgot.**
- **Nothing is enumerated now — `src/lib/recordInsertDefaults.js` reads the prefix off the TABLE'S OWN COLUMNS**, anchored on `<prefix>_record_number`. That anchor is chosen, not assumed: **all 162 record-carrying tables have exactly one** such column, and a record number is never a business field. **`_owner` is not safe to search for** — four tables carry more than one, so a first-match search is decided by declaration order, and on `property_hud_match_review` it returns `phmr_source_owner` (ordinal 7, a HUD owner NAME) ahead of the record's own `phmr_owner` (14). **The table NAME is not safe either** — `saved_list_views` prefixes its columns `list_view_`. Bare `owner_id`/`created_by` are accepted as a fallback for the pre-convention tables (email_templates, document_templates, the envelope family).
- **`applyInsertDefaults` is now async** (reading the columns is a round trip, memoised per session via `describe_object_columns`; a failed lookup is deliberately NOT cached, so one transient error cannot poison every later create). All five call sites await it — the fixture fails the build if one does not.
- **Proved on prod in a rolled-back transaction, impersonating a real user under RLS: the OLD payload still raises `not_null_violation`** (the defect reproduces) **and the new one inserts**, returning SA-00304 with the trigger-derived name and the owner stamped. Nothing was written. `scripts/record-insert-defaults-fixture.mjs` (29 checks) pins it against real production column shapes, **with the two naive rules as controls that must pick the wrong column**. 65 suites + `build:safe` green.
- **Open:** the New Service Appointment pop-up asks only for Name — the create modal shows required fields only, and start/end time and status are not required, so an appointment made by hand carries no schedule. It saves now; it is still not a useful way to schedule. And a work order can be set to **Scheduled with no appointment and no date**, which nothing objects to — the status and the schedule are two different facts and only one of them reaches the field.

**Shipped 2026-09-02 — A work step accepts a photo again: two lines in the wrong order, eleven days, and steps closed as "Photo does not upload" (branch `claude/work-orders-photos-bug-dlxf43`; 2 migrations live on prod, advisors 220 / same known categories, zero naming the new artifacts; HA-00206, HA-00197 + HA-00124 corrected):**
Technicians could not upload a photo to a work step, on **both** surfaces — LEAP Pad (WO-00243, Building Access, Appleton) and the desktop **Work Plan tab** of the work order record page (WO-00245, stuck at Photos: 0/3). *"It just gets stuck after I upload a photo, and nothing happens."* No error. Steps were being closed **Not Applicable — "Photo does not upload"** to get past a Complete button that would never enable, which is how evidence gets lost.
- **The cause is two lines, and reading them tells you nothing.** `const files = e.target.files` keeps a reference to a **LIVE FileList**; the next line, `e.target.value = ''` (which every picker does, so the same file can be chosen twice), **empties that same object**. The loop then ran zero times: no upload, no error, no spinner. Both orderings look correct — only a browser knows that clearing the input reaches back into the list you are holding.
- **Introduced by `a70eee3` (#520, 2026-08-22)**, which converted the inline step card from one file (`e.target.files[0]` — captures the File itself, survives the clear) to many. **The guided screen flow, written in the same commit, called `imageFilesFrom` FIRST and never broke** — which is why two surfaces of one feature behaved differently while both read as correct, and why it survived eleven days.
- **The data said so before the code did, and it is the check worth repeating:** photos on **screen-flow** steps — 680, still landing on 2026-09-02 at 14:26; photos on **inline-card** steps — last one **2026-08-27**. It looked like a permissions problem (only Admins had uploaded anything since 07-29) and was not: Admins happened to be doing assessments, which are screen flows. **Roman Rufino, a Project Site Lead, has 332 successful photos.** The previous session had already ruled out RLS, the bucket policy, the evidence gate and schema drift — all correctly.
- **One function owns the order now — `imageFilesFromInputEvent(event)` in `src/lib/photoDrop.js`** (snapshot, THEN clear), plus `fileFromInputEvent` for the single-file video pickers. Every picker on both screens goes through them, and the fixture **fails the build if any handler reads `.target.files` by hand again**. Returns `{files, rejected}` like `imageFilesFromDrop`, so a skipped non-image is reported the same way whichever route it came by.
- **A capture may no longer fail silently, which was the other half.** `if (list.length === 0) return` is gone: an empty pick says *"No photo was selected"*, a rejected file says *"1 skipped (not an image)"*. **And nothing in the chain could time out** — `createImageBitmap`/`canvas.toBlob` on a memory-pressed phone, libheif's `frame.display()` callback that on a failed decode is **never invoked at all** (no try/catch can catch that), a PUT whose connection goes away mid-body. New **`src/lib/uploadDeadline.js`**: optional work **degrades** (a stalled shrink or HEIC decode uploads the untouched original — failing an upload because an optimisation hung is strictly worse than not optimising), required work **throws by name**, and a photo that cannot be filed cleans up its storage object on a timeout exactly as it does on an error.
- **Two more remounts in the same family, found by the browser check, not by reading.** `Shell` was declared INSIDE the render, so it was a new component type on every render — React tore down and rebuilt every step card, **and every hidden file input**, whenever anything on screen changed; a picker open across a re-render fires `change` at an element React is no longer listening to. Hoisted to module level as `WorkOrderShell`. And `onPhotoUploaded` called a **non-silent** `load()`, so every single photo replaced the whole step list with "Loading…", losing in-flight batch progress and half-typed field text mid-job. Now `load({ silent: true })`.
- **`npm run verify:photo-upload`** — a real Chromium mounting the **REAL `WorkOrderDetail`** on WO-00243 with only the network stubbed, driving its real hidden inputs with **real files on disk**, with the **pre-fix handler on the same page as a positive control that must see ZERO files**. 16 checks. **Reading the code is not verification of this defect** — it is what let it ship. Not in `build:safe` (needs a browser binary): `npm install --no-save playwright-core` first. Build gates: `scripts/photo-input-event-fixture.mjs` (20 checks, models the live FileList and pins that the browser really behaves that way via the tool) and `scripts/upload-deadline-fixture.mjs` (22).
- **Video capture is now on Video steps only (Nicholas, 2026-09-02)** — this **REVERSES** the 2026-08-27 rule ("the user can upload videos anywhere. You can't restrict this"), which put a Video button beside Photo on every step and made a photo step's controls read as a choice between two equal things. Same person, later call; `scripts/video-evidence-fixture.mjs` was rewritten to pin the new shape so nobody re-derives the old one from the old comment. **Where a video may be STORED is unchanged** — the Photos/Files card on the work order takes one from any step, and a Video step still accepts footage outside the plan's ordering gate.
- **Lockbox is now in the "Who provided the keys?" list too**, as *"Lockbox — nobody handed them over"*. Both routes record the same single value and picking it snaps the chips back to Lockbox, so the card never shows Person selected over a lockbox answer.
- **Separate prod defect, fixed in the same push and unrelated: `record_in_state_scope()` was throwing for EVERY object.** One branch named `record_state_scope_incentives`, whose table and resolver are both gone. **A `LANGUAGE sql` function is parsed WHOLE on first call**, so every name in every branch must resolve before ANY branch runs — a 110-way CASE is 110 chances to take the entire engine down. Masked because the policies short-circuit when `app_user_state_scope()` is NULL, which it is for everyone except **James (USR-00018, NC)**, who has no auth link and has never signed in. It was a latent outage, not an inconvenience. Fixed by retiring the registry rows for objects that no longer exist and rebuilding the dispatcher — and **the rebuild now CALLS what it wrote** (with a non-null state list, since NULL returns true before the branches are ever considered and would pass on exactly the broken dispatcher). New **`record_state_scope_integrity()`** reports the two ways this can dangle; the migration raises unless it returns zero rows. Verified live: NC property visible, WI property hidden, photos/work orders/work steps all resolve.
- **Open:** the steps closed as *"Photo does not upload"* still carry no evidence — HA-00206 tells technicians to reopen the ones that can still be photographed and to leave the ones whose moment has passed honestly marked, rather than substituting another photo. WS-02133, WS-02135 (WO-00243) and WS-02160 (WO-00245) are the known ones.
**Shipped 2026-09-02 — The project lifecycle is seven project statuses, not thirty-six (branch `claude/project-record-types-mapping-xjnlfc`; 1 migration live on prod, no DDL):**
Nicholas, in Object Manager on `project_status`: *"Something is really wrong here on these statuses. These look like the work order statuses... That's really the only options. All this other stuff for incentives and work orders doesn't apply to project records."* Then: *"you could also add a project pre-construction meeting to be scheduled."*
- **This was the ROOT of the 36-chevron path, not a scoping problem.** The 36 values were three objects' vocabularies in one list — 11 "Work Order …" stages, 10 "Incentive …", 9 "Utility …"/"Test Out …", "PM Sign Off" — and only **four** that describe a project. Per-record-type scoping could never have fixed it: the values themselves belonged to other objects.
- **Seven statuses now:** Project Planning → Project Pre-Construction Meeting To Be Scheduled → Project To Be Scheduled → Project Scheduled → Project Underway → Project To Be Verified → Project Completed. Three were created (Planning, Pre-Construction Meeting, To Be Verified); four already existed. **The pre-construction meeting is placed after Planning and before To Be Scheduled** — it is held before the work is scheduled; a one-row reorder if that is wrong.
- **32 values retired (inactive), never deleted** — `audit_log` and `field_history` name them, and a retired value must still resolve to its label when history is read back. Verified first that no database function, edge function or client module references any of the 32 by name; the three RPCs that name a project status all name "Project To Be Scheduled", which is kept. **Walk-Away** went with them because it was not on the list — say so if it should come back.
- **Four live projects sat on "Work Order Created"** (PROJ-00025/32/33/38) and were moved to **Project Underway** — the nearest surviving stage that does not overstate progress, since the old chain put it after Underway. 24 projects remain on To Be Scheduled, 13 on Underway.
- **The transition graph was rebuilt** — the old one walked through the work order and incentive stages, so almost none of it survived. 25 transitions retired, 7 authored.
- **All 18 project record types now carry the seven** (`record_types_missing_status_configuration()` returns zero for projects), including `WI-IRA-MF-HOMES - AUDIT LEVEL 2`: all five of its previously-selected values were being retired, so leaving it alone would have left it with nothing. Its record type is still out of scope for the rename/mapping work.
- **A trap worth recording: writing ANY column on a project fires `cascade_derived_name()`, which touches every work order on it and re-fires `enforce_work_order_record_type_eligibility()` on rows already in violation.** PROJ-00038 is an MF-Exhaust Fan Replacement project carrying three **Building Access** work orders its record type forbids, so **that project cannot be written at all today, by anyone, for any reason** — the defect logged 2026-08-31, now shown to be a hard block rather than a nuisance. The status move ran with the cascade suppressed (a status has no bearing on a derived name); the violation itself is untouched and still needs fixing.

**Shipped 2026-09-02 — A record type with no statuses selected shows none, not all 36 (branch `claude/project-record-types-mapping-xjnlfc`; 1 migration live on prod, advisors 220 / same known categories, +1 expected lint):**
Nicholas, on PROJ-00105 (MF-INS-AIR): the record page painted **all 36** project statuses as a 36-chevron path while Object Manager showed *"Selected Values · MF-INS-AIR: Empty"*. Two screens describing one configuration in opposite terms. His ruling: *"If we don't have any statuses on the selected values, it needs to just have none... The user needs to enter them,"* and *"really, any object with a status."*
- **Nothing was broken — this was the rule `20260816164500` installed deliberately.** Strict-everywhere had blanked every picklist on every record (`building_type` went to 0 values) because only 4 object/field pairs are scoped at all, so the resolver was made permissive. That fixed `building_type` and, in the same stroke, told every record type its lifecycle was "all 36 statuses of three different objects."
- **Permissive is right for an ATTRIBUTE picklist and wrong for a LIFECYCLE.** Strictness is now a declared property of the field — **`picklist_field_record_type_scoping` (PFRS-)**, `show_none` vs `show_all` — read by `picklist_values_for_record_type`. The two kinds stopped sharing one answer.
- **Which fields are lifecycles is read from the data, never from the name.** A `/_status$/` rule would have swept in `unit_occupancy_status` (Occupied/Vacant), the three CO-test results, `combustion_ventilation_status`, `provider_acceptance_status` and the two approval fields — **27 of the 33 `_status`/`_stage` fields are ordinary attributes**, and blanking those per record type would have been a defect, not the ruling. The seed is the union of the two places LEAP already records a lifecycle: the objects `status_transitions` governs, and the fields a `status_path` widget renders. They agree on **6 pairs** and nothing else.
- **The record's own value is never hidden.** New `p_current_value` is always returned even when it sits outside the selection or the selection is empty — so a stored status keeps showing, its path still marks where it stands, and saving an unrelated field can never silently clear it. A strict rule that could blank a stored value would be worse than the bug.
- **An object whose every record type shares ONE lifecycle was written out explicitly rather than left implicit** — work_orders (11 statuses × 76 types), incentive_applications (9 × 22), project_payment_requests (9 × 1). Zero visible change; it turns an assumption into configuration a person can edit. **Without it, flipping the rule would have blanked the incentive-application status path on all 24 layouts — the strip made legible the day before.**
- **19 record types now correctly show nothing until configured**, listed by the new `record_types_missing_status_configuration()`: **16 project** record types (their 36 values mix three objects' vocabularies, which is exactly the "you can't list everything"), the 2 enrollment **Project-Reservation** types, and **WI-FOE-2026** (its stage set was never built — already logged). Verified live: PROJ-00105 offers 0, incentive applications still 9, WI-IRA-MF-HOMES still 10, `building_type` still 9.
- **Both screens now say the same thing.** The status path renders *"No statuses are set up for this record type"* and names where to fix it instead of vanishing; an edit-mode picklist with zero options says so instead of rendering an empty `<select>`; Object Manager's badge reads **SHOWS NO VALUES** in amber (not a healthy green "SHOWS ALL VALUES") and its helper text states the real rule per field.
- **Open, found on the way and deliberately not folded in:** `picklist_show_in_path = false` filters the resolver, not just the path — so **"Unable to Complete" cannot be selected in any work order status dropdown**. That is a separate defect; the resolver needs to distinguish "hide from the path" from "cannot be chosen."

Active workstream: **Equipment selection — the auditor picks a model, he does not go shopping.** Plan, current-state map and the open decisions: `docs/leap-equipment-selection.md`. An energy auditor who is not an HVAC expert gets a short ranked list of specific models that the programme will pay for, that physically fit, and that carry the load at design conditions — never a search box over NEEP's 40,000 systems. **Three responsibilities, three homes, and they must not mix (Nicholas, 2026-09-05): the ASSESSMENT produces the design loads** (a separate session is adding those fields — this workstream reads them and adds no competing columns); **the ENGINE turns loads into ranked candidates and writes nothing**, so it takes numbers rather than a record and serves two front doors — a standalone Equipment Sizing tool where a user types the loads, and the opportunity line item with every argument pre-filled; **the OPPORTUNITY LINE ITEM records the choice** in `oli_equipment_product_id`, the column that already exists and is already enforced by `enforce_line_item_equipment_selection()`. **No new selection object.** **The catalog schema is already right and it is EMPTY** — `products` (97 cols) carries NEEP's entire reporting template (capacity at 47/17/5/-13 °F, COP@5, SEER2/EER2/HSPF2 IV+V, ducting configuration, variable capacity, AHRI + NEEP links) and `ahri_certificates` + `ahri_equipment` correctly model an AHRI reference as a certified *combination*, but zero products carry performance data and both AHRI tables have zero rows. **DECIDED and VERIFIED live 2026-09-05: ingest EPA ENERGY STAR open data (`data.energystar.gov` `w7cv-9xjt`), keyed on the AHRI reference number, deep-linking to NEEP** — not scraping ashp.neep.org. **The sandbox blocks that host; Postgres does not — the `http` extension calls it server-side (`http_set_curlopt('CURLOPT_TIMEOUT','30')` in the same batch, the 5s default is too short).** It publishes `heating_capacity_at_47_f_btu_h` / `_17_f_` / `_5_f_`, `cop_at_5_f`, SEER2/EER2/HSPF2, `cold_climate`, `compressor_staging` and `ahri_reference_number` — capacity and COP at 17 °F and 5 °F were the one thing that could have forced a paid AHRI subscription, and they are there. **281,975 rows, but an AHRI reference is a certified COMBINATION: 146,238 cold-climate rows resolve to 6,662 outdoor models, a 22:1 explosion concentrated in ducted split systems (the seven largest "brands" are rebadges of one line, ~63k rows).** So `products` gets one row per MODEL and `ahri_certificates`+`ahri_equipment` carry the combinations — never one product per AHRI row. Cold-climate ductless + single package is 15,479 rows over 5,116 models, so **scope by category first**; a manufacturer cut is the right second filter for ducted split, stored as data an admin edits. **`efr_reports`, `mechanical_equipment` and `equipment_information` hold ZERO rows, have no writer, and are to be retired — do not reference or build on them.** Awaiting Nicholas on §7.3–7.6, 7.8, 7.9.

Prior workstream: **Tasks, and the signature request that lives on one** — **Nicholas, 2026-09-05: these are TWO SEPARATE THINGS and must not be mixed** (*"I really don't want to mix up two things"*). E-signature is documents sent for signature with the email logged in Communications; tasks are Salesforce-style assigned work someone creates. D1 in `docs/leap-tasks-and-signature-requests.md` is **DECIDED** and overrides that doc's original recommendation to fold the signature request into the Task object as a record type — do not re-derive it. **Shipped: a signing request now sends from the programme's shared mailbox and lands on the record's Communications feed** (`send-envelope` v20 → `send-email-v1`). It used to go through `send-email-via-graph`, which sends as the CALLER out of their personal `user_outlook_connections` token — **both tokens on the platform are long expired**, the envelope owner's since 2026-05-02 and bound to the retired `anuraenergy.org` domain, so ENV-00014 read *Sent*, moved the enrollment to *Proposal Signature Requested*, and delivered nothing. It left **no trace at all**: that path writes its `email_sends` row only after Graph accepts, so the table was empty and the Sent event held only signing URLs. **A customer email must never depend on whether the sender linked their own Outlook.** The trap inside the fix, worth keeping: **`send-email-v1` answers HTTP 200 with `{status:"failed"}`** when Graph rejects a message, so reading `resp.ok` alone reproduces the exact silent failure — the body's own status decides. A failed send now writes `env_failure_reason` and records the outcome on the Sent event; the envelope deliberately **stays Sent** because `resend-envelope-email` accepts only Sent/Delivered, so marking it Failed would strand the one recovery path. **Still open, and this is the actual gap:** nobody has ever created a task in LEAP **because there is no way to** — `tasksService.js` is fetch/complete/reopen with no insert anywhere in the client; all 71 tasks are trigger-written, every one on a work order, every one still `Open`; `conversations` has no `task_id` so a task cannot carry communications; task statuses are free text with three competing vocabularies and zero picklist rows; and `tasks` carries no `block_hard_delete` trigger. Phases 1, 2 and 5 of the doc are that build.

Prior workstream: **Project record types → the products on the opportunity** — rename the project record types so each names its work explicitly (no state, no program, no abbreviation), map product → work measure → project record type in the database so a project created from an opportunity carries a record type that opportunity's own products imply, and guard it. Plan, current-state map and the open decisions: `docs/leap-project-record-types.md`. **`WI-IRA-MF-HOMES - AUDIT LEVEL 2` is deliberately out of scope (Nicholas, 2026-09-02) — do not rename, retire or re-map it.** Nothing built yet; the plan is awaiting Nicholas's answers on the six decisions in §7.

Earlier workstream: **Report Builder + viewer + dashboards enterprise rebuild** — Salesforce/Tableau/Power BI parity for filters, field/column management, sorting, groupings, group formulas, and dashboard cross-filter/drill. Full phased plan + code-grounded architecture map in `docs/leap-report-builder-enterprise.md`. Phase 1 shipped on branch `claude/report-builder-filter-logic-89lvva` (PR #237): shared filter kernel (`src/lib/reportFilters.js`), rebuilt Filters tab with validated `1 AND (2 OR 3)` filter logic + type-scoped operators + relative dates + multi-value, searchable field picker spanning related objects, group-header label fix (picklist/lookup labels instead of UUIDs), and viewer click-to-sort + per-column summarize. Phases 2–7 (grouping depth, PARENT/PREVGROUPVAL group formulas, server-side aggregation w/ via_path, viewer conditional formatting, dashboard cascade/cross-filter/drill, delivery+security) queued in the doc. Prior workstream (LEAP Pad field-service work orders, PRs #114–#133) live on master/prod.

**Shipped 2026-08-28 — Monthly Vehicle Equipment and Documents Check, a programme not a form (branch `claude/vehicle-inspection-work-order-do7r50`; 4 migrations + 1 cron live on prod, advisors 222 / same known categories, +1 expected lint; HA-00198):**
Nicholas: *"I want to make a work order and a work order type for a vehicle inspection, just to include these items only"* — then the fourteen items. This is the Vehicle Inspection member of the technician-creatable family that `fieldMobileService.js` has named in a comment since PR #128, delivered now that the step list exists.
- **WT-00093** → **WPT-00025** → **WST-00301 "Vehicle Equipment and Documents Check"**, 15 min. Technician-creatable, so it appears in the LEAP Pad **Create Work Order** menu with no deploy — the menu reads `work_type_is_technician_creatable` from the database.
- **One screen-flow step, not fourteen steps.** The precedent is WST-00201 "Attic / Ceiling", which carries **20** photo fields in one guided flow. Fourteen separate steps would mean fourteen separate verifications for a truck inventory.
- **`wst_is_screen_flow` is load-bearing here, not a preference.** Per-field photo capture and the "mark Not Present" control exist ONLY in the guided flow (`WorkOrderDetail.jsx`); outside it a `photo`-type field falls through to `StepFieldInput` and renders as a **text box**. The migration asserts the step is a screen flow rather than trusting it.
- **A missing item is recorded, never a blocker.** Each of the 14 items is a required `photo` field with `wstf_allow_not_present` — the mechanism `_work_step_evidence_gap` already understands (the attic's `hvac_flue_photo` uses it). Photograph the item, or mark it Not Present. Proved live: a truck with **nothing** in it answers all 15 prompts and the gate goes **CLEAR**, so the empty-kit inspection gets filed instead of abandoned. "Not Applicable" stays reserved for a vehicle that genuinely carries no such item.
- **New field type `vehicle`** (constraint widened; `StepVehicleField` + `fetchVehiclesForInspection`), built exactly like `key_source`: a picker over REAL records that stores readable text (`VEH-00002 · Truck 02 — Bravo Crew (FDE-2729)`), because a stored uuid prints as a uuid in the read-only view and in reports — the same trap the assessment report hit. Retired vehicles are dropped; **In Maintenance and Out of Service are kept**, since a truck in the yard can still be inspected. Wired into BOTH render paths (inline + screen flow), which is the thing that is easy to half-do.
- **Item names are `vi_`-prefixed** so `resolvePhotoTagLabels` resolves each photo's tag to its label — the Photos card reads "Fire Extinguisher", not a field name.
- **This is NOT the Fleet pre-trip / return inspection.** `vehicle_inspection_item_templates` (VIT-00001..32, `vehicle_activities`, the LEAP Pad **Vehicle Inspection** screen) checks the vehicle itself — tires, oil, dash alerts, new damage. This checks what is **carried in** the vehicle. Two questions, two artifacts; neither was reshaped to serve the other. Only First Aid Kit and Fire Extinguisher appear on both lists. **Worth a decision:** two things both called "Vehicle Inspection" is a naming collision a technician will trip over.
- **Proved end to end on prod in rolled-back transactions**, impersonating a real technician (Lucas Wood) under RLS: `create_technician_work_order` from a live stop returned success and stamped **VEHICLE-INSPECTION**; `work_order_detail_for_technician` returned **1 step, 15 fields** (1 `vehicle` + 14 `photo`); the gate refused an empty inspection by name; saving the vehicle and marking Umbrella Not Present both succeeded; answering all 15 cleared the gate. **Nothing was written** — 0 vehicle-inspection work orders and 0 field values on prod afterwards; highest WO still WO-00213.
- **Round 2 the same day — Nicholas: *"This is more of a monthly check."*** The checklist was the easy half; **a monthly check is defined by the chase, not the checklist**. A daily pre-trip triggers itself (you are about to drive); a monthly check happens only if something opens it, names an owner and shows what is outstanding. Nothing in LEAP recurred before this — the only scheduled things were cron pipelines. Kept as a WORK ORDER rather than folded into `vehicle_activities`, because the Fleet activity object has no owner, no verifier, no status lifecycle and no appointment; folding in meant rebuilding all four beside a screen built for checkout/return.
- **Renamed for cadence** — WT-00093 / WPT-00025 / record type **MONTHLY-VEHICLE-EQUIPMENT-CHECK**. Ends the collision with the Fleet pre-trip inspection.
- **`work_orders.vehicle_id`**, and with it the structural fix: **`project_id`/`opportunity_id`/`property_id` are no longer NOT NULL**, guarded by `work_orders_subject_check` — **a work order is about a PLACE or about a VEHICLE**. The alternative was inventing a placeholder "Fleet Operations" property to satisfy three columns, which is the workaround this repo forbids. Both inheritance triggers already tolerated NULL parents (`IF ... IS NOT NULL` / LEFT JOINs), and `wo_select` never touched property, so the blast radius was smaller than it looked — the two things that DID need work were the state-scope pair below.
- **State scoping: the POLICY and the RESOLVER are two different artifacts, and installing one does not install the other.** `install_record_state_scoping` rebuilds the policy (who sees a `work_orders` ROW); `install_record_state_scope_resolver` rebuilds `record_state_scope_work_orders`, which is what **work_steps, work_plans, photos, service_appointments, work_order_time_entries and vehicle_activities all call to resolve THEIR state**. Only the policy was rebuilt at first, so a state-restricted user saw a fleet work order and **none of its steps or photos** — caught by evaluating the resolver directly against real data, not by reading the policy. Both are now correct.
- **The vehicle route was registered, never hand-patched.** A hand-edited policy is silently reverted the next time the installer runs. Registering `parent_lookup → vehicles` **with `rsss_parent_key_column = 'id'` explicitly set** is what makes `build_record_state_scope_predicate` emit the `EXISTS (SELECT 1 FROM vehicles …)` form instead of calling a `record_state_scope_vehicles` resolver **that does not exist** (vehicles is `platform_configuration`, so no resolver was ever generated and the call would silently return false). Verified NC-only: 4/4 fleet WOs and their steps visible, 28 NC property WOs, **0 non-NC**.
- **`open_monthly_vehicle_equipment_checks()`** — one WO per active vehicle on the 1st (cron `0 13 1 * *`, 8am Chicago), owned by the vehicle's assignee, falling back to Shop Steward then Project Site Lead — **never to nobody**, since an unowned check is a check that does not happen. Idempotent (run 1: 4 created; run 2: 0 created, 4 skipped). Creates the service appointment + assignment too, so it lands on the Today view. EXECUTE revoked (cron runs as owner) so it adds **no** advisor lint. **The cron schedule is prod-only config, deliberately not in a migration** — same rule as the scanner and renewal jobs.
- **`save_work_step_vehicle(step, field, vehicle_id)`** takes the ID and lets the SERVER compose the stored text and stamp `work_orders.vehicle_id` from the same row, so the readable value and the joinable key can never disagree. The client sends an id and never composes the stored string. Wired into the inline path AND `advanceField` in the screen flow — and the flow's "unchanged" short-circuit is skipped for `vehicle` fields, because the pending value is an id while the saved value is text, so they are never equal.
- **A defect the first run exposed: the work order NAME is derived, and the deriver only knew about projects** — so every fleet check came out named "Monthly Vehicle Equipment and Documents Check" with no truck in it, and the opener's own name was overwritten. `work_order_inherit_parent_fields` now takes the **vehicle** as the leading segment when there is no project (`coalesce(v_project_name, v_vehicle_name, '')`), so the name reads "Truck 02 — Bravo Crew - Monthly …". This is also what a technician SEES: LEAP Pad's Today list and header fall back to `work_order_name` when there is no property, so before the fix every fleet check on the list read identically. A project name still wins, so **no existing name changed**.
- **`block_hard_delete()` means a migration cannot prove itself with a probe insert** — the cleanup is refused (correctly), and the whole migration rolls back. Behavioural proof belongs in a rolled-back transaction; what the migration asserts instead is that the deployed `prosrc` carries the vehicle branch, since a silent revert is the regression that matters.
- **RPT-00044 "Vehicles Not Yet Checked This Month Report"** — every check not yet Verified/Closed, oldest first; a row from a previous month IS the overdue list, so it needs no date filter. No flag table was needed (unlike `account_near_miss_flags`) because the opener guarantees one row per vehicle per month. Reports auto-embed FK label columns, so `vehicle_id` renders the vehicle's name, not a uuid. **Monthly Equipment Checks** related list added to the Vehicle Detail layout.
- **Open:** the LEAP Pad create sheet still asks property → building → unit → project for every technician-created type (noise for a fleet check, harmless — it inherits the stop). `work_order_detail_for_technician` returns null property/address for a fleet WO, so LEAP Pad falls back to the work order name and hides the address block — truthful, but the header does not name the vehicle explicitly. And `record_type_eligibility` constrains WO record types under **MF-Exhaust Fan Replacement** projects only — a technician on such a project cannot create ANY technician-creatable type, which predates this change and affects Building Access and Material Delivery equally.

**Shipped 2026-08-26 — The LEAP Assistant can see the screen, keeps what it learned, and never says a record doesn't exist (branch `claude/leap-assistant-degradation-ffqw4w`; no schema change; `ai-assistant` v35 live on prod, byte-verified against the repo; HA-00101 rewritten):**
Nicholas, standing on **Community Management Corporation's own contact list**: *"Create this contact for Community Management Corporation. He's the owner."* → *"No account found for Community Management Corporation."* Then: *"When the work is going on with the LEAP assistant, it's becoming really dumb… It's definitely not functioning like Claude."*
- **The account was never missing.** ACC-00026 `14ddfac7-…` is live and `global_search('Community Management Corporation')` returns it — verified on prod AND impersonating Nicholas's user under RLS, so it was not a permission or a search-quality problem. The assistant simply never had the account, and then asserted its absence.
- **Cause 1 — it could not see the screen.** `buildContext` read the app's selected RECORD only. A related-list screen has **no** selected record, so context collapsed to `{object:'contacts', record_id:null}` — confirmed from the stored turn in `assistant_conversation_messages`. The parent account's id and label were in the address bar the whole time, in the `rel=` token. New pure **`src/lib/assistantContext.js`** passes the related-list scope (table, fk, via-chain, parent id, parent label) and **decodes any LEAP URL pasted into the message** (record links and `rel`-scoped list links; a foreign origin is ignored). The edge function resolves the scope's parent OBJECT from its foreign key (`describe_object_columns`) and renders a **"[What the user is looking at right now:"** block stating it as a fact the model already holds — *that record EXISTS, use its id, do not search for it*.
- **Cause 2 — it forgot what it learned, which IS the "gets dumber as we work" complaint.** The panel replayed **text only**: every `tool_use` and `tool_result` was discarded between turns, so each new message started blind and re-derived (or lost) ids it had already resolved. The function now returns the **real transcript** and the panel threads it back. That makes trimming dangerous — a window opening on `tool_result` blocks whose `tool_use` fell outside it is **rejected by the API outright** (the assistant would stop answering at all) — so `trimHistory` advances the start to a genuine user turn, and carried tool results are truncated to 2,500 chars with a note (one `describe_object` on `properties` is 828 columns). Budget 60k → 140k chars.
- **Cause 3 — old model, no caching.** It was running **`claude-sonnet-4-6`** while Nicholas compared it to Claude in this window. Now **`claude-opus-5`**, env-overridable via **`ASSISTANT_MODEL`**, falling back through `claude-sonnet-5` → `claude-sonnet-4-6` only if a model is unavailable to the key (recorded in `fau_outcome_message`, never passed off as normal service). **Prompt caching** added: the system prompt is split so its stable half + the whole tool catalog cache (the volatile clock/caller block sits after the breakpoint, as it must), and the conversation carries its own breakpoint. That saving is what pays for both the working memory and the model.
- **Cause 5 — the cost column was fiction, and it chose the model.** The first cut of this wrote the pricing table and the cost arithmetic FROM MEMORY, and every part of it was wrong: Opus 5 at **$15/$75** (real: **$5/$25** — 3x over), Sonnet 5 at **$3/$15** (real: **$2/$10**), and a Haiku key spelled `claude-haiku-4-5-20251001` when **model ids never carry a date suffix**, so it could never match. Worse, the three input buckets were summed and billed at one rate — but a cache **write** is ~1.25x and a cache **read** ~0.1x, so the cost column would have RISEN when caching started working and hidden the entire saving. Priced per bucket now (`costOf` in `transcript.js`), and every turn records its cache split in `fau_outcome_message` (`cache N read / N written / N uncached`) — because "cache_read is zero across repeated requests" is the one symptom that means a silent invalidator crept into the prefix. The 3x Opus overstatement is also **why Sonnet was picked first**; at the real rate Opus 5 is 2.5x Sonnet 5, not 5x, and the input side is cached. **Lesson, recorded because it cost a wrong deploy: never write a model id, a price, or a token-accounting rule from memory — check the model reference.** 18 fixture checks now pin the table and the arithmetic.
- **Cause 4 — an empty search was reported as absence.** `global_search` / `fuzzy_resolve` now retry on **entity-suffix-stripped** forms ("Community Management Corporation" → "Community Management") and return a `relaxed_search` block, or an explicit *"tried these terms, found none"* note. Prompt rule added: **never state a record does not exist** — say you could not FIND it, and never contradict the screen-context block.
- **Fixtures pin every mechanism:** `scripts/assistant-context-fixture.mjs` (**25 checks**, built from the real `rel` token that failed) and `scripts/assistant-transcript-fixture.mjs` (**61 checks** — the orphaned-`tool_result` case that would break the assistant outright, the whole pricing/cost table, and the turn deadline). Pure edge-function rules live in **`supabase/functions/ai-assistant/transcript.js`** — **plain `.js` on purpose: `netlify.toml` pins `NODE_VERSION = 20`, and Node 20 cannot import a `.ts` file at all.** Authoring it as `.ts` passed locally on Node 22 (which strips types on import) and failed the deploy build in 17 seconds. `run-fixtures.mjs` now refuses any fixture importing a `.ts` module, naming Node 20 as the reason, so a green local `build:safe` means a green build. 33 suites green under **both** Node 20 and Node 22.
- **Verified, not assumed:** `ai-assistant` (v38 live) fetched back from prod and diffed byte-for-byte against the repo; the deployed function boot-checked over the `http` extension from Postgres (returns its own `Caller is not a registered LEAP user`, which proves the module loaded and `./transcript.js` resolved in Deno); `describe_object_columns('contacts')` confirmed to return `is_foreign_key` + `references_table='accounts'` for `contact_account_id`, which is what makes the screen-context block name the account rather than say "the record".
- **HA-00101 rewritten** — it still promised a Confirm button and "Confirm all" that were removed months ago, which is a wrong instruction, not a stale one.
- **Verified end to end, live (2026-08-27).** The sandbox's network policy blocks `supabase.co`, so verification goes **through Postgres**: the `http` extension calls the function server-side. New shared-secret **self-test** (`{"self_test":true}` + `x-pipeline-test-secret` against `internal_cron_auth` name `assistant_self_test` — the same shape `admin-test-send-email` and `property-owner-research` already use; prod-only config, deliberately not in a migration). Deliberately narrow: it builds the REAL system blocks and the REAL tool catalog and asks for one word, so it exercises the cache entry live traffic hits while reading **no user data** — it therefore carries no RLS risk and proves nothing about record access. Result: **served on `claude-opus-5`** (no fallback), reply `OK`, **11,080 tokens written to cache on call 1 and all 11,080 read back on call 2**. That prefix is the system prompt + tool catalog; on Opus 5 it is the difference between $0.0554 and $0.0055 of input per turn — the 90% cut that pays for both Opus and the working memory. The logged cost ($0.07817) matches the per-bucket formula to the cent, which validates the arithmetic fixed above against real API numbers.
- **Three hazards the model switch introduced, fixed before they bit** (moving to a thinking model is not a one-line change): (1) `MAX_TOKENS` 8192 → **16000**, because reasoning is drawn from the same budget and a 17-record batch would have been cut off again; (2) **`thinking: {type:'adaptive'}` is now sent explicitly** — on Opus 5 it is the default, but `ASSISTANT_MODEL` can point at Opus 4.8/4.7 where OMITTING it means the model does not think at all, a silent downgrade of exactly the capability that was failing; (3) a **turn deadline** (`deadlineState` in `transcript.js`) — edge functions die on a ~150s idle timeout, and up to 8 sequential thinking-model calls can exceed it, so past 100s the loop stops issuing tool rounds and composes an answer from what it holds, past 135s it does not even try, and the cut-short turn is told to **say what it did not get to rather than imply it finished**. A hang is worse than a short answer. `ASSISTANT_EFFORT` dials reasoning depth without a deploy.
- **PROVEN END TO END as a real signed-in user (2026-08-27).** The "no JWT is obtainable from the sandbox" claim was wrong — with privileged SQL you can mint one. Created a temporary **Project Manager** (not Admin) auth user, signed in through GoTrue's password grant via the `http` extension for a genuinely signed JWT, and replayed **the exact failing turn** with the real `rel` scope. Result: the context block rendered *"THE LIST IS FILTERED TO ONE PARENT RECORD: **accounts** 14ddfac7-… (Community Management Corporation)"* — it resolved the parent OBJECT from the foreign key — and the assistant **ran no search at all**, went straight to `describe_object` and proposed the contact with `contact_account_id = 14ddfac7-…`. Served on **claude-opus-5**; **37,984 tokens cache-read** / 5,116 written under real load; the returned `history` carries real `tool_use` / `tool_result` / `thinking` blocks, so working memory is live. Cost $0.0687. **Nothing was written** — verified 0 rows created by the test user, which also confirms the edge function only ever PROPOSES. Test identity removed afterwards (auth user and identity hard-deleted so the credential is dead; the `public.users` row is inactive + soft-deleted, since `block_hard_delete()` forbids more — **USR-00019 remains as a soft-deleted row**).
- **Two GoTrue facts worth keeping** (they cost an hour): a hand-inserted `auth.users` row fails login with a useless *"Database error querying schema"* until its **token columns are `''` and not NULL** (the real error, `confirmation_token: converting NULL to string is unsupported`, is only in `auth_logs` — go there first); and the `http` extension defaults to a **5-second timeout**, so any call to a thinking-model function needs `http_set_curlopt('CURLOPT_TIMEOUT','170')` **in the same statement batch**.
- **Still unproven:** `tool_choice:'none'` on the close call (only reached when a turn exhausts MAX_TURNS or the deadline) and the turn deadline firing in anger. Per-turn latency was not measured.
- **Open:** `resolveCallerUserId` still decodes the JWT with `atob` instead of `getUser()` (a long-standing item — it governs only usage logging and the caller's display name; every read and write is RLS-bound to the real JWT). The assistant still cannot see the list VIEW's own filters/sort, only the related-list parent scope.

**Shipped 2026-08-25 — One omni-channel Conversations area, and an email you drag onto a record files itself (branch `claude/call-logging-activity-tracking-1dbhqa`; 3 migrations live on prod, advisors 218 / same known categories, zero findings naming the new artifacts; HA-00188):**
Nicholas: *"I need to be able to drag over an email and add it to a contact record or an account in the Conversations tab, and it logs it correctly by reading who's involved… phone call messages need to be listed under the Conversations tab, which I'd prefer. We definitely need to make sure we have one omni channel for communication and tracking area for reach. For contacts and accounts."*
- **An email could only enter LEAP two ways, and neither was "somebody sent it."** LEAP sent it through Graph, or Graph delivered a reply the token matched to an existing thread. A message sent from someone's own Outlook, a forward from a program administrator, or a thread older than the account had **no route in at all** — and `unmatched_inbox` triage only ever links to a conversation that already exists.
- **Three purpose-named artifacts, none of them a widening of the send path:** `resolve_email_participants(text[])` — who each address is; `import_email_into_conversation(...)` — file one parsed email; **`message_participants` (MPART-)** — the answer, kept. "Reading who's involved" is a **fact we store**, not a parse-time guess: every From/To/Cc address becomes a row carrying the address, the display name as it appeared, and what it resolved to (contact / account / EES user / one of our own mailboxes / **unmatched**). An address that matched nobody is recorded as unmatched rather than dropped, so the gap stays visible.
- **Direction is read from the addresses, never from the record it was dropped on.** From one of our mailboxes or one of our people → outbound; anything else → inbound, and `conv_our_address` is the EES address that was actually on the To/Cc line. A thread between two outside parties, forwarded to us for the file, is filed under the person filing it — stated on screen, rather than inventing a mailbox that was never on the message.
- **Filing the same email twice does not duplicate it.** The internet Message-ID is the dedupe key (partial unique index on imported rows); a second drop returns the original. **The rollup trigger is corrected after the insert**: it stamps `now()` and counts an inbound message unread, and neither is true of an email being filed by hand — it was sent when it was sent, and the person filing it has plainly read it.
- **Matching is SECURITY INVOKER, deliberately.** A definer function here would hand a state-restricted user another state's contacts through the back door of a matched email address. Zero new advisor lints. `msg_provider` widened with **`manual_import`** — saying `microsoft_graph` would be a lie about provenance on a message we never sent.
- **The Conversations card is now the omni-channel area, not a list of email threads.** `list_communication_timeline(object, id)` returns email threads, text threads AND logged calls/meetings/notes in one time-ordered feed. **An account rolls up its contacts' threads and activities**, each row badged *via <contact name>* — Salesforce's own account-activity roll-up, and the reason an account page read as empty while every one of its contacts had history. **Log a Call** sits in the card header; a call opens a detail pane with no composer, because there is nothing to reply to. `list_relatable_records` had known only opportunities and projects since it shipped, so logging a call on a contact offered nothing to relate and never reached the account — contacts now offer their account, ticked by default.
- **The Outlook path is the one that had to work, so it was built, not assumed.** Dragging a message out of Outlook on Windows hands the browser a **.msg** (an OLE2 compound file), and often as a **virtual file** whose `getAsFile()` returns null — `webkitGetAsEntry()` is the only way to the bytes, and skipping that is why most "drop an email here" targets silently do nothing from Outlook. `src/lib/outlookMsgParse.js` is a dependency-free CFB + MAPI reader; `src/lib/emailMessageParse.js` handles RFC-5322/.eml (folded headers, RFC-2047 encoded words, quoted-printable, multipart alternatives, attachments excluded from the body); `src/lib/droppedEmail.js` **snapshots the DataTransfer synchronously** before any await, because its item list is emptied the moment the handler returns.
- **A .msg has two sources of truth that disagree by design** and neither is preferred wholesale: MAPI properties (the only source on a **sent** item) and `PR_TRANSPORT_MESSAGE_HEADERS` (the real header block, present only on a **received** item, and where the Message-ID comes from). Each field takes the source authoritative for it and **recipients are the union of both**. Bcc is not filed as a participant.
- **Proved by building a real .msg and reading it back** — `scripts/lib/outlookMsgFixtureFile.mjs` writes a genuine compound file, `scripts/email-drop-fixture.mjs` (**68 checks**) parses it. That is what caught the bug worth recording: **the mini-FAT chain must be bounded by the TABLE's length, not the file's sector count** — mini-FAT indices address 64-byte mini sectors inside the root's mini stream, so bounding them by the number of 512-byte sectors truncated every stream past the first few kilobytes, which reads as *"this recipient has no address"* rather than as an error.
- **Then proved in a real browser** (Chromium via Playwright, per the 2026-08-22 lesson): a real `.eml` dropped as a `File` on a `DragEvent` (base64 body via `atob`, RFC-2047 name, folded To header) parses; the same **`.msg` bytes** parse; a dropped **photo is refused**, not filed; and a **text-only drag** (no file at all) is read from its From/Sent/To/Subject block and says so.
- **Also proved against real prod data in rolled-back transactions**, impersonating Nicholas: an inbound email from Jane Henderson filed on her contact resolved her contact + Westminster Company, took `ira@ees-wi.org` as our address from the To line, and recorded the unmatched Cc; a second drop of the same Message-ID returned `was_duplicate` with the same message id and **2 messages written, not 3**; a reply from our mailbox filed as outbound; the account timeline rolled up **10 entries via Jane Henderson and Ron Cagno**. Nothing was written.
- **Open:** `message_participants` has no record page (same gap as `record_state_scope_sources`), so the participant rows are visible only through the message; the dropped file itself is not stored as an attachment yet (the parsed message is), so an email's own attachments are not carried in; and `portal_download_log` + `portal_record_grants` are still unregistered in `record_state_scope_sources` — noticed while registering `message_participants`, not this change's to fix.

**Shipped 2026-08-24 — The assessment report exists: an audit's own deliverable, generated from the work order that captured it (branch `claude/work-order-audit-report-ofcui5`; 2 migrations live on prod, advisors 217 / same known categories; HA-00187):**
Nicholas, from WO-00208: *"Trying to figure out where I actually print off the report for a work order for the audit assessment report. I tagged a bunch of photos to include in the report, but where do I actually create the report at?"* — then, correctly diagnosing it himself: *"this isn't a project reservation or a final. This is an assessment… we probably need a different record type and a different template for multi-family assessment reports, right?"*
- **There was nowhere to print it, and the flag he used had no consumer.** `photos.include_in_final_report` shipped 2026-07-20 as a curation marker; the only code that ever read it was the gallery's own "report only" filter and the field app's badge — **nothing built a document from it**. Phase 2 of `docs/leap-multifamily-energy-assessment.md` ("Roll-up, export & report wiring") was never built. The project's submittal documents are a different artifact entirely: keyed program × *incentive application stage*, six kinds (`audit` invoice / `proposal` / `invoice` / two Sealed / combustion notification), none an assessment report — and `NC-IRA-MF-HOMES` carries an empty document set, so nothing was on offer there at all.
- **A fourth rendering engine, not a reshaped invoice.** `energy_assessment` joins `ees` / `sealed` / `combustion_safety` in `paperworkModel.js` with **nine section types it shares with none of them** (fixture-enforced): cover, narrative, building summary, **captured section**, photo documentation, findings, deliverables, acknowledgment, footer. New kind `energy_assessment_report`; `buildSubmittalPdf` and `buildSubmittalPdfWithSignatureTabs` both dispatch it, so the e-signature route works the day it is wanted.
- **Keyed by the WORK ORDER record type, because the report's shape follows what was assessed** — a whole multifamily building vs a single-family home — not which program pays for it. `src/lib/assessmentReport.js` maps record type → document key; **MULTIFAMILY-ENERGY-ASSESSMENT is built, SINGLE-FAMILY and HES are DECLARED and not built**, so the action says the report doesn't exist for that shape rather than silently handing back another shape's document. Program variation rides the axis that already existed: a template scoped to an opportunity record type overrides the global default (Clone Template), so NC/WI/MI wording is config.
- **The report is honest about gaps.** Each section is driven by the work step TEMPLATE, not by the value rows that happen to exist — a question the assessor skipped prints its label with an em dash instead of disappearing, and a step marked Not Applicable prints as such with its reason. Zero is an answer, not a blank (fixture-pinned).
- **Photos print with the section that captured them** (`config.photos: 'step'` on every seeded section), not in one block at the end — Nicholas's own follow-up ask, so the report can be laid out to mirror the Asset Score report and read side by side with it. A trailing `assessment_photo_documentation` with `exclude_printed: true` catches anything left over and says so when there is nothing.
- **The template IS the layout.** `submittal_document_templates` **SDT-00006 "Multifamily Building Energy Assessment Report"**, 22 sections, seeded byte-for-byte from `DEFAULT_DOCUMENT_SECTIONS.energyAssessmentReport` so the stored template and the built-in fallback are the same document. Every section type has a typed config form in `submittalSectionSchemas.js`, so Edit Sections reorders, renames, rewords and re-columns the whole report with no deploy — which is what makes "same order, same headers as Asset Score" a data edit.
- **Action:** Actions → **Generate Energy Assessment Report** on the work order (`recordActions.js`, gated on `hasAssessmentReport(recordTypeValue)`). `RecordDetail`'s action context gained **`recordTypeValue`** — guards that key off configuration must use the stored value, not the display label an admin can rename (the QI Tool's existing label comparison happens to work only because that picklist's label equals its value).
- **Proved by rendering, not by reading.** `scripts/assessment-report-fixture.mjs` (**91 checks**) renders real multi-page PDFs: the full default template, every palette entry with its default config alone, a template naming a step the work order never captured (prints "not captured", does not throw), a report with no flagged photos, and an unknown section type (throws loudly). All 16 fixture suites and `npm run build:safe` green.
- **Live state of WO-00208 when this shipped:** 32 photos, **4 flagged**, and **zero field data captured** across all 15 sections — so the first report is a photo report until the auditor fills the sections. The modal states both counts before you press Generate.
- **Same day, second round — the report now MIRRORS the DOE Audit Template report** (Nicholas: *"What if I give you the audit template from asset score and you format it… same order, same headers, same sections, so that somebody can read them side by side? And they'd be in the same order, so all the photos from the systems would be on the same systems"*). Taken from the real report for **101-111 Queens Court, Rocky Mount** (PNNL Audit Template, ANSI/ASHRAE/ACCA **Standard 211-2018**). Its six parts, in its order, are now the report's order — Overview · Contact Information and Audit Details · Facility Description · Utility Data and Benchmarking · Energy Savings Opportunities · Attachments — with **its sub-section names as the headings** (Roofs, Walls, Windows, Foundation Types, Lighting, HVAC Systems: Heating / Cooling, Distribution Equipment and Zone Controls, Service Hot Water Systems), each fed by the LEAP work step that captured it and printing that step's flagged photos. **Two ordering facts, recorded because they are why this is not the work plan's walk order: Audit Template puts Windows BEFORE Foundation Types, and Lighting BEFORE HVAC.** "Building 360 Video" has no counterpart and cannot render in a PDF, so it has no section; contact/audit details live on the cover, so that part has none either. The migration **asserts the Windows-before-Foundation order** rather than assuming it, and soft-deletes the previous section rows instead of dropping them. Whole change was **template rows + the matching code default** — the engine did not change, which was the point of making the layout data.
- **Same day, third round — the report was cut down to what it is for.** Nicholas, on the first real runs: *"These sections are junk. We just want the photos. Just the tagged photos."* Removed the Overview narrative, Scope & Methodology, Energy Savings Opportunities, the Deliverables list, the Acknowledgment + signature block and the two "Attachments —" framings — **all of it invented, none of it asked for**. The report is now the cover, the Building Summary, one section per system (each appearing **only when it has photographs**), a catch-all for any tagged photo the sections did not show, and the footer. Within a section only ANSWERED questions print; a column of em dashes is filler. Other corrections from the same runs: **record ids never reach the page** (many `building_*` columns are picklist FKs holding a uuid, so the summary printed `09888d66-…` where **Apartment** belongs — everything resolves through `picklist_values` first and anything unnameable is dropped); the **Building Summary is building columns only** (property totals said nothing about the building walked, and it now also reads the alternate columns a fact can land in, e.g. `building_number_of_units`); the company is named for **the state the building is in** (`companyNameForState` — an NC building is assessed by *Energy Efficiency Services of North Carolina*, and the Monona address is withheld from a non-WI report rather than printed under another state's name); **status is never a trigger** anywhere (a photo that was taken is a fact whatever state the step is in; Not Applicable is recognised only by an explicitly recorded reason and no longer suppresses a section); the file is **named for the building**; and each photo in the PDF is a **link to the original capture with EXIF intact** (read-only signed object URL, 1-year TTL — note anyone holding the PDF can follow it, no sign-in). Fixture 91 → 119 checks. HA-00187 rewritten to match.
- **Open:** single-family and HES assessment reports are declared and unbuilt. The report does not model savings/cost/payback — the Audit Template's own Energy Savings Opportunities section reports those, and the report says so. Also noted while verifying: **WO-00208's opportunity OPP-00151 runs `NC-IRA-MF-HOMES`, not `NC-IRA-MF-HOMES-AUDIT`**, though the WO name says AUDIT and PROJ-00087 is a Multifamily Energy Assessment project — per the 2026-08-23 ruling that an assessment is the AUDIT program's own work, that pairing is worth a decision before templates are scoped per program.

**Shipped 2026-08-24 — Every object keeps its URL, its Back/Forward and its breadcrumb (branch `claude/navigation-breadcrumbs-bug-m310ns`; no schema change):**
Nicholas, from a work step: *"When I'm on work steps and work orders, it keeps taking back to the home screen. That breadcrumb doesn't work. Why does this keep breaking?… You have to retain the navigation, the breadcrumbs, URLs for every single object and sub-object, child, parent, everything,"* and *"I should be able to back arrow and forward arrow just like any other thing in Salesforce."*
- **Root cause: two hand-maintained allowlists that nothing checked.** `TABLE_MODULE_MAP` in `urlNav.js` decided whether `/<table>/<uuid>` was a record at all, and `TABLE_META` in `RecordDetail.jsx` supplied the breadcrumb's app name, object label and parent chain. The old comment said "keep them aligned when adding new objects" — that instruction WAS the defect. **103 objects have a record page; the URL list covered 52 and the display list 74.** Every object added to LEAP after those lists were written silently lost its navigation.
- **What that actually did.** For the 51 unaddressable objects (`work_steps`, `work_plans`, `photos`, `documents`, `activities`, `price_books`, `service_territories`, `occurrences`, `opportunity_line_items`, …) an in-app click wrote the right URL to the address bar, but `parsePath` could not read it back — it returned the **Home** module with **no record**. So browser Back, browser Forward, a reload and a pasted link all landed on the Home screen. Verified against the pre-fix code: `/work_steps/<uuid>` → `home`, record lost.
- **Second, independent cause of "the breadcrumb takes me home": list URLs built from the table name.** Section ids are not table names — Enrollment exposes enrollments as `enrollment`, Fleet exposes vehicle_activities as `activities`, Stock exposes product_items as `inventory` — so `/m/enrollment/enrollments` etc. hit a section the module doesn't declare and rendered its **Home tab**. That hit objects that WERE in the map.
- **The fix is a single derived registry, not a longer list.** New pure `src/lib/objectNav.js`: **derivation first — an object that is not registered still gets a working URL, a readable label and a host module.** `parsePath` now recognises `/<table>/<uuid>` **syntactically** (reserved routes `m`/`help`/`search`/`sign`/`auth` excluded), so there is no allowlist to fall off and a non-existent id is reported by the record loader instead of a silent redirect to Home. All 103 record-page objects are registered with their real module and their module's real section id (or `null` where the object has no list anywhere).
- **The URL grammar is now testable.** `parsePath`/`buildPath` and the query-string codecs moved to pure `src/lib/urlGrammar.js` (urlNav re-exports them, so no call site changed) — they previously could not be imported without React, which is why nothing ever caught this. `scripts/object-nav-fixture.mjs` (**230 checks**) pins the *mechanism*: an object nobody has registered still resolves to a record. There is no list to keep in sync, so this cannot silently regress.
- **Breadcrumb.** Module and object label come from the registry, so an unlisted object never renders `— / work_steps` again; labels are humanized with LEAP's acronyms kept (`efr_reports` → "EFR Reports", `ahri_certificates`, `gps_points`). The object crumb links only when a list genuinely exists and is **plain text otherwise** — never a link to a module Home. For the 29 objects with no declared `parents`, the parent chain is **derived from the page layout's own lookup fields** (excluding layout-declared system-audit fields and lookups onto `users` — an owner is not a parent), so a work step shows its Work Order the day its page is built.
- **`RecordLink` now renders a real `<a href>` for every object**, so open-in-new-tab / Cmd-click / copy-link work platform-wide and the URL they produce resolves back.
- **A record survives the module-access redirect.** App's guard called `navigateToModule(fallback)`, which clears `selectedRecord` — a shared link to an object whose default app the user's role lacks became a Home screen. It now carries the record to the fallback app (record access is RLS, not app membership).
- **Proved in a real browser** (Chromium via Playwright against the live hook, per the 2026-08-22 lesson that reading the code is not enough): from the Qualification opportunities list → work order → work step, **browser Back returns the work order and Forward returns the work step**, both keeping the module; reload keeps the work step; a cold deep link to `/work_steps/<id>` resolves (not Home); Back twice lands on the list; cold links to work plans / photos / documents / activities / price books all resolve. 14 fixture suites and `npm run build:safe` green.

**Shipped 2026-08-23 — Geographic (state) record access: a user can be restricted to one state, across every object (branch `claude/user-setup-nc-access-r8scf9`; 12 migrations live on prod, advisors 216 / same known categories, no new category; HA-00182):**
Nicholas: "I only want him to access North Carolina properties and information… this goes from records to accounts to contacts, absolutely everything," then, decisively: **"He cannot see anything outside North Carolina at all for any reason."**
- **It was not set up, and the thing that looked like it wasn't.** `app_user_can(object, action)` takes no record — object-level only, so a role that reads `properties` read all 21,534 in every state. `app_user_in_scope(object, record_id)` existed and looked like record-level security: **no policy anywhere referenced it** (zero), its third statement is `IF NOT v_is_portal THEN RETURN true` so internal staff bypass it by construction, it scoped by account/property lists rather than geography, and `user_account_scopes` held 0 rows. `service_territories` is dispatch geography, not security.
- **Three purpose-named artifacts:** `user_state_scopes` (USS-) — the grants; `record_state_scope_sources` (RSSS-) — the registry of how each object resolves to a state, one row per resolution *path* (an object is in scope if ANY path resolves); `install_record_state_scoping(object)` — generates that object's enforcement. Six kinds: `own_state_column`, `parent_lookup`, `child_reverse_lookup` (accounts ← properties), `polymorphic_lookup` (`related_object`/`related_id`), `platform_configuration`, `hidden_when_scoped`. **All 245 public base tables are classified — `record_state_scope_status()` reports zero unregistered**; 116 carry a live policy, 129 are configuration.
- **Enforcement is a RESTRICTIVE policy per object**, ANDed with what the object already had, so installing it on 116 objects could not widen anyone's access. `app_user_state_scope()` returns **NULL** for anyone unrestricted (Admin, portal user, or an internal user with no grants) and NULL short-circuits the predicate — **every existing user's access is unchanged** (verified: Project Site Lead still sees 21,534 / 7,536 / 112 / 57). `FOR ALL` + `WITH CHECK` means it governs writes too.
- **Fail closed, per Nicholas's ruling:** a record that cannot be *proved* to be in the user's states is not shown.
- **Four hard-won lessons, each of which changed the shape** (full detail in the doc): (1) **inline joins in a policy recurse** — `work_orders.wo_select` reads `service_appointments` and the new `service_appointments` policy read back, giving `infinite recursion detected in policy for relation "work_orders"`; any table reference in a policy is subject to that table's policies, so the chain must resolve where RLS does not apply. (2) **`plpgsql EXECUTE` re-plans per row** — one generic definer function fixed the recursion and then timed out counting 7,536 accounts; the answer is a **generated resolver per object with a STATIC `LANGUAGE sql` body** so the plan is cached. (3) **a bare STABLE function in a policy is evaluated per row** — the cancellation named `app_current_role_name during startup`; wrap it as `(SELECT public.app_user_state_scope())` so the planner hoists it into an InitPlan, which is **exactly the shape LEAP's existing policies already use** (`( SELECT app_user_can('accounts','read') )`). Corollary: `x = ANY ((SELECT f()))` parses as the subquery form and fails `text = text[]` — cast it `::text[]`. (4) **generating 110 granted functions is 110 advisor findings** — policies now go through the single entry point `record_in_state_scope(object, id)`, itself SECURITY DEFINER, so the nested resolver runs as owner and EXECUTE stays **revoked** on all 110 resolvers.
- **Proved by impersonating James in rolled-back transactions against real prod data**, not by reading code: properties 5,597 of 21,534 with **0 outside NC**; accounts 1,816 (matches the independent NC-linked count exactly); contacts 22/57; buildings 46/92; opportunities 54/97; projects 10/36; work orders 22/112; work steps 195; photos 100; field history 55; config objects fully visible. Writes: creating a WI/TX/GA/MI property is **blocked** by `state_scope_properties`, editing an NC property succeeds, moving an NC property to WI is blocked, and a blanket UPDATE or DELETE of every WI property touches **0 rows**. Nothing was written — every check rolled back.
- **`app_current_role_name()` requires `user_is_active = true`.** An inactive user has **no role at all**, so `app_is_office_side()` is false and work orders vanish for reasons unrelated to geography. James was created inactive at first and his access looked far narrower than it is.
- **James: USR-00018, new role Operations Manager** (object + module access copied from Project Manager — 107 objects, 14 modules — and added to `app_is_office_side()`), granted NC. **No auth link, so he cannot sign in; the invite is deliberately unsent.** Still needed: his last name (`user_last_name` is NOT NULL and currently empty) and work email.
- **Admin surface:** Users list gains a **Record Access** column (emerald state badge, or a neutral "All states"); **Manage** opens `UserStateAccessModal` — state chips with the property count behind each, a banner stating plainly which of the two states the user is in, and **Preview access** → `preview_user_state_scope_access(user_id)`, which counts visible vs total per object using the same generated predicate and never returns records. State options come from `list_state_scope_options()`, read from the data.
- Pure rules in `src/lib/stateScope.js` + `scripts/state-scope-fixture.mjs` (26 checks), which pins the dangerous one: **an empty grant list means unrestricted, not "no access."**
- **Open (needs Nicholas):** the fail-closed edges — 35 contacts hidden because their company holds no NC property (EES's own staff contacts are the case worth checking, since they name crews in dispatch), `unmatched_inbox` 482 of 483 hidden, the six `hidden_when_scoped` staging tables. Also: `record_state_scope_sources` has no record page, so a new resolution path is a database change today (same gap as `record_audit_column_overrides`); and the dead `app_user_in_scope` / `user_account_scopes` / `user_program_scopes` trio should be rebuilt on this engine or retired. Full map: **`docs/leap-geographic-record-access.md`**.

**Shipped 2026-08-24 — An out-of-state program record type is impossible, not merely un-offered (branch `claude/incentive-record-type-filtering-fnx3lf`, PR #531; 3 migrations live on prod, advisors 217 / same known categories; HA-00179 + HA-00183 corrected):**
Nicholas: "It should be impossible to have a Wisconsin opportunity record type on North Carolina property. Need to switch them," and — answering the one question 08-23 left open — "yes, of course, they use different payment request application forms."
- **The grandfather clause was the hole.** Both state rules (`20260823002712`, `20260823202021`) returned early when an UPDATE left the record type and parents alone, so a pre-existing mismatch stayed saveable forever. Correct while the five FOE-2024-WI rows awaited a decision; a hole once it was made. **Removed from both** — a record whose program doesn't run where its property is can no longer be saved at all.
- **The five, switched to NC-IRA-SF-HOMES-AUDIT.** Identified from the data, not the names: two single-unit (`property_total_units = 1`) Huntersville NC properties, one `SINGLE-FAMILY-ENERGY-ASSESSMENT` project each, one work order, no assessments/applications/appointments, no stage. Single-family energy assessments, and **an assessment is the AUDIT program's own work** (the 08-23 ruling). Run with **triggers enabled** — `derive_opportunity_name()` recomposed all five names and cascaded them to the projects, `set_opportunity_price_book_from_record_type()` repointed the price book, and the audit log records a real change of program. `opportunity_stage` left NULL (it already was; which stage a scheduled assessment belongs in is a business call). The migration re-counts mismatches afterward and **raises** rather than shipping a record the new rule would make uneditable.
- **The one path that could still create a mismatch: the PROPERTY moves.** Every rule fired on the child; `cascade_property_state_to_opportunities` carried only `opportunity_state`, so correcting a property WI→NC stranded every Wisconsin program on it. New `enforce_property_state_change_against_program_records()` (`trg_zz_*` BEFORE UPDATE OF `property_state`) **refuses the property's state change** while any opportunity, incentive application or assessment on it runs another state's program, naming them — there is no automatic conversion (FOE-2024-WI has no NC counterpart), so it is a decision, not a cascade. Safe to make absolute: **0 live properties have a null state**, so there is no bulk "set the state for the first time" operation to block.
- **NC and MI payment-request forms.** 08-23 deliberately did not mirror WI-IRA-MF-HOMES-PROJECT-PAYMENT-REQUEST because nobody had confirmed the other states use one. Confirmed → mirrored with layouts cloned (10 sections each). All three MF HOMES programs now offer two forms, application + payment request, and are expected to diverge. Only the **multifamily** form exists in WI, so only it was mirrored.
- **Proved in rolled-back transactions on real data:** WI program onto an NC opportunity **refused**; a normal edit of a valid opportunity still saves (no collateral lock-out); moving the NC property to WI **refused, naming ASMT-00025 + OPP-00142/143/144** (which also proves the guard covers all three objects); a property with no program records still changes state freely. Platform-wide cross-state count across opportunities, applications and assessments: **0**.
- **Help articles corrected, not appended:** HA-00179 and HA-00183 both promised a grandfather clause that no longer exists; HA-00183 also said NC/MI had no payment-request form. Fixed in place.

**Shipped 2026-08-23 — Incentive application record types are scoped by state AND by opportunity record type (branch `claude/incentive-record-type-filtering-fnx3lf`, PR #528; 3 migrations live on prod, advisors 217 / same known categories; HA-00183):**
Nicholas, straight after the opportunity fix: "NC properties only see NC incentive record types… these need to be also tied to opportunity record types, not any incentive record type should be able to be created for any opportunity record type," then "this is similar to assessments also." The same two rules assessments got on 08-22 (`20260822213141` + `20260822213503`), applied to the object that had neither.
- **State.** All nine active application record types carried `picklist_state = NULL` — eight Wisconsin programs and **Electrify Denver (Colorado), which was ALSO the flagged platform default**, so `enforce_rt__incentive_applications` stamped a Colorado program on any application inserted without a record type. Scoped WI/CO; the object now has **no default at all** (an incentive application is always some program's application, so unlike opportunities there is no nationwide type for the flag to move to — derivation replaces it).
- **Program.** `record_type_eligibility` now governs `opportunities → incentive_applications`, one edge per program: **21 of 22** opportunity record types constrained (Field Operations is not a program, so it constrains nothing). The three FOE years share the one **WI-FOE** form; **WI-IRA-MF-HOMES** also carries WI-IRA-MF-HOMES-PROJECT-PAYMENT-REQUEST, which is how the live IA-00002 was built.
- **NC and MI got their own six IRA application record types**, mirrored from Wisconsin with **their page layouts cloned** (12 types, 12 layouts, section/widget counts matching sources exactly). Not mirrored on purpose: WI-FOE (a Wisconsin utility program), the WI HOMES payment-request form (whether NC/MI use one is a program fact nobody here knows), Electrify Denver (no CO opportunity record type exists).
- **Guarantees in the DB:** `derive_incentive_application_record_type()` (`trg_0_*`, must sort before `trg_enforce_record_type` — unlike assessments, whose `trg_assessment_0_*` already did) takes the opportunity's program when none is supplied; `enforce_incentive_application_record_type()` (`trg_zz_*`) refuses a form from another state or another program, naming both. Pre-existing rows grandfathered. **Proved in rolled-back transactions on real NC data:** WI form on an NC property rejected, NC-IRA-SF-HEAR on an NC-IRA-MF-HOMES opportunity rejected, the program's own form accepted, no record type supplied → derived NC-IRA-MF-HOMES (previously ELECTRIFY-DENVER), live WI application still editable. Nothing written.
- **Two defects found on the way, fixed at the root.** (1) **`validate_page_layout_widget_config()` made any layout carrying a virtual field un-clonable** — it knew related fields only by the dotted `fk.column` spelling and knew nothing of `field_metadata` inherited/formula/roll-up fields. The 2026-07-29 conversions (`20260729153704`) deliberately kept **non-dotted** names (several `ia_*` fields resolve to one source column), so 20 related fields + 3 inherited ones tripped it. It only ever passed before because those conversions rewrote `widget_config` in place, row by row; **any re-INSERT — a clone, Setup's Clone Layout button, an import — failed, on every object with such a field.** Both shapes now validate properly (a bad `fk_column` or missing source column still fails; 5 cases verified). (2) **`fetchAvailableRecordTypes` widened to the FULL active set when the state filter came back empty**, so a Texas property (4,037 live; GA 2,150, MN 1,742) was offered every Wisconsin form. It now returns empty with `_noneInState` and the picker says no record type runs there and names the configured states — never another state's programs, never advancing with no record type.
- **Client:** `fetchConstrainingParentForCreate(childTable, seed)` in `layoutService` reads which parent/child pairs are governed **from `record_type_eligibility`** (parent object → its `{prefix}_id` FK on the seed → that parent's record type), so a pair configured in Setup later works with no deploy; it replaced the assessment-specific inline block and is used by **both** create paths (pop-up + inline `+ New`). `stateHasNoPrograms()` added to `src/lib/programStateScope.js`, pinned in `scripts/program-state-scope-fixture.mjs` (32 checks). Incentive applications joined `CHILD_RECORD_TYPE_SCOPES` in `RecordTypesPane`, so the matrix is editable at Object Manager → Opportunities → Record Types → **Application forms**.
- **Both open items closed 2026-08-24** (see the entry above): the five FOE-2024-WI opportunities were moved to NC-IRA-SF-HOMES-AUDIT, and Nicholas confirmed NC/MI HOMES use their own payment-request forms, which now exist.

**Shipped 2026-08-23 — Opportunity record types are scoped to the property's state (branch `claude/state-filtered-opportunities-du6ae4`; 3 migrations live on prod, advisors 220 / same known categories; HA-00179):**
Nicholas, from the New Opportunity pop-up on BLD-00153 ("931 Tessie Street - Rocky Mount", North Carolina): it offered FOE-2024-WI, the whole Michigan set and every Wisconsin program. "North Carolina properties only get North Carolina opportunities."
- **Two independent root causes.** (1) **The state was never known.** The picker has filtered on `picklist_state` since the assessment work on 08-22, but the create-prefill seeded that state from the parent record's own state column — which works from a property and fails from a BUILDING: `building_state` is null on **24 of 73** live buildings (a building only inherits the address when its property carried one), and a null state falls back to the full list. (2) **The platform default was a Wisconsin program.** `default_record_type_for('opportunities')` resolved to **FOE-2024-WI**, and `enforce_rt__opportunities` stamps it on any opportunity inserted without a record type — which is what `create_service_appointment` (the public scheduler) does. That is how OPP-00132/133/134/135/140 came to run a Wisconsin 2024 program on properties in **Huntersville, NC**. Those five are left as-is (which program they should have been is a business call) but can never be added to.
- **The state belongs to the property, and is now derived there.** `opportunities.property_id` is NOT NULL, so it is always resolvable. `sync_opportunity_state_from_property()` (trigger `trg_0_opportunity_state_follows_property`) forces `opportunity_state` to the property's state on insert/update, and `cascade_property_state_to_opportunities()` carries a property's state change down — the same shape as the 2026-07-09 rule that an opportunity's account always equals its property's. **82 of 94** live opportunities carried a null state before this: the column was declared in the baseline and never maintained. Backfilled under `session_replication_role = replica` so 82 migration writes didn't land in `audit_log` as if a person had made them.
- **The guarantee is in the database.** `enforce_opportunity_record_type_state()` (`trg_zz_*`) refuses a record type whose `picklist_state` differs from the property's state, naming both states and what is available where the property actually is; nationwide types (state null — Field Operations) pass anywhere; a property with no state constrains nothing. Pre-existing mismatches are **grandfathered** — the trigger returns early when neither the record type nor the property changed — so the five NC rows stay editable. Proved live in rolled-back transactions: WI-IRA-MF-HOMES on the Rocky Mount property rejected with the full message, NC-IRA-MF-HOMES-AUDIT accepted and stamped `NC`, and an unrelated edit to OPP-00132 still saved.
- **The platform fallback record type must be nationwide** — the default flag moved FOE-2024-WI → **Field Operations**. A platform-wide default cannot be state-specific; this is what stops the scheduler silently stamping a program from another state.
- **Client:** new pure `src/lib/programStateScope.js` (`scopedToState` / `statesInRecordTypes` / `needsStateChoice` / `programStateFromSeed`) — an **unknown** state narrows to the nationwide types and asks, it never widens to every program. `fetchProgramStateForCreate(seed)` in `layoutService` resolves the state from the **property** (seeded property → seeded building's property → the building's own state → any `*_state` on the seed), used by both create paths: `RelatedListWidget.handleNewClick` (as the transient `__programState`, which `prefillState` now prefers) and `QuickCreateModal`, so an inline lookup **+ New** is scoped identically. `RecordTypePicker` gained the **state selector** Nicholas asked for: when the record can't say where it is (a module's New button, a list-view New), it asks for the state first and lists only nationwide types until then — and never auto-picks a lone nationwide type while a state is still owed. When the state IS known it shows as a badge so a short list reads as deliberate. `scripts/program-state-scope-fixture.mjs` (24 checks).
- **Retired: the two generic assessment opportunity record types** (Nicholas: "Multifamily assessments and single-family assessments shouldn't be record types. Make those inactive. Don't delete them"). MULTIFAMILY-ENERGY-ASSESSMENT / SINGLE-FAMILY-ENERGY-ASSESSMENT were the last nationwide program-shaped opportunity types, left from before assessments became per-state — an assessment is the AUDIT program's own work (NC-IRA-SF-HOMES-AUDIT and the rest, seeded 08-22). **Inactive, not deleted**: gone from the picker, kept on the 9 live opportunities that carry SINGLE-FAMILY-ENERGY-ASSESSMENT, stage sets untouched. The identically named **projects** and **work_orders** record types are deliberately untouched.
- **Open (needs Nicholas's call):** `create_assessment_work_order` and `create_mf_building_assessment_work_order` (the LEAP Pad self-service assessment paths) still resolve those two retired values by name when they create the assessment opportunity — they keep working, but the right target is the property state's audit program, and which program and which of its stages an ad-hoc field assessment belongs to is a product decision. The five FOE-2024-WI opportunities on NC properties were resolved 2026-08-24 (moved to NC-IRA-SF-HOMES-AUDIT).

**Shipped 2026-08-22 — Create date / Created by / Last modified date / Last modified by on every record (branch `claude/system-info-fields-4109gs`, PR #519; 6 migrations live on prod, advisors 220 / same known categories; HA-00177):**
Nicholas: "Every single record should have this… Every single page layout needs that under the System Information section. If not there, create it." Placing four fields was the easy half — the data behind them was largely fiction, so this shipped the platform rule first.
- **What was actually wrong.** 21 of the 103 objects with a record page were missing at least one of the four columns outright (`activities` had no `created_by`; `picklist_values` and `roles` had no updated pair; `page_layouts` had no updated-by). "Last Modified" barely existed: only 27 of 96 tables with an `updated_at` had anything maintaining it, and **nothing in the platform ever stamped `updated_by`** — `saveRecord` sends only the changed fields, so every hand-written `<prefix>_updated_by: userId` scattered through the services was a workaround for a missing rule. On the layout side, of 112 System Information field groups only ~40 carried each field, and ~100 record layouts had no System Information section at all.
- **The rule, in three purpose-named artifacts** (`20260822225224`): `resolve_record_audit_columns(object)` — one definition of which columns are an object's audit fields, by the table's dominant column prefix, accepting the bare (`created_at`) and `_id` (`tasks.created_by_id`) spellings; `record_audit_column_overrides` (RACO-) — the registry for objects that don't follow the convention; `install_record_audit_stamping(object)` — adds any missing columns and generates that object's BEFORE INSERT/UPDATE trigger. **101 objects stamped.** INSERT fills created_at/created_by (else now() / the acting app user) and seeds updated_* from them, so a new record's Last Modified is never blank; UPDATE sets updated_at/updated_by and holds created_* immutable (a NULL can still be filled, so backfills stay possible). Proved in a rolled-back transaction on `accounts`: correct stamps on insert and update, and a deliberate `created_at = '2000-01-01'` tamper bounced.
- **Generated per-object trigger functions, not one generic jsonb trigger** — `properties` carries **828 columns**, so a `to_jsonb` / `jsonb_populate_record` round trip per row would tax every write on the platform's biggest table. The generated functions assign the four columns directly and are SECURITY INVOKER, so they add **zero** advisor lints.
- **`audit_log` (742k rows) and `field_history` are in the override registry as append-only logs** — their `al_performed_at`/`fh_changed_at` pair IS both the create and the last-modified stamp, so they carry no stamping trigger and no duplicate columns. That decision also avoided a full rewrite of a 116 MB table to store the same fact twice.
- **Backfill ran under `session_replication_role = replica`** — otherwise the stamping trigger would have overwritten every backfilled `updated_at` with now() (defeating the point) and the audit logger would have written ~15k snapshots recording a migration as if a person had edited each record. Created-by was recovered from the column that already held it (`performed_by`, `uploaded_by`, `taken_by`, `triggered_by`, `author_user_id`, `assigned_by`, `user_id`); a genuinely unknown author stays NULL (802 baseline picklist values) rather than being attributed to someone.
- **Layouts** (`20260822225628`): all **213** active record layouts now carry the four, stripped from any other field group first so nothing renders twice, appended in a fixed order below whatever the group already held (Record Type, Inactive…), with the section created — Details tab, 2 columns, collapsible, collapsed by default — where it was missing.
- **Read-only is declared by the layout, never guessed from the column name** (`20260822225751` sets `system_audit: true`). Created By / Last Modified By are `lookup` fields onto users, so before this they rendered as **editable dropdowns** offering a change the trigger silently reverts. A name pattern would have swept in `vehicles.vehicle_odometer_updated_at`, a real business timestamp a user edits — the same trap the clickable-contact-field work hit, where the fix belonged in the field's declared type.
- **Client:** new pure `src/lib/systemAuditFields.js` — `isSystemAuditField(field)` (layout flag → read-only) vs `isSystemAuditColumn(name)` (name rule, used ONLY where there is no layout to consult: create-form field selection and editable-column metadata, where a false positive costs nothing). The old inline regex in `FieldGroupWidget` required a leading `_`, so the bare `created_at` / `created_by_id` spellings a dozen tables use were rendering as blank inputs on the create form — fixed via the shared rule, and `fieldMetadataService` / `createRecordFields` / `objectListService` now agree with it. `fieldRenderKey(field, i)` because a field name is not unique within a group (an append-only log binds two labels to one column, and an admin can drag any field into two sections). `scripts/system-audit-fields-fixture.mjs` (52 checks) pins all of it, including the documented false positive.
- **Not built (needs Nicholas's call):** `record_audit_column_overrides` is registered in the Object Manager catalog but has no record page layout, so adding a future exception is a database change; Object Manager is schema administration only (Details / Fields / Layouts / Record Types — no Records tab). HA-00177 says so plainly rather than promising a screen that doesn't exist.

**Shipped 2026-08-22 — Back goes back one screen, not to a module Home page (branch `claude/property-page-contact-links-1il2mj`, PR #508; no schema change):**
Nicholas, from an assessment: Back landed on a Home screen instead of the property or opportunity he came from. Nothing recent broke it — `urlNav.js` was last touched 08-13; this was the behavior since record URLs shipped. **Root cause:** a record URL is deliberately just `/<table>/<id>` so it can be shared, so it carries no memory of the screen it was opened from; the URL being the source of truth means every browser Back/Forward re-derived state from the path alone. Reproduced in Chromium against the real module (Enrollment › Properties → property → assessment → Back): the property URL came back with **`section: null`**, so the module chrome behind the record fell back to its **Home tab** (that's why a property page's own top breadcrumb read "Enrollment / Home"), and the in-app back — breadcrumb, mobile arrow — then had no list to return to and pushed the module's Home page. **Fix:** new pure `src/lib/navTrail.js` keeps an in-page trail of the entries this page session pushed, indexed into `history.state`; on popstate it restores ONLY what the URL can't carry (module + section) and only when the entry matches the path, so a reload or a deep link keeps URL-derived state. `closeRecord` now goes **back** (`history.go`) to the previous screen instead of pushing a fresh list page; a record's `?tab=` entries share its trail key (`pushRecordSubPath`) so they're stepped over in one go while browser Back still steps through tabs. With no in-page history the fallback is the record's own object list — **never a module Home** — which needed `TABLE_LIST_SECTION_MAP` aliases (`opportunities→opps`, `incentive_applications→applications`, `efr_reports→efr`, `project_payment_requests→requests`, `payment_receipts→received`, `portal_users→users`): building a list URL from the table name landed on a section id the module doesn't have. Also: a FAILED `my_accessible_modules` lookup no longer reads as "may see nothing" and bounce the user to Home (`accessibleModules` stays null; RLS still governs). **Second symptom, same PR — the error screen flashing on every module switch:** it's the stale-bundle screen (a deploy lands while a tab is open → the next lazy module chunk 404s), and its 3-second countdown to the reload was being **cancelled by the ErrorBoundary auto-resetting on the module change** — so it flashed away without picking up the new build and did it again on the next switch. The boundary now holds a stale-chunk error until the reload happens, and the breadcrumb it logs gets ~700ms to land before the reload aborts it (which is why none of these ever appeared in `client_errors`). `scripts/nav-trail-fixture.mjs` (29 checks) covers the flow that broke, the tab-entry skip, the deep-link fallback, forward-stack truncation, and the StrictMode double-invoke; `scripts/run-fixtures.mjs` discovers every `scripts/*-fixture.mjs` and runs in `npm run build:safe`. **Hard-won:** verifying a nav change needs a real browser — mount the hook in a scratch Vite page and drive it with Playwright (`/opt/pw-browsers/chromium`); reading the code was not enough to see the lost section.

**Shipped 2026-08-22 — Phone / email / website fields are real links everywhere (branch `claude/property-page-contact-links-1il2mj`, PR #506; 2 data migrations live on prod, advisors 219 / same known categories):**
Nicholas, from the Website on PROP-00506: it rendered as dead text. Two halves were missing. **Renderer:** new pure `src/lib/fieldLinks.js` resolves a value to `mailto:` / `tel:` / `https:` (US numbers → E.164 with the extension kept as `;ext=`, bare hosts → `https://`, http(s) only — `javascript:`/`data:`/`file:` never link) and returns **null** for anything not actually actionable, so a note in a phone column, two numbers in one field, or a storage key in a url column stay plain text instead of becoming a dead link; `FieldValueLink` renders the anchor (stopPropagation so a number inside a list row dials instead of opening the record). Wired into record pages (own fields, cross-object related fields, formula/rollup/inherited), related lists (desktop + mobile cards), and list views; `url` joined the typed editors and phone/email/url inputs carry the matching mobile keyboard in both create paths. **Types — the reusable part:** these columns are plain `text` in Postgres so the layouts inherited that (`property_website` typed `text`; `property_phone`/`property_email` carried no type at all). **`field_metadata.fm_display_type` is the place a column's logical type belongs** — it's already overlaid onto every layout at load, so one row fixes the field on all of an object's layouts and any built later; never fix this in a layout, and never hardcode a name heuristic. Migration `20260822194532` seeds 89 real contact-method columns (23 url / 28 email / 38 phone) across the user-facing objects, excluding storage-path columns (`photos`/`documents` `file_url`, profile photos, `portal_url_path`), import staging and system/log tables. Related (cross-object) fields froze their parent column's type at placement, so `overlayRelatedFieldDisplayTypes` re-reads it at load from the cached `describe_object_columns`. List columns carry it as `linkType`, kept separate from `type` so a website still *filters* as text. `scripts/field-links-fixture.mjs` (44 checks) covers every value that must NOT become a link. Help article **HA-00174**. Side effect worth knowing: a column typed `phone` is normalized to 10 digits on save by the existing phone-normalizer, so HUD raw phone columns will canonicalize the next time someone edits one.

**Shipped 2026-08-18 — Property Owner Portal repaired and proved (branch `claude/property-owner-portal-status-h16n55`; 3 migrations + `portal-email-visit` v1 live on prod, advisors 219 / same known categories):**
Nicholas asked for the portal's status. It had **never worked in production** — built 2026-06-29 → 07-01, never touched again, and nobody had ever logged in, so three independently fatal defects sat undetected: (1) **no self-select RLS on `portal_users`** — `fetchPortalUserSelf()` is an RLS-bound SELECT and the table carried only the internal-staff `app_user_can(...)` policies, which evaluate against `public.users`, a row no portal user has; the SELECT returned nothing and the portal bounced back to its own login screen forever (the fix was written 2026-06-29 and the migration was never applied); (2) the login gate compared `me.status !== 'Active'` against the **`Portal User …` picklist**, so every invited owner was told their access was inactive — the provider portal, written a month later, had it right; the same stale `"Active"` sat in `portal-email-visit`; (3) **`get_portal_calendar()` did not exist in prod** (its migrations were never applied) and `portal-email-visit` was never deployed — the client try/catches the failure, so the Calendar tab rendered *empty* instead of erroring, which is why it was never noticed.
Also: **repo↔prod migration drift** — prod carried `portal_tracker_account_scope_hardening` with no file in the repo, so a branch replay produced the **pre-hardening** tracker (no `property_account_id` guard); captured verbatim, byte-verified via `md5(prosrc)`. **A record type that didn't exist** — `portal_invite_create` always wrote `record_type = 'Portal User'` with only `Provider User` ever seeded; added the purpose-named **Property Owner User** record type and pointed the RPC at it (no backfill — zero rows carried the old string). The restored calendar now carries the tracker's account guard so both reads scope identically; **anon revoked** on both (matching `get_provider_portal_data`); provider logins refused by the owner portal; stale `PortalModule` filter options (Status `['Active','Inactive','Suspended']`, which matched no row) corrected to the real picklists.
**Proved** by impersonating portal users in rolled-back transactions against real data (PROP-23587): correct-account owner → own row only, 1 property / 2 opportunities / WI-IRA-MF-HOMES 10-stage track / 51 work orders / 51 calendar visits; owner granted **another account's** property → 0 and 0; suspended owner with a valid grant → `no_portal_user`; non-portal user → 0 rows. Nothing was created (all rollbacks clean; grants still 0). Help article **HA-00108** rewritten — it still described a hardcoded 10-phase HOMES/HEAR bar and no calendar.
**Round 2 the same day — photos, and the storage hole they exposed:** with login finally working, every photo in the portal rendered as a broken image. Two defects: (1) **photos could never have rendered** — `photos.file_url` holds a *storage path*, not a URL, `work-evidence` is private, and `thumbnail_url` is NULL on all 499 work-step photos, so `<img src={path}>` 404'd against the site root (the internal app resolves paths via `hydratePhotoUrls()` → `createSignedUrl`; the portal was written as if `file_url` were public); (2) **every private bucket was readable — and deletable — by any authenticated user**, since the policies were blanket `TO authenticated USING (bucket_id = '<x>')` for all four verbs, and portal users hold the `authenticated` role. The moment portal login worked, any property owner or provider could list/download/overwrite/**delete** every object in `work-evidence`, `property-documents`, `program-applications`, `service-provider-documents`, `templates`, `signatures`, `communications-attachments`, `fleet-evidence` — including other owners'. Latent until Lane A; nothing exploited it. Fixed: **every private bucket locked to internal staff** (each policy keeps its bucket condition and gains `current_app_user_id() IS NOT NULL` — true for staff, false for portal users; verified internal still sees all 1,238 `work-evidence` objects, a portal-only user now sees **0** in every bucket; no portal client touches `supabase.storage` at all, and `portal-uploads` is locked the same way pending its own grant-scoped policy), plus purpose-built **`portal-photo-urls`** v1 (resolves caller → portal user, re-resolves each photo through `work_step → work_order → property/building`, filters to their grants with the same account guard as the read RPCs, returns 15-min signed URLs, watermarked variant preferred). `PhotoStrip` signs lazily per strip via a module-level cache with in-flight de-dup. **Noticed, not fixed:** `process-photo` writes `storage_path_watermarked` with the filename doubled (`…/<uuid>.jpg/<uuid>.jpg`) — objects exist at those keys so rendering works, but fix it before the table grows.
**Shipped 2026-08-23 — Portal View As for internal admins (same branch; 3 migrations + `portal-photo-urls` v2 live on prod):** Nicholas: *"All the system admins from our side should be able to log in as a user to see what they use… what if I don't have a user on a portal? The admin has to verify and check before we invite a user."* Salesforce **Login As** parity, as **two separate modes** — neither substitutes for the other: **View Portal as This User** (Actions on a `portal_users` record → that user's own grants + account guard) and **View Owner Portal** (Actions on an `accounts` record → every property on the account, *works with no portal user in existence*, which is the pre-invite check that unblocks the workflow — only 2 portal users exist platform-wide, so a user-only feature would have been useless). Both read RPCs gained `p_view_as_portal_user_id` / `p_preview_account_id` (defaulted NULL, so the portal's own calls are unchanged; exactly one may be supplied). **Admin-only enforced in the DB** (`app_is_admin()` inside every RPC and inside `portal-photo-urls` v2) — the URL is a request, not a grant; verified live that a Project Site Lead is refused on all four entry points. **Logged**: new `portal_view_as_sessions` (PVA-) records who viewed as whom and when, written only by `portal_view_as_start()`. **Unmistakable**: dark banner naming target + mode on every screen (survives screenshots) with Exit preview; sidebar shows the target, not the admin. Read-only by construction — when Phase 1 adds portal writes they must refuse while a view-as context is set. Help article **HA-00180**. This **replaced a stopgap** (pointing an internal auth identity at a `portal_users` row) that only worked for one account and would have broken `fetchPortalUserSelf()`'s `maybeSingle()` the moment a second row existed; the stopgap row + its 23 grants were soft-deleted.
**Shipped 2026-08-25 — Program Manager Portal, built end to end (same branch; 5 migrations + `program-portal-file` v1 live on prod):** Nicholas: *"I need to create a Program Manager portal… share certain objects, like assessment records and project records… only the records that I select, like the actual record IDs… totally read-only… They can download stuff if we give them permission, but we want a record if they download stuff."* **A third scoping model, not a variation:** the owner portal scopes by property ownership, the provider portal by work-order assignment; a program manager owns nothing and is assigned nothing, so the shared thing is a **RECORD**. New **`portal_record_grants`** (PRG-, polymorphic object+id, assessments/projects today) — no implicit access, nothing visible that is not named. **Everblue = ACC-07622, Program Implementer** (the record type existed with zero accounts; this is the first). Third portal user record type **Program Manager User** + Program Manager / Program Reviewer roles. **`get_program_portal_data()`** resolves a granted assessment (or project) down property → building → assessment → **assessment work order** → work steps → photos, plus any generated Energy Assessment Report. **Two deliberate constraints:** work orders are filtered to the assessment record types (an assessment's project can also carry install work orders — not shared, must not leak on adjacency), and the assessment header is an explicit **field whitelist** since field tiers are unbuilt. **Downloads:** permission at BOTH levels as asked (`accounts.account_allow_portal_download` + `portal_users.portal_user_allow_download`, both default false, both required), and every download writes **`portal_download_log`** (PDL-) — which is *why* files are served by **`program-portal-file`**: a URL the browser signs for itself cannot be logged. That function re-derives reachability server-side on every call (file → work step → work order → project → a live grant) and serves watermarked inline views vs. original downloads. **`/program-portal`** is its own surface — property → building → assessment, reports, work steps, photo strips with a lightbox; no search, no record pages, no edit controls. **Manage Shared Records** action on a Program Manager User lists every assessment and project and shares them one at a time (revoke = soft-delete, so it stays auditable that access existed); **View Portal as This User** now routes by record type. Verified in rolled-back transactions: 2 granted assessments → 2 properties / 2 buildings / 2 work orders / 30 steps / **99 photos** (72+27 ✓); no grants → nothing; a Property Owner User deliberately given a program grant → refused on record type. **Data reality worth knowing:** only **6 of 18** assessments have a work order at all, and **no Energy Assessment Report PDF has ever been generated** (template exists, documents don't). **Not built:** the invitation path — Everblue has no contact or portal user yet, and `portal_invite_create` is property-grant shaped; testing goes through admin View As. Plan + current state: **`docs/leap-program-manager-portal.md`**.
**Shipped 2026-08-24 — Include in Property Owner Portal (same branch; 2 migrations + `portal-photo-urls` v3 live on prod, advisors 217 / same known categories):** Nicholas: *"on the account record, the property record, and the building record, there must be a radio button that says 'Include in property owner portal'. I need some real tactile controls on what's viewable, what's not."* Portal visibility is now **two independent axes, both of which must say yes**: **publication** (the new checkbox on accounts/properties/buildings — is this record published at all?) and **grant** (`portal_user_property_grants` — who may see it). Publication cascades top-down: **account is the master switch** (off = that organisation publishes nothing), property publishes itself and everything beneath, building publishes itself + its units/opportunities/projects/work orders/photos/visits (property still lists as an empty container when only the building is excluded). Defaults deliberately asymmetric — `accounts` **false** (publishing an org is an affirmative act), `properties`/`buildings` **true**; backfilled the 34 accounts that already had portal content to true so the reviewed previews kept working, everything else starts unpublished. **One definition of visibility**: `portal_visible_property_ids()` / `portal_visible_building_ids()` apply publication AND the grant/account guard in one place, used by the tracker, the calendar and `portal-photo-urls` v3 — instead of three drifting copies of the same CTEs; EXECUTE revoked from anon+authenticated (internals, not an API, so zero new advisor lints). **Proved in rolled-back transactions** on LSS (baseline 23 properties / 3 buildings / 56 visits): account off → 0/0; one property off → 22 properties, visits 56→5; its building off only → 23 properties with 0 buildings on that one, visits 56→5. Control placed on **all 19** account/property/building record layouts in a purpose-named *Property Owner Portal* section; Portal module list shows a *Not published* badge + published/total counts; the portal's admin preview explains an empty result by naming the flag. Help articles **HA-00108** + **HA-00180** updated.
**Still viewing-only.** Document upload, in-portal e-signature, record-level comments, messaging, and payment status are unbuilt; portal roles are decorative (nothing reads `Property Administrator` vs `Property Viewer`); `AddToPortalModal` grants whole properties only though the table and both RPCs support building-level grants. Full current-state map, pain points, and the phased Lane B plan: **`docs/leap-property-owner-portal.md`**. Open calls in that doc: property-level opportunities are invisible to owners (tracker requires `building_id`); `portal-email-visit` sends from a hardcoded `assessments.wi@EES-WI.org` instead of purpose-routed `obm_purpose`; the first real invitation should go to an internal address before any property owner.

**Shipped 2026-08-16 — New = pop-up, required fields only, everywhere (branch `claude/building-creation-modal-fields-k7ovzs`; migration `20260816164500` live on prod, advisors 219 / same known categories):**
Nicholas's standing rule, from creating a Building on a property and getting a full-page form with dozens of inherited fields and a dead Type dropdown: **every manual New/Add opens a modal that asks for the required fields only — whatever the object, record type, or page layout — and the record page is where everything else is filled in.**
- **Presentation, not a second create engine.** `RecordDetail` in `mode='create'` now renders itself as a centered portal modal instead of the full page: required fields only (DB NOT NULL ∪ layout `required:true`, minus system/audit, trigger-derived names, formula/rollup/inherited/related, spacers, and no-write fields), grouped and ordered by the page layout, with a **Show all fields** toggle, the duplicate soft gate, and Cancel/Save. Everything underneath is untouched — parent-chain prefill, `RecordTypePicker`, dependent lookups, per-table create defaults (enrollment inheritance, Project-Reservation defaults), `applyInsertDefaults`, and the insert path all behave exactly as before, so inherited values still land on the record whether or not the modal displays them.
- **One interception point.** `App.jsx` wraps `navigateToRecord`/`replaceRecord`: `mode:'create'` opens the modal above the current page instead of pushing `/<table>/new`, so the page behind stays live and Cancel returns you to it. A typed/bookmarked `/<table>/new` opens the same modal and drops the create URL. Covers related-list New, list-view New, Setup New, module New buttons, and every module wrapper (they all delegate to these two). `ObjectListSection` keeps its list mounted for local (non-URL) creates.
- **Field selection is a tested pure module** — `src/lib/createRecordFields.js` (`buildCreateModalGroups`, `listUnlaidOutRequiredColumns`) + `scripts/create-modal-fields-fixture.mjs` (11 checks). Required columns the layout doesn't carry are resolved to real editors from `describe_object_columns` (picklist/lookup/number/date/boolean), which is also what makes the modal work on an object with no layout at all.
- **Root cause of the dead Type dropdown (migration `20260816164500`):** `picklist_values_for_record_type` was made strict on 2026-08-13 — a record type with no explicit `picklist_value_record_type_assignments` selection returned ZERO values. Only 4 object/field pairs in the platform are scoped at all (`accounts.tax_classification`, `enrollments.enrollment_status`, `opportunities.opportunity_stage`, `projects.project_status`), so every other picklist went blank on any record with a record type. Now: a record type WITH a selection still shows only those values (strict, no leakage — the opportunity-stage rule is untouched); a record type with NO selection shows every active value. Verified live: `building_type` 9 values, WI-IRA-MF-HOMES stage still 10, an unconfigured project record type 36 statuses instead of 0.
- Inline lookup **+ New** (`QuickCreateModal`) got the same typing upgrade: required picklist columns render as dropdowns (re-scoped when the record type changes), non-`TABLE_META` FKs as lookups, and number/date/boolean as their own controls instead of text boxes.
- **Config gaps this surfaced (not fixed here, needs Nicholas's call):** opportunity record types **FOE-2024-WI / FOE-2025-WI / FOE-2026-WI** have no stage assignments (5 live opportunities), so they now list all 186 stages instead of none — they need their own purpose-built stage sets per the hard rule. 17 of 18 project record types have no `project_status` selection.
- Help article **HA-00172** (`creating-records-pop-up`).
- **Follow-ups the same day, all merged to master (PRs #472–#476):** **#472** Assessment create — `assessment_building_occupancy_rate` widened `numeric(3,2)`→`(5,2)` (the create-prefill seeds the property's occupancy *percentage*, 94.09, so every save died with "numeric field overflow"); all 12 assessment layouts repointed off the legacy uuid columns onto `opportunity_id`/`building_id` as typed, property-scoped lookups, `building_id` added to the 5 layouts missing it, `assessment_property_contact_for_iq_assessment` given a real FK to contacts; the parent record a create was launched from is now shown read-only (`__lockedFields`); `fetchPageLayout` fills in the editor type for any layout field that doesn't declare one (from `describe_object_columns`), which is what stops legacy untyped uuid fields rendering as text boxes platform-wide. **#473** derived names stop repeating a record-type label the parent name already ends with. **#474** dropped the dead legacy columns `assessment_opportunity` / `assessment_building_del` after a full dependency sweep (0 views/functions/policies/live layouts/reports referencing them; 0 rows where they disagreed with the real FK). **#475** inline lookup **+ New** names lookups after the object they point at (not the column — `opportunity_id` was rendering as "ID") and skips trigger-derived columns. **#476** `resolveInheritedParents(targetTable, known)` — the shared rule that a create never asks for what the platform can derive: same column → same *relationship* under a different prefix (`work_order_account_id` satisfies `project_account_id`) → one hop up the chain (a project's account comes from the property). Used by BOTH create paths; a work order created from a record with no project resolves the opportunity's most recent live project.
- **Deploy hazard (2026-08-16):** master merges #474–#476 sat undeployed for ~40 minutes while deploy previews built fine — production builds queued/stopped on Netlify's side. **Verifying a merge is not verifying a deploy:** check the site's published `commit_ref` (Netlify API `get-project` → `currentDeploy`, then `get-deploy`) before telling anyone a fix is live. Build env lives in committed `netlify.toml`, so a local `npm run build:safe` is byte-for-byte reproducible if a manual deploy is ever needed.
- **Same-day follow-up — Assessment create from an opportunity (migrations `20260816173700` + `20260816174500`, live on prod):** Nicholas hit three things creating an Assessment off an opportunity. (1) **"numeric field overflow" on save** — the assessment create-prefill seeds occupancy from the property's occupancy *percentage* (94.09 on PROP 5513 N Hopkins), but `assessments.assessment_building_occupancy_rate` was `numeric(3,2)` (max 9.99). Widened to `numeric(5,2)` to match every other occupancy figure in LEAP; verified with a rolled-back dry-run insert of the exact failing payload. (2) **Opportunity came in as a raw UUID text box** — the assessment layouts bind Opportunity/Building to the legacy import columns `assessment_opportunity` / `assessment_building_del`, plain uuids with **no FK** and no `type` on the layout entry, so the renderer fell back to text. Fixed at the root: all 12 assessment layouts now bind `opportunity_id` / `building_id` (typed lookups scoped by `opportunities_for_property` / `buildings_for_property`), `building_id` was added to the 5 layouts that never carried the NOT NULL column, `assessment_property_contact_for_iq_assessment` got a real FK to `contacts` (so it types as a contact lookup), duplicates were deduped, and the create-prefill stopped double-writing the legacy copies (the old workaround). Live data was consistent (12 rows, 0 mismatches) so nothing moved; the legacy columns remain in place, unused — dropping them is a separate call. (3) **The parent you created from is no longer editable** — `handleNewClick` marks every prefilled column pointing at the parent record (`__lockedFields`), and the pop-up renders those read-only with the parent's *name* resolved, not its id. Also: `fetchPageLayout` now fills in the editor type for any layout field that doesn't declare one (from `describe_object_columns`) so legacy untyped fields stop rendering as text boxes platform-wide; FK columns not on a layout are labeled by their target object ("Building", not "Id"); and write failures surface Postgres `details`/`hint` instead of a bare message.

**Shipped 2026-07-27 — Sealed documents sectioned + submittal document template editor (branch `claude/sealed-docs-template-editor-jhtz3i`; migration `20260727025206` live on prod, advisors ~202):**
Phases A + B of `docs/leap-project-paperwork-port.md` §7c. **Phase A:** `buildSealedPdf(m, kind, sections)` is now section-driven — 10 `SEALED_SECTION_RENDERERS` (9 types; rebate section used twice) on a Sealed context (`bh`/`lines9`/zebra/`sect` helpers, not EES grid helpers), byte-identical to the shipped Sealed proposal/invoice. `DEFAULT_DOCUMENT_SECTIONS.sealedProposal`/`.sealedInvoice`; `DOCUMENT_KIND_ENGINE` + `SECTION_TYPES_BY_ENGINE` + `buildSubmittalPdf` dispatch EES vs Sealed. `sdt_kind` widened with `sealed_proposal`/`sealed_invoice`, both templates seeded. Harnesses cover both Sealed docs; `scripts/paperwork-math-fixture.mjs` (38 checks) committed. **Phase B:** lazy `SubmittalDocumentTemplateEditor.jsx` (Edit Sections action on `submittal_document_templates`) — dnd-kit reorder, engine-keyed Add-Section palette, activate/remove, typed config forms (`src/data/submittalSectionSchemas.js`), live PDF preview, Clone Template scoped to an opportunity record type. Service: `loadSubmittalTemplateForEdit`/`saveSubmittalTemplateSections`/`cloneSubmittalTemplate` in `paperworkService.js`. HA-00151.

**Shipped 2026-07-27 — Signing route for generated PDFs (Phase C; migration `20260727130400` + `send-envelope` v4 live on prod, advisors ~202):**
The Final Project Payment Request invoice (`homes_project_invoice`) can now be sent for a property owner's signature — it's produced in-app by paperworkModel and has no `document_template`, so the e-signature pipeline had no route to it. Additive path per §7b: `envelopes.document_template_id` nullable + new `env_source_document_id` FK + CHECK exactly-one; `send-envelope` v4 accepts `source_document_id` + explicit `tabs` (skips template/snapshot/render, reads PDF bytes from the documents row, builds tabs from the caller) with the template branch byte-unchanged. `buildSubmittalPdfWithSignatureTabs(m,kind,sections)` returns `{blob,tabs}` — the `acknowledgment_and_signature` section records the property-owner sig+date tabs in bottom-left PDF coords (H=792) matching the portal overlay + pdf-lib stamp; capture is opt-in so default renders stay byte-identical. Client: **Send for Signature** on any requires-signature stage doc in `ProjectSubmittalDocumentsModal` — generates the PDF+tabs, `uploadDocument`s to the project, calls `send-envelope`; confirm dialog defaults the recipient to the property-owner contact and returns the signing URL when Outlook isn't connected (safe test mode). HA-00152. **Next: a controlled internal test send (to an internal address) before the first real customer send — then coverage for other programs is config, not code (clone templates + map stage requirements).**

**Shipped 2026-07-26 — Per-user list view defaults that apply on load (PR #221; migration `20260726184725` live on prod):**
Nicholas: two stars in the view selector and the default never survived a refresh. Root causes: (1) the default flag lived ON the shared `saved_list_views` row and save-time dedupe only cleared rows `list_view_owner`-ed by the current user — the seed defaults are owned by his legacy `nicholas@anuraenergy.com` user record, so they could never be un-starred from the ees-wi.org login; (2) `ListView` loaded the default into a state variable nothing read, so it was never applied. Fix: new `list_view_user_defaults` table (one pinned view per user+object, unique constraint, RLS = own rows only via `current_app_user_id()`); existing flags backfilled (latest-updated wins) and legacy `list_view_is_default` cleared/deprecated — never read or write it again. `listViewsService` computes `isDefault` per-user and exposes `setDefaultViewForObject`/`clearDefaultViewForObject`; `ListView` applies the default on first load (filters/sort/columns, once, skipped after user interaction; drill-downs opt out via `applyDefaultViewOnLoad={false}` from `ObjectListSection`). Admin Saved List Views column is now "My Default" (RLS means admins see their own pins). HA-00093 updated.

**Shipped 2026-07-26 — Dashboard rehaul round 2: 43-component library + on-canvas filters (PR after #229):**
- Palette now **43 components**. New this round: **Speedometer Gauge** (gas gauge — needle + sky/amber/emerald segment bands from cond breakpoints), Bullet/Linear Gauge, Progress Ring, Rating, Multi-Row Card, Combo (bar+line, opt-in dual axis), Pareto, Rose, Radar, Sunburst, Sankey, Calendar Heatmap, Box Plot, Matrix/Pivot (totals + value-shaded cells), **on-canvas filter widgets** (Picklist/Toggle/Date Range — filter every widget on change, slicer-style; state in `DashboardRunner`, fields join the override list), Link card. New `runWidgetData` shapes: `combo`, `filter`. Not built with reasons in the rehaul doc: geo map (GeoJSON asset), state timeline (no interval history), packed bubbles (no native type), AI visuals (→ AI workstream). HA-00113 updated to the 43-type list.

**Shipped 2026-07-26 — Dashboard rehaul Phases B+C: 25-component library + query shapes (PR #229, merged to master/prod same day):**
- **DB (migrations `20260726200000` + `20260726210000`, applied to prod, advisors unchanged at 205):** widened `dashboard_widgets` dw_width/dw_height to 1..12/1..24 and replaced the hardcoded 9-type `dw_widget_type` whitelist with a slug-shape check (registry governs types) — both were save blockers for canvas dashboards. New SECURITY INVOKER RPCs `report_aggregate_2d` (category×series pivot), `report_aggregate_time` (day/week/month/quarter/year buckets, optional series), `report_aggregate_single` (no-group aggregate), sharing `report_aggregate` semantics via `_report_agg_*` helpers; all verified live.
- **Client:** `runWidgetData` in `reportsService` routes each widget type to its query shape (`WIDGET_QUERY_SHAPES`) with row-fetch fallback — metric/gauge/KPI no longer pull every report row. Palette grew 12 → **25 components** (KPI Card, Stat+Sparkline, Stacked/Clustered/100% Stacked Bar, Area (time), Pyramid, Treemap, Waterfall, Heatmap, Scatter, Histogram, Image); table widget gained ordered column picker (`multi-field` inspector control)/sort/totals; metric/KPI/stat conditional color breakpoints (emerald/amber/sky); reference lines + axis titles on bar/line/area; palette search box removed (Nicholas: browsable list, users can't search names they don't know); content widgets render in the runner without a report fetch. HA-00113 updated with the full library.

**Shipped 2026-07-26 — Dashboard widget type conversion + canvas save fix (PR #220, merged to master/prod same day; dashboard rehaul Phase A ECharts port included):**
- **In-place widget type conversion:** the dashboard inspector's new **Widget type** dropdown (grouped by palette category) converts a placed widget between types without delete-and-recreate — report binding, title chrome, and position survive; shared config (group_by/measure/sort/limit) carries via `convertConfigForType()` in `componentRegistry.jsx`; the canvas grows the tile to the new type's `minSize`. HA-00113 updated.
- **Save-blocking bug fixed:** `dashboards_dash_columns_check` capped `dash_columns` at 4 (form-editor era) while the canvas adapter saves 12 — every canvas dashboard save failed the constraint. Widened to 1..12 (migration `20260726190000`, applied to prod, advisors clean).
- **Dashboard rehaul is the ACTIVE WORKSTREAM (Nicholas, 2026-07-26: "enterprise class… 25 or more components… full rehaul… why aren't you just building"):** complete research-backed spec in `docs/leap-dashboard-rehaul.md` — vendor component synthesis (Power BI/Salesforce/Tableau/Grafana → 38-type master list), Salesforce per-widget/dashboard parity checklist, phased plan (A: polish/port renderers; B: four new query-shape RPCs; C: expand to 25+ components; D: subscriptions/drill/view-as). **DECIDED: Apache ECharts engine** (Apache-2.0, DB-storable JSON themes); **TanStack Table** over AG Grid Community (grouping/aggregation is AG Enterprise-paywalled). **Phase A first increment shipped on the same PR:** `src/builder/chartKit/` (LEAP ECharts theme + lazy core in its own `vendor-echarts` chunk — match `/echarts/` path-delimited in manualChunks, bare `echarts` also matches `recharts`), bar/line/pie/donut/funnel/gauge ported to themed fill-height ECharts with data labels/legends/styled tooltips/drill preserved, new additive config keys `show_data_labels`/`show_legend`/`number_format`/`decimals` in the inspector. Recharts stays only for 7 non-dashboard modules. Advisor baseline note: security lints now total ~205 (drift from recent sessions' RPCs — same known categories, nothing new from this session's DDL).

**Shipped 2026-07-26 — WYSIWYG card placement + cross-object page-layout fields (migration `20260726190000`, applied to prod):**
Nicholas's ruling: "sections should function exactly the same on all tabs" — whatever a section holds displays where the section is placed.
- **Cards render in their section, on its tab.** Related lists / file galleries / conversation panels / publish history / embedded reports are no longer force-relegated to the Related tab: `RecordDetail` renders each main-flow section as shell + its cards in widget order (cards hidden in insert mode; card-only sections contribute no empty shell), and `buildOrderedTabs` derives tabs purely from section placement (cards no longer conjure a Related tab; right-rail sections contribute no tab names). Data migration moved **113 legacy card-only sections** (no field group — authored under the old force-to-Related rule) from Details to the Related tab so every live record page kept its exact appearance; sections carrying a field group (the canvas editor always adds one) kept their tab, so cards now appear beside their fields. Editor tiles dropped the "· Related tab" hint.
- **Cross-object (related) fields, Salesforce-style drill-down:** the layout editor palette gained a "Related Object Fields" group per outgoing FK (audit FKs excluded) — expand, click a parent column, and a READ-ONLY field lands in the section as `{name:'<fk>.<col>', type:'related_field', related:{fk_column,table,column,column_type,lookup_table?,lookup_field?}}`. `loadRecordDetailData` fetches each referenced parent row once (grouped by FK, RLS-respecting) and merges values into the record under the dotted names; parent picklist columns resolve via the global picklists map, parent lookup columns get display wiring resolved at placement time. Renderer shows a blue RELATED chip, always display-only (hidden on create; dotted keys stripped from the save diff). `validate_page_layout_widget_config` extended: dotted names validate FK-exists + is-FK + column-exists-on-referenced-table (verified live: accepts `property_id.property_hud_management_org` on opportunities, rejects bad columns and non-FK prefixes).

**Shipped 2026-07-26 — Page layout editor: first-class tabs (PRs #206/#210, live on master/prod):**
Nicholas's ruling: tab structure must be *visible* — no per-section Tab dropdown ("no way to visually see that"). `LayoutCanvasEditor` now shows the record page's tabs across the top of the canvas as file-folder-styled tabs (filled shape, rounded top corners, hover states, active tab raised white with emerald top accent — scoped `lce-` stylesheet since inline styles can't do `:hover`): Details, Related, custom tabs (alphabetical, matching `buildOrderedTabs` in the renderer), and a dashed "Activity · automatic" ghost. `placement='right'` sections render in a **live always-visible Right Sidebar rail beside the canvas** (WYSIWYG — they show on every record tab; drag sections into/out of the rail; the rail is a `tabdrop::` drop target). Canvas shows ONLY the active tab's sections; pills carry section counts. Tabs are created (+ New Tab), renamed (pencil, moves all its sections), and deleted (×, confirm dialog moves sections to Details) — Details/Related standard. Sections move between tabs by dragging the ⠿ handle onto a pill (`tabdrop::` ids join the section drag family); the per-section **Tab and Placement dropdowns are gone**. Add Section is tab-local; drag-reorder maps tab-filtered positions back onto the full array so global `section_order` (drives Related-tab card order) is never scrambled. No schema/renderer change — tabs persist through each section's `tab` value, so an empty tab is editor-local until it has a section. HA-00122 rewritten. Hard-won: this env's network policy blocks direct fetches of ees-ops.netlify.app (proxy 403) — verify deploys via the Netlify API (current deploy `commit_ref` + state), not curl.

**Shipped 2026-07-26 — Related-record create inheritance + property-scoped opportunity pickers (branch `claude/enrollment-data-inheritance-92nr9k`; DB live on prod):**
Nicholas's ruling: creating a child from a parent must inherit everything the system knows, and child lookups must respect the parent chain — "no way possible an incentive application can be on an opportunity that's not on this property."
- **Root cause of the dead Record Type dropdown on /enrollments/new:** `enrollments` (plus a dozen other tables) was missing from `TABLE_COLUMN_PREFIX` in `layoutService.js`, so `getRecordTypeColumn` fell back to a non-existent `record_type` column (the picker's choice was seeded into a bogus key) and `fetchPicklistOptions` never stripped the `enrollment_` prefix to find the picklist rows. Map now covers every table with a prefixed record-type column; `diagnostic_tests` was also corrected `'dt'` → `'diagnostic'` (dt is document_templates' short prefix). `applyInsertDefaults` gained `enrollments`.
- **Property → Enrollment field inheritance** in `RelatedListWidget.handleNewClick`: new enrollment from a property pre-fills all HUD property/site/owner/management/contract fields, derives subsidized-share % from unit counts, seeds `enrollment_state` (which state-filters the record-type picker), and composes the name "<property> - <RT label>" via the derived-name mechanism (recompose-on-RT-change generalized from projects-only to any table with a nameColumn). `enrollments` added to `TABLE_META`.
- **Property-scoped opportunity pickers** (migration `20260726120000`): new dependent-lookup kind `opportunities_for_property` + SECURITY INVOKER RPC `list_opportunities_for_property(p_property_ids, p_include_opportunity_id)`; applied to every `opportunity_id` lookup on enrollments (7 layouts), projects (7), work_orders (7 — the 2 WO layouts without a property_id field were deliberately left unscoped so their pickers don't dead-end). Also fixed: dependent lookups no longer fall back to *unscoped* server search when the user types (the search box was leaking the whole table past the scope — affected contacts_for_accounts etc. too), and the empty-state hint now distinguishes "fill property first" from "property has no opportunities."
- Help article **HA-00141** (`creating-related-records-data-inheritance`). (Shipped: PR #207, merged to master same-day.)
- **Admin build indicator (follow-up, same day):** bottom-left of the sidebar shows the active build for Admin-role users only — "#PR · sha · UTC build time" (`SidebarBuildTag` in `UI.jsx`; `__BUILD_PR__` extracted at build time in `vite.config.js` from the squash-merge commit subject, alongside the existing `__BUILD_ID__`/`__BUILD_SHA__` the LEAP Pad footer uses). Hidden when the sidebar is collapsed and for non-admins.
- **Inline "+ New" on every lookup, system-wide (follow-up, same day — Nicholas: "always allow a create new record when searching related records"):** `allow_inline_create` flipped from opt-in to **on by default** for all lookup fields (`allow_inline_create: false` opts out; hard exclusion `QUICK_CREATE_EXCLUDED_TABLES` = users/portal_users — identity objects are Admin-provisioned). `QuickCreateModal` upgraded to carry it: required FK columns render as real searchable lookups (resolved via TABLE_META parents, not raw UUID text boxes), and when the create is seeded with a property, its building/opportunity pickers ride the scoped RPCs (`buildings_for_property` / `opportunities_for_property`) with local-only filtering so the parent chain stays intact inside quick-create too. `LookupEditControl` singularization fixed (properties → Property, not Propertie). HA-00141 extended.

**Shipped 2026-07-26 — Related-record (multi-hop) related lists + rebuilt Add Related List UX (branch `claude/related-list-field-selection-jeb2f0`, PRs #212 + follow-up; DB live on prod):**
Nicholas wanted a Units related list on the Property page — impossible before, because related lists required a direct FK and units link to buildings, not properties. Then he ruled the first cut (raw table dropdown + manual column picking) unacceptable UX ("this can't be a programming task"). Final state:
- **Add Related List modal is a drill-down object browser**: top level = the object's direct children (from `describe_object_incoming_fks`, now cached in `adminService`); a `›` on each row lazily drills into THAT object's children (depth cap 3), so any descendant is one click ("Units" under "Buildings", badge "via Buildings"). Search box; join-path summary; multiple FKs to the same parent become separate rows labeled with the FK.
- **Selection auto-fills everything**: title (humanized table name, stops once hand-edited), default columns (Record # / name / `*_status` / `*_record_type` where present), sort (record number asc). A **Column Order** panel (↑/↓/×) controls left-to-right order — `config.columns` order is what the renderer draws; the checkbox list below adds/removes.
- **Config shape**: direct child unchanged `{table, fk, columns…}`; deeper = `config.via = [{table, fk}, …]` ordered target-side first (each fk points at the NEXT entry; last fk points at the layout object; `config.fk` = target's FK to the first intermediate). `fetchRelatedRecords` resolves the chain as nested PostgREST inner-join embeds (`_v1:<fk>!inner(_v2:…)` + `.eq('_v1._v2.<fk>', parentId)`), one round trip, RLS-respecting, count-accurate; `_v1` stripped from rows; legacy single-object `via` normalized. Via lists hide the New button (child's FK points at the intermediate — create from the intermediate's page); rows navigate normally. `validate_page_layout_widget_config` needed no change (it checks `fk` exists on the target table, not where it points).
- The interim two-hop RPC `describe_object_grandchild_fks` (migration `20260726150000`) was superseded by recursive one-hop discovery and dropped same-day (`20260726180000`).
- Also fixed in the modal: soft-delete filter column detected from the target's real columns (`unit_is_deleted` etc. — the old blanket `'is_deleted'` default 400'd fetches on prefix-audited tables); stale display/sort picks dropped when the target changes. Help article **HA-00143** (`related-lists-via-related-records`). Live-verified by Nicholas: Units card on PROP-23587 showing all 17 units.

**Shipped 2026-07-26 — Duplicate checking on create + account cleanup reports (migrations `20260726090000`/`20260726100000`, applied to prod; HA-00140):**
- **Create-time duplicate warning (accounts/properties/buildings):** purpose-built SECURITY INVOKER RPC `find_duplicate_candidates(p_object, p_name, p_street, ...)` — accounts match on `normalize_duplicate_match_name()` (punctuation + entity-suffix stripped) plus trgm similarity ≥0.55; properties on normalized street+zip5/city (existing `normalize_street_address`) plus name; buildings on number/name within the parent property. Debounced probe in `RecordDetail` create mode AND `QuickCreateModal` (lookup-inline create) renders `DuplicateCheckPanel` (blue banner, `RecordLink`s to matches); **soft gate** — first Save press pauses with a warning, second press creates anyway (documentation never blocked). Trgm indexes on `lower(account_name)`/`lower(property_name)` + btree on the normalized account name.
- **Report rows now hyperlink + inline-edit:** `TabularLayout` first column renders a real `RecordLink` to `/{primaryObject}/{id}`; picklist cells on the primary object are double-click editable (`ReportPicklistCellEditor` → `bulk_update_records`, local overlay, no re-run). Fixed `loadPicklistLabels` to query both full-column and prefix-stripped `picklist_field` spellings (accounts rows use `record_type`, properties use `property_status` — `account_record_type` labels never resolved before; `guessPrefix` now exported from `fieldMetadataService`).
- **RPT-00039 "Account Near-Miss Duplicate Names Report"** — scoped via cross filter on purpose-built flag table `account_near_miss_flags` (FK → accounts; cross filters require a real table, views break `describe_object_columns`), rebuilt by `refresh_account_near_miss_flags()` (144 groups / 294 accounts at ship). **RPT-00040 "Accounts Missing Record Type Report"** — `account_record_type is_null` (795 active at ship; 780 from the 06-29 HUD import, mostly management companies). Both tabular on `accounts`, sorted by name, Record Type editable inline.

**Shipped 2026-07-25 — Account dedupe: one account per company (migration `20260725120000`, applied to prod):**
Ruling (Nicholas): a company is ONE account regardless of role — never a second record just to carry the Property Management Company record type; the **Property Owner account is primary** and `properties.property_management_company_id` may point at it (the lookup is not record-type filtered, so no UI change was needed). Root cause: the HUD imports (06-26 owner + management-company passes, 06-29 owner + untyped passes) created one account per role with no cross-type name matching — e.g. "CDA OF THE CITY OF MADISON WIS" existed as ACC-02217 (owner) + ACC-03162 (mgmt co). Merged **981 duplicates into 937 survivors** (survivor = property_owner record type, then any type, then most-linked/oldest): repointed all account FKs (21 `property_account_id`, 1,490 `property_management_company_id`, zero rows on every other referencing table) + polymorphic `activity_relations`; carried loser contact/address/HUD-number data onto null survivor fields (HUD number moved after soft-delete — partial unique index covers active rows only); untyped survivors owning ≥1 property set to Property Owner; losers soft-deleted with reason naming the survivor; all 981 pairs logged in `account_merge_log`. Verified: 0 active duplicate-name groups remain, no FK points at a deleted account, opportunity accounts still match their property accounts. **Any future import must match accounts by normalized name across record types before inserting.**

**Shipped 2026-07-14 — Field-created work orders: Create Work Order menu, ad hoc explicit selection, Field Data Verification Review (PRs #128–#133, live on master/prod):**
- **Technician-creatable work types (data-driven family):** `work_types.work_type_is_technician_creatable` + `work_type_default_work_order_record_type` columns; `create_technician_work_order(p_source_work_order_id, p_work_type_id)` clones property/building/unit/project/account from a scheduled stop's WO. **WT-00080 "Building Access - Unlock and Lock"** is the first (5 chain-of-custody steps: Key Checkout Photo + "Checked Out From" `key_source` field [Lockbox OR Person = account contact, "Someone else" free text], Technicians On-Site `user_multiselect` [all active users incl. Project Site Leads], Door Unlocked/Locked photos, Key Check-In + "Returned To"). New `work_step_template_fields` field types `user_multiselect` + `key_source`. Help article HA-00124. Coming next from Nicholas (pure config once step lists arrive): Post Notice of Entry (**one WO per unit — WOs never span units**), Incident Report, Damaged Equipment. Material Delivery (WT-00084) and the Monthly Vehicle Equipment and Documents Check (WT-00093) are built.
- **Create Work Order menu in LEAP Pad** (home-screen sheet + per-WO button): type list → today's stops → **"Property is not in this list" ad hoc path where the technician explicitly selects everything** (Nicholas: no server assumptions): property search → building (auto-created from the street number ONLY when the property has zero buildings — building naming = street address number) → unit (required always; pick or type a new name) → project (pick, or **Create New Project** → Field Documentation project under the property's Field Operations opportunity, created if absent — both purpose-built record types, opportunity stage "Opportunity — Field Operations Open" via pvrta, never shared). **Documentation is never blocked** ("quit preventing users from completing work"). RPC `create_technician_work_order_for_property`; migrations `20260714080000/090000/100000`.
- **Field Data Verification Review** (Nicholas: real-time QA without losing in-the-moment documentation): when the ad hoc flow *creates* records, `_create_field_data_review_task` routes a High/due-today task (created-records list + WO link) to the first active **Project Coordinator** — currently **Priya Nair** — in-app notification only, no email; falls back to the creating technician with an explicit note if no PC exists. Picking existing records raises no review.
- **Vehicle Activities parked as its own future workstream** (Nicholas): company-level vehicle record + activity records (inspection / fuel / maintenance record types), NO project/building/unit — do not fold into work orders. See `leap-fleet.md` when building.

**Shipped 2026-07-13/14 — Work order status automation, measurement capture, verification routing, photo pipeline (PRs #114–#127, live on master/prod):**
- **Status automation:** WO stays Scheduled until real step activity — `mark_work_order_in_progress_from_activity` + triggers on `work_steps`/`photos`/`documents` flip New/Scheduled → In Progress on first evidence (verified live by Nicholas on WO-00056). WO → To Be Verified fires `create_work_order_review_task`: review task + in-app notification (NO email) to `project_coordinator_id` (fallback WO owner) — verified live on WO-00058.
- **Measurement capture (hard-gated):** `work_step_template_fields` 'number' type + `save_work_step_field_value` RPC + `_work_step_evidence_gap` fields branch; "Square Feet of Attic Insulation 100% Removed" on WPT-00004 (now 22 steps, WST-00050 at position 20). Work orders cut into ~5,000 sq ft / 8-hour blocks; future: read attic sq ft off the building record (parked).
- **Photo pipeline quality (Nicholas's bar: keep ALL EXIF, GPS + timestamp most important, tag with work step name):** `process-photo` v7 watermarks with the work step name using a **self-hosted font** (`templates` bucket `fonts/watermark-font.ttf` — the old GitHub raw Inter URL 404'd on his first real photo); GPS-missing warning is **async** (screen updates instantly, warning follows) and structured as first-photo-per-WO check. `work-evidence` bucket allows `video/mp4|quicktime|webm|3gpp`.
- **Real chain at 1837 Alden Rd, Janesville (PROP-23587):** BLD-00075 "1837" (building naming = street number), 16 units (Attic, Mechanical Room, Office, Laundry Room, hallway x2, Units 1–11), OPP-00066/67, PROJ-00025, **WO-00056** insulation removal (22 steps, In Progress) + **WO-00058** Machine & Dumpster Setup (WT-00079, 3 steps, To Be Verified → TSK-01007 to Brittin). WO-00055 test chain from PROP-23706 soft-deleted per Nicholas.
- **Reviewer routing note:** Priya Nair is the only active Project Coordinator (Brittin Wood is Admin) — both review-task automations route accordingly; reassigning is a role change in LEAP Admin.

**Shipped 2026-07-13 — Insulation Removal - Attic work plan + video evidence + step-level Not Applicable (branch `claude/insulation-removal-work-order-yqr75j`; DB live on prod):**
The first work plan built to spec by Nicholas, plus two generic work-step capabilities:
- **Work plan template WPT-00004 "Insulation Removal - Attic - Standard"** — 21 purpose-built step templates (WST-00027–00047, never shared with another plan): start-of-day building/hatch/containment photos + 5 existing-attic photos + attic 360 video → timed checkpoints at 10 AM / 12 PM / 2 PM (containment photo + Square Feet Removed measurement field each) → end-of-day insulation-removed photo sets (pipes / wires / top plates / exhaust fans / light boxes-can lights / eaves wide angle, 4 each) + closing 360 video → hatch closed & cleanup photo → closing building number photo. Every step owned by **Lead Technician**, verified by **Project Site Lead** (there is no "Field Technician" role). Wired as WT-00041's default template: 480 min / 8 hr, 0.096 min per sq ft (5,000 sq ft per crew-day basis).
- **Video evidence type (new, hard-gated):** picklist value on `wst_required_evidence_type_id`; `_work_step_evidence_gap` blocks step completion until a `documents` row with `mime_type video/%` exists on the step ("360 video" = full-circle phone pan, per Nicholas — no special camera). LEAP Pad: Record Video button + inline playback (`VideoStrip`); step-scoped documents now bucket-route to `work-evidence` (`storageService`).
- **Step-level Not Applicable with required reason** (Nicholas's chosen flow for e.g. "no can lights present"): status reactivated, `work_steps.work_step_not_applicable_reason` column, `mark_work_step_not_applicable(p_step_id, p_reason)` RPC, N/A modal in LEAP Pad; `rollup_work_plan_status` excludes N/A steps from the applicable totals. N/A ≠ Unable to Complete — Unable stays reserved for real blockers and still forces the plan Unable. `work_order_detail_for_technician` now returns `video_count` / `videos` / `not_applicable_reason` per step. Migration `20260713113959_work_step_video_evidence_and_not_applicable.sql` (in repo + applied to prod). Advisor baseline still ~174 (the new RPC carries the standard authenticated-executable lint like every technician RPC).
- **First work order in LEAP: WO-00055** on seed property PROP-23706 (new Building 1 → opportunity on WI-IRA-MF-HOMES Phase 6 → project "Insulation Removal - Attic - Seed Property", MF-INS-AIR record type) — the `instantiate_work_plan` trigger auto-created **WP-00007** with all 21 steps; photo + both video evidence gates verified live (correct blocking messages). Whole chain `is_seed_data=true`.
- Also fixed: `statusChip` matched 'complete' before 'unable', so "Unable to Complete" chips rendered green. Help article **HA-00123** (`insulation-removal-attic-work-plan`).
- **SA-00142** schedules WO-00055 for Nicholas (2026-07-13, 8 AM–4 PM Chicago) so LEAP Pad's Today view shows it; WO moved to Scheduled.
- **Customer notification scope (Nicholas, 2026-07-13): the SA notification pipeline (booking confirmation, 48h/24h/morning-of reminders, on-my-way/arrived/completed/canceled/rescheduled) exists FOR ASSESSMENT SCHEDULING and must keep working. Customers should not be emailed about install/removal work orders — in practice those SAs carry no `contact_id`, which is the pipeline's existing gate.** Cautionary tale: migration `20260713121244` tore out the whole pipeline on a misread of "no emails for work orders"; `20260713121827` restored it verbatim from the baseline within ~10 minutes (verified: 2 triggers, cron `*/5`, 4 functions, 14 active templates; zero customer-attached SAs existed during the gap, nothing was missed). **Do not touch the SA notification pipeline when working on work orders/work plans.** Advisor baseline back at ~174.

**Shipped 2026-07-07 — Page layout section + related-list customization (PRs #99/#100/#101/#103, live on master/prod, tested by Nicholas):**
`LayoutCanvasEditor` full section/related-list control, no schema change (persists through the existing bulk soft-delete + recreate adapter):
- **Sections:** drag-reorderable (⠿ handle; array order → `section_order`, which also drives Related-tab card order); **Tab select** (Details/Related, custom values preserved) per section; "+ Add Section" buttons at BOTH top (prepends) and bottom (appends) of the canvas.
- **Card widgets** (related lists, file galleries, conversation panels, reports, prtsn history) are first-class tiles: inline title rename (title = the Related-tab card heading), drag within/across sections, remove, and "+ Add Related List" per section → `RelatedListCanvasModal` (canvas-local; target table from `describe_object_incoming_fks`, FK, display columns, sort). Column entries emitted in the live renderer's shape: `{name,type,label}`, picklist FKs → `picklist`, other FKs → `lookup` with `fk_column`/`lookup_table`/`lookup_field` resolved from the referenced table's first `*_name` column. Tiles labeled "· Related tab" since cards always render there regardless of section tab.
- **Renderer consistency (RecordDetail `Section`):** blank sections now RENDER (header + muted empty state — "cards appear on the Related tab" / "no fields yet") instead of vanishing, so record pages match the editor 1:1. Exception preserved: sections whose widgets are all suppressed via `hiddenWidgetTypes` (docx-only widgets on document templates) still hide.
- One shared DndContext drives all three drag families (`sec::`/`wgt::`+`wzone::`/field names) with family-filtered collision detection (`dragFamily` in `LayoutCanvasEditor`).
- Help article **HA-00122** (`page-layout-sections-related-lists`). The four orphaned pre-canvas widget modals (`AddWidgetModal`, `WidgetEditorRelatedList`, `WidgetEditorFieldGroup`, `WidgetEditorConversationPanel`) were deleted in PR #109; `eesFieldTypes.js` remains (used by `RelatedListCanvasModal`).

**Shipped 2026-07-06 — Security & efficiency hardening + related-list counts (live on master/prod):**
Whole-platform security + efficiency review (PR #11/#98 + follow-ups on `master`).
- **DB (live):** closed an unauthenticated bulk-data leak on `outreach_properties_v` — a July perf migration had dropped the baseline's `security_invoker`, re-exposing ~17.5k property rows (owner names, HUD emails/phones, LIHTC financials) to any holder of the public anon key. Fixed by **revoking `anon`** (the actual leak) and restoring `security_invoker=on`; then **reverted to `security_definer`** because the Enrollment/Outreach Properties list (loads all ~17.5k rows through the 4-join view) hit `statement_timeout` under invoker-RLS. Anon stays revoked, so the leak is closed regardless of mode. `execute_flow` client EXECUTE revoked; `property_hud_match_review` `USING(true)` policies scoped to admin.
- **Edge functions (deployed + verified):** `admin-reset-user-password` verifies the JWT via `getUser()` (was unsigned `atob`); `send-notification-email` requires the service-role key (was an unauthenticated open Graph relay); `signing-portal-submit` builds the next-signer link from `APP_BASE_URL` (was request `Origin`/`Referer`); `dispatch-scheduled-reports` gated on a **keyless shared secret** in `internal_cron_auth` sent by its pg_cron job via `x-internal-cron-secret` (cron re-registered via `cron.schedule`).
- **Frontend (live):** DOMPurify on inbound-email HTML (`ActivityTimeline`, `UnmatchedInboxPane`); help-markdown URL-scheme allowlist; security headers in `netlify.toml`; `xlsx`/`jspdf` lazy-loaded off the record-open path; `describeObject` memoized; **related-list widgets show a subtle total count** (`count:'exact'` via `fetchRelatedRecords._total`, rendered in `RelatedListWidget` header).
- **Ops:** org over-quota traced to a **separate project (HPR / Heat Pump Ranch, ~104 GB of product-media PDFs)**, not LEAP (~54 MB) — resolved by Nicholas. That throttling was the main page-slowness driver (`count(*)` on the view went 4.1s → 1.2s after the fix). The view's FK join keys are already indexed.

**Deferred (next UI/perf session):** server-side pagination for the big list views (Properties etc. still load all rows) + optional DB compute-tier bump for snappier loads; then restore `security_invoker=on` on `outreach_properties_v` once the list is paginated. Also still open from the review: financial-tier server-side enforcement, strict CSP, `xlsx@0.18.5` upgrade, `verify_jwt` pinning in `config.toml`, remaining `atob`→`getUser` in `ai-assistant`/`flow-*`.

**Shipped to production (master):**
- **Phase 0 — Foundation:** `react-grid-layout` + `@dnd-kit/*` (isolated vendor chunks, verified no TDZ cycle); `src/builder/` — geometry model (`{x,y,w,h}` 12-col), per-surface component registries, three-pane `LeapCanvas`.
- **Phase 1 — Dashboards:** `DashboardCanvasEditor` replaced the old `DashboardEditor` everywhere; live report-data previews; geometry + Title/Subtitle/Footer chrome persisted in `dw_widget_config`; `DashboardRunner` renders by geometry (view == build) with legacy fallback.
- **Phase 2 — Home pages:** canvas generalized into a per-surface engine (registry object); `HomePageCanvasEditor` replaced `HomePageBuilder`; geometry in `hpc_config._geometry`; `ConfiguredHome` honors geometry with legacy fallback.
- **Phase 3 — Reports:** drag-and-drop field selection; formula engine extended (38 functions) + visual calc-field editor (insert field/function pickers + Check syntax); **live preview from unsaved config** (`runReport` split into a loader + `runReportDefinition`; `buildReportDefinition` mirrors `saveReport`). NOTE: deliberately extended `lib/reportFormulaEval` instead of swapping to mathjs/formulajs (would break existing formulas — see handoff doc).
- Legacy `DashboardEditor.jsx`, `HomePageBuilder.jsx`, and the Phase 0 `BuilderStudio` preview deleted.
- **Help articles (prod):** HA-00113/114/115 for the new builders.

**Phase 4 (record page layouts) — re-platform DECLINED:** the existing `LayoutEditor`/`LayoutCanvas` already IS a working WYSIWYG drag canvas, so folding it onto the shared engine is high risk / low user-value on a core feature. The WYSIWYG goal is met across all four surfaces. The rearchitecture is functionally complete; the unified fold remains an optional, staging-soaked follow-up. See handoff doc.

**Shipped 2026-06-29 (PR #11, live on master/prod):**
- Dashboard filter columns now override a report's own saved filter on the same column (`reportsService.runReport`/`runWidgetAggregate` `overrideFields` param; `DashboardRunner` passes its filter columns). Fixed Outreach Dashboard STATE filter — "All" = all states (10,964), non-NC states filter correctly, NC stays the default.
- Enrollment module (`OutreachModule.jsx`, route `/m/enrollment`) home now resolves to HP-00005 Enrollment Home / DSH-00009 Enrollment Overview instead of the Outreach dashboard (was wrongly `moduleId="outreach"`).

**Migration baseline / dev workflow (2026-06-28):** the migration history was squashed into a single verified baseline so Supabase branching (isolated sandbox DBs) works. `supabase/migrations/` now contains only `20260412000000_leap_baseline_schema.sql` (generated from live prod, fingerprint-verified against production on a throwaway branch — every table/column/function/policy/constraint identical). The 190 prior files live in `supabase/migrations_archive_pre_baseline/` (reference only, never replayed). Production's 870-row migration registry was replaced with the single baseline row; the full history is backed up in `supabase_migrations.schema_migrations_backup_20260628`. Going forward, every schema change is a NEW migration file added after the baseline. Custom RLS roles (`internal_staff`, `external_partner`, `customer`) are created at the top of the baseline. See `docs/leap-dev-workflow.md`.

**Done and shipped to master:**
- Two purpose-built reports: **RPT-00036** Outreach Status Report (primary object `properties`, NC state runtime filter) and **RPT-00037** Outreach Pipeline by Stage Report (primary object `opportunities`, NC filter).
- **DSH-00010 Outreach Dashboard** — 5 widgets: Total Properties (metric) + By Status (bar) on row 0; By County (bar, top 20) + By Property Owner (bar, top 20) on row 1; Pipeline by Stage (funnel) on row 2. Cascading state filter on `property_state`, default NC.
- State filter converted from hardcoded text input to a data-driven dropdown: new RPC `dashboard_filter_distinct_values` (security invoker, RLS-respecting), `dfilt_options` descriptor `{source:'distinct', object:'properties', field:'property_state'}`, `fetchFilterOptions` helper in `reportsService.js`, `DashboardRunner.jsx` renders `<select>` when options exist.
- `OutreachModule.jsx` corrected from `moduleId="enrollment"` to `moduleId="outreach"` (`crumb="Outreach"`) — now resolves to home page **HP-00006** with its DSH-00010 dashboard component. (Opportunities was already in the Outreach nav — `CODE_SECTIONS` `{id:'opps'}` — no change needed.)
- Embedded-dashboard **Edit** button rewired (`ConfiguredHome` → `HomeComponentRenderer` → `EmbeddedDashboard` → `DashboardRunner.onEdit`) to open the full `DashboardEditor` (Salesforce-style builder) full-screen, home remounts after save.
- `HomePageBuilder.jsx` got pre-save validation + `friendlySaveError()` translating raw Postgres errors into plain messages. HP-00006 component title corrected to "Outreach Dashboard."

**Architecture facts confirmed:** Dashboards (`dashboards` + `dashboard_widgets`, rendered by `DashboardRunner`) and Home Pages (`home_pages` + `home_page_components`, rendered by `HomeComponentRenderer`) are parallel subsystems. A home page component with `hpc_type='dashboard'` and `hpc_source_id` = a dashboard UUID renders `DashboardRunner` directly. Home page resolves per module via `resolve_home_page_for_module` RPC. `report_aggregate` RPC returns `{label, value, raw_value}`, verified NC-scoped for County (20 groups), Owner (20 groups), Pipeline by Stage (1 group). Note: `report_groupings.rgr_field_via_path` documented shape `{from,fk,to}` does NOT match what `getRowValue` in the live runner expects (bare FK string arrays like `["property_id"]`) — verify against the runner before authoring related-field groupings.

**Shipped 2026-07-05 — Activity + email layer (PRs #57–61, #84–90, live on prod):**
Salesforce-parity activity logging (Log Activity composer, type picklist, multi-relate `activity_relations` junction with rollup) + fully intelligent two-way email: real Graph sends from shared state mailboxes, per-thread conversations, cross-object merge fields, attachments on the actual email, hidden reply-to token → replies auto-thread in ~60s, 6h subscription auto-renew, NC DKIM signed. Autonomous self-test harness `admin-test-send-email` (send/reply_sim/inspect) — **run it after any email-pipeline change**. Full handoff, per-state rollout playbook, and follow-up list: `docs/leap-activity-email-layer.md`. Help articles HA-00118/119.

**Shipped 2026-07-06 — Email layer round 2 (PRs #92–95, live on prod):**
WI email fully live: purpose-aware mailbox routing (`obm_purpose` — General Correspondence vs Assessments; WI correspondence → `ira@ees-wi.org`, assessments boxes never picked), Graph subscription + ees-wi.org DKIM enabled, full self-test loop verified (send/attachment/merge/reply-thread, seed record PROP-23706). Attachment virus-scan layer (`scan-message-attachments`, 5-min cron — EICAR/executable-content/extension/spoof checks; blocked files undownloadable; HA-00120). Inline email replies in the conversation panel (stale "not supported" notice removed). Program signatures (`obm_signature_html`, appended by send-email-v1 v13 on every send incl. replies; seeded NC + WI). Note: the scanner + renewal crons and the shared pipeline secret are prod-only config, deliberately NOT in repo migrations.

**Shipped 2026-07-07 — Property Owner Research tool (branch `claude/property-owner-research-tool-7r7kzk`; schema + edge fn live on prod):**
Finds decision makers (CEO/owner/president, asset manager, facilities director — NOT site property-management staff) for owner groups (accounts) and specific properties. Tiered by cost: **free AI web research** (Claude Opus + web search over the org's domain, leadership pages, parent companies, registries — runs as an edge-function background task, client polls) → **Lusha prospecting search** (no credits; names/titles + has-email/phone flags) → **Lusha enrich** (paid credits, per explicitly selected person only, confirm dialog). New objects `owner_research_requests` (ORQ-) / `owner_research_candidates` (ORC-) with full LEAP conventions (record numbers, audit, soft-delete, RLS via `app_user_can`, role access mirrors `accounts` minus portal roles); target job titles are the admin-manageable picklist `orq_target_job_title`. Edge fn `property-owner-research` (v7; auth = JWT→app user, plus the shared-pipeline-secret self-test gate à la `admin-test-send-email`); LUSHA_API_KEY lives in Supabase Vault, read via service-role-only `get_integration_secret()`. Hard-won edge-fn facts: a research turn blows the platform's **150s request idle timeout** → web research runs via `EdgeRuntime.waitUntil` + client polling, and must also fit the **400s worker wall clock** → time-boxed (5 searches, `effort: low`, explicit speed instruction); stale Submitted runs >8 min are auto-failed on the next call; Lusha **enrich** nests contact data under `data` (`emailAddresses[].email`, `phoneNumbers[].number`, `socialLinks.linkedin`) — not top-level. UI: `PropertyOwnerResearchPanel` on the Related tab of accounts + properties (`src/data/ownerResearchService.js`); candidates promote to Contacts (CT-) or dismiss; manual Google/LinkedIn/state-registry shortcut links per state. All three tiers live-verified against Westminster Company: ORQ-00001 Lusha search (4 candidates), ORQ-00005 web research (ORC-00005 Leah Lyerly, EVP & Founder, 2 evidence links), enrich on ORC-00001 (Jane Henderson VP → work email A+ confidence, phone flagged doNotCall, LinkedIn; 1 credit spent). Help article HA-00121.

**Shipped 2026-07-09 — Owner research workflow Phases 1+2 (branch `claude/property-owner-research-tool-7r7kzk`; schema + edge fn v14 live on prod):**
Deep research is now a staged pipeline and findings flow through a review queue into real CRM records. Edge fn `property-owner-research` v14: new `deep_research` action walks Owner Identification → Organization Research → Decision Maker Discovery (web pass + inline credit-free Lusha search, merged by normalized person name; Lusha failure never sinks the stage) → Contact Info Gathering; each stage is its own invocation (fresh 400s budget, effort `medium`, 4 searches/3 fetches/6k-token fetch cap per stage), chained by self-invocation to the function's own URL (anon-key JWT for the gateway + `x-pipeline-test-secret` + `on_behalf_of_user_id`); every stage's output persists in `orq_stage_results`; stale sweep generalized per-stage on `orq_stage_started_at`; `run_stage` action retries one stage; runs end "Research Request Ready for Review" (org identification alone counts). Migrations: `20260708120000_owner_research_workflow_v2.sql` (orq_stage/_results/_started_at, `orq_org_approval_status`, `orq_approved_account_id`, `orc_rejected_reason`, new statuses/stages picklists) + `20260708123000_outreach_owner_research_section_seed.sql`. Review queue `src/components/OwnerResearchQueue.jsx` at Outreach → **Owner Research** (`OutreachPropertiesModule` section `research`): approve identified org → `findAccountMatches` (normalized name/domain, reviewer chooses, never auto-merged) → link or create account (reviewer edits the name) → optional property-repoint checkbox → pending candidates re-linked to the real account; approve person → edit-before-save modal → Contact created (`promoteCandidateToContact` now takes overrides + accountId); reject with reason; bulk approve. Panel button is now **Run Deep Research** with live stage progress (`waitForRequestCompletion` treats Submitted/In Progress as in-flight, 20-min default timeout, `onProgress`). Live-verified on the Wilmington NC placeholder-owner property, ORQ-00015: identified **JES Holdings, LLC** (jesholdings.com) + corporate structure and returned 13 candidates (1 evidence-backed Founder/CEO + 12 enrichable Lusha execs) in ~6.5 min — **deliberately left pending in the queue as real data for Nicholas's review-flow test**. HA-00121 rewritten for the workflow. Phases 3 (batch research) + 4 (outreach handoff) remain planned: `docs/leap-owner-research-workflow.md`.

**Shipped 2026-07-09 (round 2, after Nicholas's live review-flow test — PR #113):**
- **Rule: opportunity account always equals its property's account** (Nicholas: "no way possible an opportunity can't be on the same account as the property"). DB-level triggers (`20260709130000_opportunity_account_follows_property.sql`): BEFORE INSERT/UPDATE on `opportunities` forces `opportunity_account_id` to the property's account; AFTER UPDATE of `properties.property_account_id` cascades to its opportunities. Both SECURITY DEFINER (REVOKE anon/authenticated/PUBLIC) so RLS can't silently skip the sync; 4 diverged rows backfilled (incl. OPP-00046 → JES Holdings); live-verified the trigger snaps a forced divergence back.
- **Clean account names + hierarchy from research.** Edge fn v15: stage 1 must return the clean registered legal name (context → identification_notes); stage 2 output gains `subsidiaries[]` (clean names) and its verified `official_name` overwrites `orq_company_name`. Approval writes the research narrative to `account_notes` (never the name); `buildRelatedOrgOptions` + Corporate Structure checkboxes in the approve dialog create/link parent (→ `parent_account_id` of the approved account), subsidiaries (children), and management orgs (standalone) — existing accounts matched by normalized name, never duplicated, hierarchy links only set where currently null.
- **Related Accounts related list** (child accounts via `parent_account_id`) added to the Related Records section of all 8 account page layouts (`20260709133000_account_related_accounts_widget.sql`).
- Data fixes on prod: ACC-07572 renamed "JES Holdings, LLC" (context → notes); pre-existing ACC-04709 "Fairway Management, Inc." (HUD import) linked as its child — the JES hierarchy is Nicholas's live example.
- Advisor note: the two new trigger functions initially added 4 `*_security_definer_function_executable` lints; fixed with explicit REVOKEs (also in the migration file). Baseline count is now ~174.

**Open / on the horizon (confirm with Nicholas before acting):**
- **Program submittal documents — SHIPPED 2026-07-26** (`docs/leap-project-paperwork-port.md`): **there is no generic "paperwork."** Every program runs its own incentive application, and every incentive application has the same three stages, often months apart — **Income Qualification Application** (stage 3; already built as `run_income_qualification` on **enrollments**, never duplicated), **Project Reservation** (stage 6), **Final Project Payment Request** (stage 11). It is a matrix, not a list: a property runs several programs at once and each carries all three stages — which is why WI-IRA-MF-HOMES and WI-IRA-MF-HOMES-**AUDIT** are separate `incentive_applications` record types with parallel opportunity stage sets. **The audit is its own program**, so the Energy Audit Invoice is the AUDIT program's Final Project Payment Request document. Two explicit project actions (`generate_project_reservation_submittal`, `generate_final_payment_request_submittal`) open `ProjectSubmittalDocumentsModal` scoped to that one submittal. The program × stage → document matrix lives in **`src/data/paperworkSubmittals.js`** (all 8 programs declared; the 6 without built documents carry empty sets so gaps are visible). Math in `paperworkModel.js` (pure, node-tested against the Hampton fixture: $87,150/$7,150/$80,000/TOTAL DUE $0.00, audit invoice one page); record prefill in `paperworkService.js`; workbook template at `public/paperwork/`. Help article **HA-00144**.
  - **2026-07-27 — keyed on opportunity record type + wording in the database:** the matrix is now keyed by **opportunity record type** (the complete state-scoped program axis: 26 active values across WI/NC/MI incl. FOE by year — `incentive_applications` record types are Wisconsin-only, so they dead-end outside WI). All 20 programs declared. New object **`submittal_document_text_blocks`** (SDTB-, migration `20260727100000`, applied to prod, advisors unchanged at 205) holds the document wording — measure descriptions, acknowledgments, proposal title, header/footer lines — with `{{baseline_r}}`/`{{improved_r}}` tokens; a row scoped to an opportunity record type overrides the global default for that program only. Seeded with the 11 current blocks, **MD5-verified byte-identical to the in-code fallbacks**. Editable at Setup → Communication Templates → **Submittal Document Wording**. Math and layout deliberately stay in code.
  - **Rejected after investigation (2026-07-27): rebuilding these as `document_templates`.** That system is flat-merge only (`buildMergeDict` emits one parent row + `today`/`template.name` — no arrays, no cross-object, no computed values), so the variable-length measure table cannot be expressed; and docx → mammoth → `htmlToPdf.ts` would degrade the gridded layout. Also note `document_templates.record_type`/`program_id`/`state`/`requires_signature`/`signer_role` are **declared but inert** — only `related_object` is read anywhere.
  - Open: **signing route for generated PDFs** — `envelopes.document_template_id` is NOT NULL and `send-envelope` renders from a template snapshot, so a generated PDF has no route into e-signature; the Final Project Payment Request invoice is the one document Nicholas wants signed. Also open: make each submittal a real record (`project_payment_requests` has a 9-status lifecycle; `project_reservations` is an empty shell) — see doc §9.
- **Work plans for the new technician-creatable work types** — Material Delivery (WT-00084) and the **Monthly Vehicle Equipment and Documents Check (WT-00093)** are built. Still awaiting step lists from Nicholas: Post Notice of Entry (one WO per unit), Incident Report, Damaged Equipment. Pure config: work type + plan template + `work_type_is_technician_creatable` + default record type.
- **Vehicle Activities workstream** (parked, see above) — build only when Nicholas calls for it.
- Desktop review experience for verifiers (PC/PSL) — approving submitted work orders/steps beyond the auto-created task; geolocation-based filtering of the ad hoc pickers (advanced, parked).
- **By Status widget disposition** — all NC properties currently have null `property_status`, so the widget may warrant soft-deletion. Confirm: keep or remove.
- Verify all four grouped widgets render correctly on the live site after the recent pushes.
- Email-layer follow-ups (see handoff doc §5): Message-ID reconciliation (tier-2 reply matching), CC/BCC in composer UI, multi-contact activities UI, commit out-of-band function sources (`outlook-oauth-*`, `create-graph-subscriptions`, `send-email-via-graph`) or retire the per-user Outlook path, contact dedupe (nwood3764@gmail.com on two contacts), remaining state mailboxes/DKIM (MI/CO/IN — playbook in handoff §4), real program signature content per mailbox (starter blocks seeded).
- Financial visibility tier gating (Tier 1/2/3) — hard blocker before external/portal users; `field_metadata` and `field_permissions` are currently empty.
- Null record types on real records across multi-type objects (`work_steps` 52, `buildings` 7, `contacts` 6, `work_orders` 5, `opportunities` 4, `projects` 3, `incentive_applications` 2) — resolve via Salesforce import.
- Docs cleanup: ✅ Done — the 19 `anura-`-prefixed files in `/docs/` were renamed to `leap-*`, "Anura" scrubbed from content (→ LEAP / EES), and internal cross-references fixed. The word now appears only in `leap-naming-standard.md` and one line of `leap-build-discipline.md`, where it is the deliberate definition of the forbidden-word rule.

---

## Detailed reference — `/docs/`

| Topic | File |
|---|---|
| Master project instructions | `LEAP-PROJECT-INSTRUCTIONS.md` |
| Property hierarchy | `leap-property-hierarchy.md` |
| Roles, field ops, asset accountability | `leap-roles-and-field-structure.md` |
| Program portfolio (5 states) | `leap-programs.md` |
| 12-stage project lifecycle | `leap-project-lifecycle.md` |
| Status lifecycles per object | `leap-status-lifecycles.md` |
| Work types, work plans, materials | `leap-work-types.md` |
| Vehicles and fleet | `leap-fleet.md` |
| Communications and templates | `leap-communications.md`, `leap-communications-module-1.md` |
| Admin Builders | `leap-admin-builders.md` |
| Builder rearchitecture (WYSIWYG) handoff | `leap-builder-rearchitecture.md` |
| Portals (owner and partner) | `leap-portals.md` |
| Field Mobile | `leap-field-mobile.md` |
| Reports and dashboards | `leap-reports.md` |
| Data standards, validation, retention | `leap-data-standards.md` |
| AI assistant | `leap-ai-spec.md` |
| Module list and build order | `leap-modules-and-build-order.md` |
| Schema session instructions | `leap-schema-session.md` |
| Development workflow (sandbox → prod) | `leap-dev-workflow.md` |
| Staging environment (full-data copy) | `leap-staging-environment.md` |
| Environment & promotion standard (prod-safe) | `leap-environment-promotion-standard.md` |
| HUD data sources | `leap-hud-data-sources.md` |
| Agent operating mode | `leap-agent-operating-mode.md` |
| Build discipline (folded into this file) | `leap-build-discipline.md` |
| Naming standard (folded into this file) | `leap-naming-standard.md` |
| Session log 2026-07-01 (nav/list/layout/property fixes + verification) | `leap-session-2026-07-01.md` |
| Activity + email layer (log activity, two-way Graph email, self-test harness) | `leap-activity-email-layer.md` |
| Owner research → outreach workflow (staged research, review queue, approval) | `leap-owner-research-workflow.md` |
| Project Paperwork port (HOMES invoices/proposals from the Audit Template Builder into LEAP) | `leap-project-paperwork-port.md` |
| Dashboard rehaul (enterprise-class builder: ECharts, 25+ components, query shapes, parity) | `leap-dashboard-rehaul.md` |
| Report Builder + viewer + dashboards enterprise rebuild (filters, sort, groupings, group formulas, cross-filter) | `leap-report-builder-enterprise.md` |
| Multifamily Energy Assessment WO (Asset Score + ASHRAE L2 field capture — build spec) | `leap-multifamily-energy-assessment.md` |
| Field type system (modifiable types, formula fields, roll-up summaries) | `leap-field-types.md` |
| Inherited fields & lookups (full-chain inheritance, stop duplicating parent data) | `leap-inherited-fields-lookups.md` |
| Service Provider portal + intake (subcontractor onboarding, payout book, proposals, AP) | `leap-service-provider-portal.md` |
| Geographic (state) record access — record-level security, per-user state grants | `leap-geographic-record-access.md` |
| Property Owner Portal (current state, repair log, Lane B build plan) | `leap-property-owner-portal.md` |
| Server-side list pagination (engine shipped, client plan, the nine full-row dependencies) | `leap-server-side-list-pagination.md` |
| Project record types → the opportunity's products (rename + mapping plan) | `leap-project-record-types.md` |
| Tasks + the signature request that lives on one (assignable work, task communications, send-a-form-for-signature) | `leap-tasks-and-signature-requests.md` |
| Equipment selection (NEEP/AHRI catalog, Manual J load capture, sizing engine, the auditor's picker) | `leap-equipment-selection.md` |
