// Set this to the deployed OSM Fixer service URL (the Flask/Docker service),
// e.g. "https://osm-fixer.fly.dev". Left blank, the page explains it needs
// configuring. Kept as a separate committed file so the URL is set per-deploy
// without editing the app.
window.OSM_FIXER_URL = "";
