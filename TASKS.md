# EES Platform — Task List

Live task list. Updated as each commit lands. **Maintained by Claude** as part
of every working session — every completed step gets reflected here before
the next one starts.

## Format

- `[x]` = done and committed (commit hash in trailing parens)
- `[~]` = in progress (uncommitted changes on disk)
- `[ ]` = not started
- `[blocked]` = blocked on external dependency

**Self-referential hashes:** A commit can't know its own hash in advance, so
when a TASKS.md update lands in the same commit as the work it describes,
the entry uses `(this commit)` as a placeholder. A follow-up chore commit
runs `git log -1 --format=%H` and substitutes the real hash.

## Standing protocols

- **Documentation discipline:** every shipped feature that affects user behavior gets a help article in the same session — anchored to the relevant route, object, or concept so the in-app HelpIcon surfaces it in context. Don't go crazy; only document where users would intuitively reach for help. Required pieces per article: `ha_title`, `ha_summary` (search-friendly), `ha_body_markdown`, and at least one `help_article_anchors` row.
- **Migrations: production-only.** No Supabase dev branches. Always verify with a SELECT after `apply_migration`. Use `apply_migration` for DDL, `execute_sql` for reads.
- **Communication:** one yes/no question at a time. Make a recommendation, ask for approval. No multiple choice.

---

## Up next (open backlog)

### Foundation — start importing real data
- [ ] **Data import: property hierarchy first** — start with Property Owners → Properties → Buildings → Units (via the `accounts` and property tables), then Accounts, Contacts, then Opportunities → Projects → Work Orders. Always import a parent before its children. Format of source export TBD — needs SF CSV/JSON from Nicholas. Production data verified safe to add: schema is stable; renames/type changes/backfills are mechanical (~5 min each); no work blocked by populated tables. Destructive column drops always require a `SELECT COUNT(*)` check-in first per the safety rule.

### Permissions (continuation)
- [ ] **Field-level enforcement on the frontend** — `field_visible` and `field_editable` resolver functions exist in the DB but `RecordDetail.jsx` and widgets don't call them. Today field-level security is queryable but not enforced in the UI. Multi-session backlog item; pairs with the Permission Builder Field Permissions tab UI.
- [blocked] **Row-scoping policies for external portal users** — Property Owner, Property Manager, Subcontractor Partner roles have zero `role_object_access` rows so they're fully locked out at the object level. Activate when subcontractor portal subdomain is built — each gets scoped row-level policies via the resolver functions already in place.

### Help system & documentation
- [ ] **Help articles for remaining shipped features** — ongoing protocol per standing rule. Outstanding: Recycle Bin (restore + purge workflow), Page Layouts / Object Manager (admin editing), Reports module (build, schedule, filters, groupings, calc fields), Dashboards (widgets + folder shares), Audit Log (what's tracked, search), Field History (which fields, viewing), Document/Email Templates (merge fields, publish/lock, snapshots), E-Signature / Envelopes (send, sign, void, lifecycle). Authoring opportunistically as we touch each feature; not a single-session push.

### Recycle Bin
- [ ] **Cascade rule per spec: parent restore restores children together** — investigated previously. Current schema has zero FK CASCADE on delete (all NO ACTION), no batch-id tracking. Implementing properly needs either (a) walking parent-child topology in a SECURITY DEFINER function with depth limits, or (b) adding `deletion_batch_id` on every soft-deletable table. Both sizeable. Deferring until soft-delete cascade pain is real.

### Scheduling Platform (FSL-equivalent customer-facing scheduling — multi-session build)
Replaces the current Jobber-based scheduling flow. Customer self-serve scheduling for residential energy assessments via slug-based public URLs on the existing ees-ops site. Schema slice (8 new tables, PostGIS, polygons, picklist lifecycle) shipped this session. Remaining slices below.

- [x] **Customer-schedulable work_types seeded** (`9057d98`, 2 migrations) — 4 customer-schedulable + 2 dispatcher-only work_types created as net-new rows. Customer-schedulable: Single-Family Energy Assessment (WT-00072, 90 min, slug `single-family-assessment`), Townhome Energy Assessment (WT-00073, 30 min, slug `townhome-assessment`), Multifamily Energy Assessment (WT-00074, 60 min × number_of_buildings, slug `multifamily-assessment`), Multifamily Diagnostic Assessment (WT-00075, 120 min, slug `multifamily-diagnostic`, 1/2 crew). Dispatcher-only: HVAC Quote (WT-00076, 45 min), Customer Consultation (WT-00077, 45 min). New column `work_types.work_type_default_project_record_type uuid → picklist_values(id)` so create-service-appointment edge fn reads the project record type directly from the work_type instead of slug→record-type mapping in code. Each customer-schedulable type gets its own URL `/sa/<slug>` and its own marketing funnel.
- [ ] **`compute-availability` edge function** — core slot-generation engine. Inputs: address + work_type slug + date range. Validates address via Google Address Validation API. Resolves containing service territory by polygon containment (PostGIS `ST_Contains`) with ZIP-list fallback. Filters qualifying Service Resources (Technician contacts with required `work_type_skill_requirements` satisfied via `contact_skills`). For each candidate slot: computes drive time from home base or previous appointment via Google Routes API (`compute-route-matrix` wrapper, cache-aware), enforces 15-min minimum buffer, validates 45-min lunch block fits within 11:30-13:00 window, applies day-fill preference for resource selection. Returns achievable slots only. Out-of-territory addresses return empty slot list + flag for dispatcher follow-up.
- [ ] **`create-service-appointment` edge function** — transactional cascade. One call creates Account (match by normalized address or create), Contact (match by email+phone within account or create), Project (new, record_type=work_type's appointment type, project_record_type picklist value), Service Appointment (new, scheduled time/territory/work_type), Work Order (new, from work_type's `work_type_default_work_plan_template_id`), Service Appointment Assignment (junction Contact↔SA for assigned auditor). Issues a Service Appointment Token for the customer's manage-appointment link. Fires appointment_confirmation notification template via Twilio + Graph.
- [ ] **`compute-route-matrix` edge function** — Google Routes API wrapper. Uses Compute Route Matrix endpoint with `routingPreference: TRAFFIC_AWARE`. Caches results in `drive_time_cache` keyed on SHA-256 hashes of origin/destination; 60-min TTL during business hours.
- [ ] **`validate-address` edge function** — Google Address Validation API wrapper. Returns canonical normalized address for Account dedup.
- [ ] **`send-sms` edge function** — Twilio wrapper. Writes to `notification_logs` with `nl_provider='twilio'` and Twilio SID. Blocked from production sending until A2P 10DLC registration completes (Nicholas's parallel workstream).
- [ ] **`send-email` edge function** — Microsoft Graph wrapper using application-permission Mail.Send. Sender mailbox derived from the appointment's Service Territory state (e.g. WI appointments send from `assessments.wi@EES-WI.org`, future MI from `assessments.mi@EES-WI.org`, etc.) rather than a single hardcoded sender. First mailbox confirmed by Nicholas: `assessments.wi@EES-WI.org`. Writes to `notification_logs` with `nl_provider='microsoft_graph'` and Graph message ID. Blocked on Azure AD app registration + Mail.Send permission grant on the mailbox.
- [ ] **`twilio-inbound` webhook** — receives Twilio SMS replies. v1 stores reply against `notification_logs`; v1.x surfaces in dispatcher inbox UI.
- [ ] **Customer scheduling pages** — one per customer-schedulable work_type, routes `/sa/<slug>` on the existing ees-ops site outside the auth shell. Lean intake form: first/last name, validated service address, phone, email, IRA HOMES program confirmation number, SMS consent checkbox. Multifamily variant includes "Number of buildings" field. Address validation before slot display. Slot list calls `compute-availability`. Confirmation page shows date/time/auditor + small "Manage appointment" link backed by single-use service appointment token. Custom Fields hook so admin can add per-work-type intake fields later without code changes.
- [ ] **Dispatcher console** — live map (all Service Resources' GPS pins color-coded by status, 30-60s refresh), schedule grid (Gantt-style; drag-and-drop reassign within skills constraints, drag-and-drop reschedule within rules), appointment management (search, filter, manual scheduling for dispatcher-only types and out-of-territory leads), resource management (skills, territory memberships, absences, home-base override).
- [ ] **Auditor PWA** — installable React PWA for Android crew phones. Day view (today's schedule, chronological). Tap-to-call/text/navigate per appointment. On-My-Way button (manual + 30-min ETA prompt + 15-min auto-fire backup). Arrived button (manual + GPS auto-fire). Unable-to-Complete flag. Microsoft SSO via Azure AD. GPS always-on as install condition. The Start Work Order action hands off to the existing audit workflow (out of this build).
- [ ] **Notification templates seed** — defaults for nine trigger events (appointment_confirmation, reminder_48hr, reminder_24hr, reminder_morning_of, on_my_way, arrived, completed, rescheduled, canceled) per the spec's voice. Authoring UI inside Anura Admin's Templates Builder (when that module exists; until then, seed via migration + edit via SQL).
- [blocked] **Twilio A2P 10DLC registration** — Brand + Campaign "Customer Care / Appointment Notifications". 5-10 business day approval. Nicholas's parallel workstream.
- [blocked] **Azure AD app registration + Mail.Send permission grant** — `assessments.wi@EES-WI.org` shared mailbox confirmed created by Nicholas (2026-05-14). Remaining: Azure AD app registration with Mail.Send application permission, plus PowerShell `Add-MailboxPermission` / `Add-RecipientPermission` (Send As) scoping the app to the mailbox. Future state mailboxes (assessments.mi, assessments.nc, assessments.co, assessments.in) follow same pattern.
- [blocked] **Google Cloud project for Routes API + Address Validation API** — billing enabled, API keys in Edge Function secrets, key restricted by Supabase Edge IP range. Nicholas's parallel workstream.

### Major unbuilt (each is a multi-session commitment)
- [ ] **Templates Builder dedicated module** — today document/email templates are managed inside record-detail views; Salesforce parity wants a dedicated module
- [ ] **Lifecycle Builder** — define per-object status transitions, automation triggers, ownership rules
- [ ] **Automation Builder** — Salesforce Flow Builder equivalent (record-change triggers, scheduled actions, multi-step flows)
- [ ] **EES AI assistant** — Claude-API-backed in-app assistant per `anura-ai-spec.md`
- [ ] **E-signature workflow polish** — schema exists (envelopes/recipients/tabs), workflow needs front-end completion
- [ ] **Portal subdomain deployments** — `owner.ees-ops.netlify.app`, `partner.ees-ops.netlify.app`, etc.
- [ ] **Field Mobile PWA** — purpose-built mobile per `anura-field-mobile.md`; includes photo compression at upload + watermarking pipeline
- [ ] **Azure AD / LEAP integration** — app pending Nicholas's manual setup at entra.microsoft.com

### Long-term cleanup (not urgent)
- [ ] **`cfp_*` tables permission policies** — two Cap Forecasting Pipeline tables still have `USING (true)` policies (isolated feature, not in main app flow). Replace with role-aware policies when CFP feature is touched again.
- [ ] **LayoutEditor support for `conversation_panel` widget config** — `AddWidgetModal` now offers Conversation Panel as a widget type, but `LayoutEditor` falls through to `UnknownWidgetTypeModal` on edit because the widget has no dedicated contents editor. The FK column (one of `contact_id` / `account_id` / `project_id` / `service_appointment_id`) and the optional `channel_filter` must be set via SQL or page-layout JSON for now. Build a 2-field editor (FK picker + channel filter dropdown) when admins start adding the panel to additional layouts.

---

## Recently completed (this session — 2026-05-17)

- [x] **Communications Module v1 — schema + visibility model** (`0ea66ec`, 7 migrations) — First phase of the LEAP Communications Module per `leap-communications-module-1.md`. Database foundation only; no edge functions or UI in this phase. The conversation panel widget shipped earlier this session (`fdfed14`) keeps working — Nicholas's Admin session short-circuits `is_admin()` true so all threads remain visible.

  **New tables (5)**: `outbound_mailboxes` (OBM-####, shared mailbox config keyed on program + state), `message_attachments` (file metadata + virus-scan status + delivery method `inline`|`signed_link`), `message_ai_transcripts` (MAT-####, per-iteration AI compose audit with stricter RLS than the parent message), `communications_view_all_grants` (role/permission-set grants for cross-thread visibility, seeded with Admin + Program Manager), `unmatched_inbox` (UI-####, inbound triage queue for emails that couldn't auto-thread; unique on `(ui_provider, ui_provider_message_id)` for webhook-retry idempotency).

  **Column extensions**: `email_templates` gains `template_locked_regions jsonb '[]'`, `template_ai_assist_allowed boolean true`, `template_default_outbound_mailbox_id uuid` (FK). `messages` gains `msg_ai_assisted boolean false` + `msg_ai_iterations integer 0`. Spec deviation logged in the migration: the existing `msg_provider_message_id` column already serves the spec's `message_external_message_id` role for `In-Reply-To` matching (it holds Twilio SIDs for SMS and would hold Microsoft Graph internetMessageIds for email); no duplicate column added. `opportunity_contact_roles` gains `ocr_includes_communications boolean true` (default-on so existing roles confer visibility; set false on tracking-only roles). `users` gains `user_voice_profile_enabled boolean false` + `user_voice_profile_style_signals jsonb '{}'` for the opt-in passive AI voice profile.

  **Six visibility helper functions** (all `SECURITY DEFINER STABLE`, granted to `authenticated`): `is_admin()`, `has_communications_view_all()`, `resolve_anchor_opportunity(p_conversation_id)` (returns setof — walks the four conversation FKs to their containing opportunities), `is_on_anchor_opportunity_contact_roles(p_conversation_id)`, `is_recipient(p_conversation_id)`, `is_record_owner_in_chain(p_conversation_id)`.

  **Replaced SELECT policies** on `conversations`, `messages`, and `message_ai_transcripts`. New form: `app_user_can(...) AND (is_admin() OR has_communications_view_all() OR ...record-specific helpers...)`. The object-level read gate stays as a precondition; the visibility model is a strict refinement on top. Insert/update/delete policies untouched — visibility model is read-only protection. AI transcripts get the stricter rules: admin / composing user / anchor-record owner / view-all only — no contact-role visibility (recipients never see how their email was drafted).

  **Spec deviations called out in the migrations**: (1) reused `msg_provider_message_id` instead of adding `message_external_message_id`; (2) added locked-region columns to `email_templates` only (the platform's three template tables split cleanly — `notification_templates` and `document_templates` don't need TipTap + AI workflow); (3) `permission_set` path on `communications_view_all_grants` exists in schema but `has_communications_view_all()` only checks the role path until the Permission Builder UI ships a permission-sets-to-users assignment table.

  **role_object_access seeds**: 5 internal roles (Admin, Program Manager, Project Manager, Project Coordinator, Director of Field Services) get read access to all five new tables; write access varies by table (Admin-write for mailboxes / grants; broader write for attachments / unmatched_inbox to support the triage flow; AI transcripts are read-only + admin-delete only since they're append-only audit). External roles (Property Owner, Property Manager, Subcontractor Partner) get no rows — they don't touch correspondence infrastructure.

  **Mirrored migrations into repo** under `supabase/migrations/20260517210000_*` through `20260517210500_*` (6 schema migrations + the help migration). Behavior verified: helpers don't error when called with no auth context (return false safely); Nicholas's session resolves to Admin so `is_admin()` short-circuits true.

  **Help: HA-00029** `communications-module-schema-and-visibility` (Communications category, 14 anchors) documents the new tables, column extensions, helper-function semantics, visibility model, and what isn't yet built. The Communications category is new to the help system — first article under it.

  **What's not in this phase** (per the spec's build order, all separate sessions): `send-email-v1` and `inbound-email-webhook` edge functions, Microsoft Graph app-only auth setup, TipTap rich-text composer + locked-region UI, AI compose assistant + voice profile UI, Unmatched Inbox triage UI, bell-icon unread surface + home-screen "needs my attention" cards, e-signature integration with conversations.

- [x] **compute-availability v4 — drive time delegated to compute-route-matrix** (`3234222`, 1 migration) — Architectural cleanup. v3 maintained an inline `driveTimeMinutes` helper + duplicate ZIP_CENTROIDS table to estimate morning travel from each resource's home base to the customer; the same math already lived in `compute-route-matrix` with a cache + Google passthrough layer on top, but the customer-scheduling flow never reached it. v4 fires a single batched `POST /functions/v1/compute-route-matrix` after `findQualifyingResources` returns and hoists the response into a `Map<contact_id, durationMinutes>` consumed inline by `generateSlotsForResourceDay`. Unique home-base origins are keyed by 5-decimal lat/lng so resources at the same shop share a matrix row. Resilient: non-2xx / malformed JSON / thrown exception all fall through to a zero-travel Map, matching v3's existing `customerForDrive → homeBaseLatLng` degradation path.

  **Net code change**: −50 lines inline math (haversine + ZIP_CENTROIDS + `driveTimeMinutes` removed), +60 lines `precomputeDriveTimes()` + Map plumbing. Source mirrored at `supabase/functions/compute-availability/index.ts`. Deployed and live; ezbr sha `a66124b593454276...`.

  **Behavior in mock mode**: identical to v3. Route-matrix's mock fallback uses the exact same haversine × 1.3 detour ÷ 25 mph math, so slot counts and ordering don't move. Smoke test against the live endpoint with single-family-assessment in Madison 53703 returned 72 slots over 3 days with the first slot at 7:30 AM Chicago — same as v3 would have produced.

  **Behavior post-key-deployment**: the moment `GOOGLE_MAPS_API_KEY` lands in Edge Function secrets, customer-scheduling slots start using live Google Routes traffic-aware durations + the 60-minute `drive_time_cache`. Zero code change required to flip. The cache is shared with any other surface that calls `compute-route-matrix` directly (dispatcher quote tools, future routing UI), so warm-up reuse comes for free.

  **What didn't change**: response shape, status codes, skill-requirement filter, operating-hours, 15-min buffer, 45-min lunch carve, day-fill resource preference, ZIP-fallback territory resolution. All identical between v3 and v4. The morning-travel constraint still only affects the first slot of a resource's day — Phase 2 work will compute true inter-appointment leg-by-leg routing.

  **Help: HA-00028** `compute-availability-v4-route-matrix-integration` covers what changed, why it matters, the resilience model, and what stayed the same. Anchored to route=/m/field + scheduling-platform / compute-availability / compute-route-matrix / edge-functions / drive-time concepts (6 anchors).

- [x] **Conversation panel widget — Service Cloud Messaging-style split-pane** (`fdfed14`, 2 migrations) — Closes the "conversation detail page UI" backlog item from the LEAP Communications spec. Replaces the read-only related-list table on the existing 22 page layouts (contacts/accounts/projects/service_appointments) with an inline chat surface that keeps thread context bound to the parent record — no navigation hop, matches Salesforce Service Cloud Messaging.

  **New widget type `conversation_panel`** registered in `AddWidgetModal` and rendered out of `RecordDetail.jsx` on the Related tab alongside file galleries, PRTSN history, and report widgets. Self-contained: loads its own conversations + messages on mount, owns its own state, never goes through `layoutService.fetchRelatedRecords`. Widget config keeps just `{ table: 'conversations', fk, channel_filter }` — fixed columns and sort, no per-layout customisation surface (yet — see backlog).

  **Migration `conversation_panel_widget_type`** swaps all 22 existing `related_list` widgets whose `widget_config.table='conversations'` to `widget_type='conversation_panel'`, preserving the FK column (one of `contact_id`/`account_id`/`project_id`/`service_appointment_id`) and dropping the dead `columns`/`sort_field`/`sort_dir`/`is_deleted_col` since the panel renders fixed columns + always-current sort. Verified post-migration: 8 accounts, 6 contacts, 7 projects, 1 service_appointment layout now carry the panel.

  **`ConversationPanelWidget` component** (`src/components/ConversationPanel.jsx`, ~520 lines) — split-pane with collapsible card chrome matching `RelatedListWidget`. Left pane (280px on desktop, full-width on mobile): scrollable thread list sorted by `conv_last_message_at DESC`, each row showing thread number, channel icon (SMS green, Email blue), customer address, relative timestamp ("2m ago" / "Yesterday" / "Mar 14"), 2-line preview, and a red unread badge. Right pane: `ThreadHeader` with record number + channel + status + customer/our addresses, scrollable `MessageBubble` timeline (outbound right-aligned emerald, inbound left-aligned sky, failed bubbles in red wash with hover-title error text), then `Composer` at the bottom. Aggregate unread badge in the card header sums `conv_inbound_unread_count` across all threads. Total card height: 520px desktop / 560px mobile.

  **Composer** is SMS-only in v1: 1600-char counter with live-red overflow indication, Cmd/Ctrl+Enter submit shortcut shown inline on desktop, Send button disabled while empty or sending. On non-SMS threads renders a friendly "email composer ships with the Communications Module" notice instead of a broken UI — matches the spec's deferral of TipTap rich-text + locked regions to the next phase.

  **Send path** routes through `sendReplyToConversation()` in the new `src/data/conversationsService.js`, which invokes the existing `send-notification-sms` v2 edge function with `trigger_event='dispatcher_reply'`, customer phone as `recipient_phone`, our phone as `from_number`, and all four FKs (`contact_id`/`account_id`/`project_id`/`service_appointment_id`) lifted off the conversation row. The edge fn's `find_or_create_conversation` idempotency means the new outbound message threads onto the existing conversation, never spawning a duplicate. Toast distinguishes mock-mode ("Reply queued (mock mode — Twilio not configured)") from real send. On send success both panes refresh in parallel — message list re-fetches (so the new bubble appears with status `sent` and provider id), thread list re-fetches (so the rollup trigger's new `conv_last_message_preview` and `conv_last_message_at` are reflected).

  **`mark_conversation_read` wiring**: on every thread-select the panel calls the existing RPC in parallel with the messages fetch. Optimistic local update zeroes the unread badge immediately; the rolled-up count from the next refetch reconciles. Failure is non-fatal — RPC error is console-warned, the user proceeds.

  **`conversations` + `messages` added to `TABLE_META`** in `RecordDetail.jsx` so direct URL navigation (or future global search hit) renders a sensible breadcrumb + header instead of the "Record" fallback. The panel remains the canonical surface; these registry entries are defensive.

  **`AddWidgetModal`** gains a Conversation Panel option (FK left null at creation — admin must wire it post-creation since the widget editor doesn't have a contents editor yet; backlog item logged for the LayoutEditor follow-up). Hint text states the four supported FK columns.

  **Build verified**: clean, `RecordDetail` bundle 347.03 → 363.87 kB / 81.64 → 85.82 kB gzip (~4 kB gzip for the new widget + service layer). 19.80s vite build.

  **Help: HA-00027** `conversation-panel` covers the layout, the open-thread/reply flow, the mock-vs-real Twilio behavior, the FK-to-surface mapping, and the email-deferred-to-Communications-Module behavior. Anchored to objects=contacts/accounts/projects/service_appointments/conversations/messages and concepts=conversation-panel/conversations/sms/reply/threading/notifications (12 anchors).

  **Open follow-ups from this work**: LayoutEditor contents editor for `conversation_panel` (backlog above); real-time `messages` subscription so inbound replies appear without a manual refresh (deferred — refresh-on-send + refresh-on-thread-switch is enough for v1); email composer (deferred to Communications Module per the spec).

---

## Recently completed (prior session — 2026-05-16)

- [x] **Scheduling Platform Phase 4c — conversations + messages threading** (this commit, 4 migrations) — Two-table conversation architecture modeled after Twilio Conversations / Salesforce MessagingSession + ConversationEntry. `conversations` is the thread (one row per channel + our_address + customer_address pair, partial unique index enforces one open thread per pair). `messages` is the children — direction-aware, channel-aware, same table for inbound and outbound so the threaded view is a single `ORDER BY` query. Full audit columns, RLS via `app_user_can`, role_object_access seeded for 5 internal roles.

  **Rollup trigger**: `trg_messages_rollup_to_conversation` fires AFTER INSERT on `messages` and updates the thread's `conv_last_message_at`, `_direction`, `_preview` (first 200 chars), `_inbound_unread_count` (incremented on inbound). Related list always shows the latest activity without joining to messages.

  **Helper RPCs**: `normalize_phone_e164` (any format → E.164, default US +1 for 10 digits), `resolve_contact_from_phone` (best-effort lookup against `contact_mobile_phone` and `contact_phone`; returns `{contact_id, account_id, project_id, service_appointment_id}`), `find_or_create_conversation` (idempotent, backfills null FKs on existing threads), `mark_conversation_read` (clears unread counter).

  **`twilio-inbound` webhook** (new): receives customer SMS replies. URL `https://flyjigrijjjtcsvpgzvk.supabase.co/functions/v1/twilio-inbound`. Signature validation via X-Twilio-Signature header when `TWILIO_AUTH_TOKEN` set (HMAC-SHA1 over sorted-param payload). MessageSid idempotency check prevents Twilio retries from double-inserting. Resolves contact context → finds/creates thread → inserts inbound message → rollup trigger updates thread. Returns empty TwiML. Smoke-tested: simulated webhook payload → CONV-00003 created, MSG-00004 inserted, inbound_unread_count=1; duplicate MessageSid → no double-insert.

  **`send-notification-sms` v2**: now dual-writes to both `notification_logs` (legacy low-level provider audit) AND `messages` (canonical thread). New optional fields in the request body: `account_id`, `project_id`. Outbound message gets `msg_status='queued'` → `'sent'` on Twilio 200 or `'failed'` on error. Mock mode still writes both tables with `mock-<uuid>` IDs. Smoke-tested end-to-end: outbound notification + inbound reply landed in same conversation (CONV-00004), ordered chronologically — outbound MSG-00005 with mock id + audit link, inbound MSG-00006 with Twilio SID.

  **Related list surface** (22 page layouts seeded): "Conversations" section + related_list widget added to every page layout on contacts (6), accounts (8), projects (7), service_appointments (1). Widget config filters by the appropriate FK column, sorts by `conv_last_message_at DESC`, displays thread number, channel, customer address, last-message timestamp/direction/preview, unread count, status. Section is collapsible, collapsed by default.

  **Help: HA-00026** `conversations-threading` documents the full schema, trigger logic, helper RPCs, edge fn integration, and what's not yet built (conversation detail page UI, dispatcher inbox aggregating unread, email-side dual-write port). Migrations: `conversations_and_messages_threading`, `messaging_helper_rpcs`, `conversations_related_list_widgets`, `help_conversations_threading`.

- [x] **Scheduling Platform Phase 4b — `send-notification-email` + `send-notification-sms` edge fns** (`fd2242b`, 1 migration) — Paired notification dispatch skeletons. Both deployed, mock-mode end-to-end verified through `notification_logs`.

  **`send-notification-email`**: Customer-facing email sender. Distinct from existing `send-email-via-graph` (which is delegated-OAuth, sends as the logged-in user's mailbox). This new fn uses Microsoft Graph **application-permission** Mail.Send to `/users/{mailbox}/sendMail` from a configured shared mailbox (default `assessments.wi@EES-WI.org`; sender resolves via `sender_mailbox` override → `DEFAULT_SENDER_BY_STATE[sender_state]` → `FALLBACK_SENDER`). Token via `client_credentials` flow against `OUTLOOK_CLIENT_ID/SECRET/TENANT_ID` (already set in Edge secrets for the OAuth fn — same Azure AD app, different permission type). Real send activates when those env vars present AND the Azure AD app has Mail.Send granted as application permission with `Add-ApplicationAccessPolicy` scoping it to specific mailboxes. Pre-launch: mock mode returns `mode:"mock"` and `provider_message_id:"mock-<uuid>"` while still writing the `notification_logs` row in `sent` state so the pipeline can be exercised end-to-end. On Graph 202 → real-mode synthetic ID `graph-<notification_log_id>` (Graph sendMail returns no message ID). Endpoint: `POST /functions/v1/send-notification-email`.

  **`send-notification-sms`**: Customer-facing SMS sender via Twilio Messages API. E.164 phone regex on `recipient_phone`, body truncated to 1600 chars (Twilio max; templates should aim ≤160 for single segment). Real send activates when `TWILIO_ACCOUNT_SID/AUTH_TOKEN/FROM_NUMBER` present. Pre-launch (A2P 10DLC pending): mock-mode same shape as email. On Twilio 200 with SID → `nl_provider_message_id` = the Twilio SID, `nl_status='sent'` (not `'delivered'` — delivery confirmation awaits a future status-callback webhook). Endpoint: `POST /functions/v1/send-notification-sms`. End-to-end smoke: `+14155551234` + body → NL-00002 row with `nl_provider='twilio'`, `nl_status='sent'`, mock id; row cleaned up.

  **Audit shape**: Both fns insert `notification_logs` in `queued` state BEFORE the provider call so a network failure still leaves an audit trail. Then update to `sent` on success or `failed` (with `nl_provider_error_message` + `nl_provider_error_code`) on failure. `nl_provider` is `microsoft_graph` for email, `twilio` for SMS — both pass the existing CHECK constraint without schema change. `notification_template_id`, `service_appointment_id`, `contact_id` all nullable so the fns can be called before templates seed lands.

  **What's deliberately not done**: (1) `notification-dispatch` orchestrator that resolves templates and routes by channel — blocked on `notification_templates` seed which needs customer-facing copy review; (2) Twilio status-callback webhook (out of v1 scope); (3) `twilio-inbound` for customer SMS replies — blocked on schema decision (`nl_status` CHECK has no `received` value, needs either CHECK extension or separate `inbound_messages` table); (4) wiring any of this into an actual UI trigger — that's the next session, once Nicholas decides which trigger surfaces (manual-send button on SA RecordDetail vs automation rule on status change vs both).

  **Help: HA-00025** `notification-send-edge-fns` covers both fns in one article — they're a pair and anyone configuring one will need the other. Anchored to route `/m/field`, objects `service_appointments`, `notification_logs`, `notification_templates`, concepts `notifications`, `sms`, `email`, `scheduling-platform`, `edge-functions`. Migration: `help_notification_send_edge_fns`.

- [x] **Scheduling Platform Phase 4a — `validate-address` + `compute-route-matrix` edge fns** (`0e82866`, 1 migration) — Two skeleton wrappers for the Google APIs, both deployed and smoke-tested in mock mode. Both env-gated on `GOOGLE_MAPS_API_KEY`: present and real API activates, absent and the mock fallback matches `compute-availability` v3 inline behavior. No code change needed to flip.

  **`validate-address`** (HA-00023, mock-mode verified): wraps Google Address Validation API. Returns `canonical_address`, `latlng`, `verdict` (granularity, completeness flags), and a `source` tag (`google` / `mock`). Customer scheduling intake calls this before submitting to `compute-availability` so the user sees address errors before the slot picker, not after. Endpoint: `POST /functions/v1/validate-address`. Mock smoke test: `{street:"3218 Progress Rd", city:"madison", state:"wi", zip:"53716-1234"}` → `{state:"WI", zip:"53716", latlng:{lat:43.0731,lng:-89.3411}}`.

  **`compute-route-matrix`** (HA-00024, mock-mode verified): wraps Google Routes ComputeRouteMatrix endpoint with `routingPreference:TRAFFIC_AWARE`. Accepts up to 25×25 origin/destination matrices with waypoints as either `{coordinates:{lat,lng}}` or `{address:{...}}`. Cache layer: every (origin, destination) result hashes to `drive_time_cache` keyed on `SHA-256(label + "|t=" + traffic_aware)` with a 60-minute TTL (`DEFAULT_TTL_MINUTES`); cache hits skip the outbound call. Mock results aren't cached — only real Google responses go in. Per-cell `source` tag in the response surfaces which cells were `cache` vs `google` vs `mock` so partial-cache responses are observable. Mock fallback uses haversine × 1.3 detour ÷ 25 mph average — same math `compute-availability` v3 uses inline, so swapping to real API doesn't change slot ordering during transition. Endpoint: `POST /functions/v1/compute-route-matrix`. Smoke test: 1×1 (Madison home base → 53703) returned 407s / 4553m mock; 2×2 (Madison + Milwaukee → 53703 + 53202) returned correct cross-product with origin/destination collinear cells reading 0/0.

  **Frontend service layer**: `src/serviceAppointments/serviceAppointmentService.js` gained `validateAddress({address})` and `computeRouteMatrix({origins,destinations,traffic_aware,departure_time})` wrappers. Not yet wired into the intake step — error UX (inline vs blocking vs soft warning) is a UX decision deferred until Nicholas can review.

  **Pre-launch behavior**: until Google Cloud project + billing land, both functions always return `source:"mock"`. The customer scheduling flow already runs in mock mode end-to-end in production via `compute-availability` v3; these wrappers replicate that pattern so the swap-to-real-API is a single env-var change with no code touch. Migration: `help_validate_address_and_compute_route_matrix`.

---

- [x] **Scheduler Phase 3 — single-WO Schedule modal + Dispatch Console v2** (this commit, 1 migration) — Two operational gaps closed in one session.

  **1. Single-WO Schedule modal.** New `WorkOrderScheduleModal` at `src/components/scheduler/WorkOrderScheduleModal.jsx` — small focused modal mirroring `ServiceAppointmentRescheduleModal`'s shape rather than running the full Project wizard. RecordDetail surfaces a `Schedule` toolbar button (calendar-plus icon, emerald) plus a matching overflow-menu entry on work-order records when the resolved status label equals `'To Be Scheduled'`. Modal loads the WO with picklist-resolved status, work_type duration, building/property/unit context, then calls `team_leads_qualified_for_work_orders([wo_id], start_date)` to populate a grouped Team Lead dropdown (Qualified / Missing Certs). Submit calls `bulk_schedule_work_orders` with a one-element WO array and an explicit `p_pinned_placements=[{work_order_id, start_ts, force:false}]` so the same engine + skills/cert gate + daily-window validation that drives the wizard handles this single-WO case. Default start = tomorrow 07:00 local. End time is read-only (work-type duration). Error mapping via existing `describePlacementError`. **Help: HA-00022** (`work-order-schedule-single`) anchored to object=work_orders + concept=single-work-order + concept=schedule-work-order.

  **2. Dispatch Console v2.** Existing read-only Dispatch Board at `/m/dispatch` upgraded into a true Salesforce Field Service-style console: filter rail on the left, unscheduled WO palette on the right, drag-to-assign and drag-to-reassign on the swimlane grid. **Data layer** (`src/data/dispatchBoard.js`): `fetchActiveTeamLeads` enhanced to return `{id, full_name, title, crew_label, service_territory_id, certification_ids[]}` joining `contact_certifications` filtered by `cc_expires_date >= today`. New exports: `fetchServiceTerritories`, `fetchActiveCertifications`, `fetchUnscheduledWorkOrdersForDispatch` (To-Be-Scheduled WOs joined with work_type required certs + building + property + service_territory with WO→building fallback), and `dispatchAssignWorkOrder` wrapping `bulk_schedule_work_orders` for the one-WO pinned case. **`DispatchFilterRail`** (`src/components/dispatch/DispatchFilterRail.jsx`, ~280 lines): collapsible left rail (240px / 36px), name search, multi-select crew / service territory / certification popovers, "Available only" toggle (merges absence intervals against the visible view range), `showing X of Y Team Leads` footer. Exports pure predicate `laneInScope(...)` used by the parent to filter lanes — Unassigned always passes. **`DispatchUnscheduledPalette`** (`src/components/dispatch/DispatchUnscheduledPalette.jsx`, ~320 lines): collapsible right rail (260px / 36px), search + territory + work-type filters, "Hide WOs without duration" toggle (on by default; null-duration WOs cannot be scheduled by the engine), color-coded by work_type_id matching the SA palette, HTML5 `draggable` with `dataTransfer.setData('application/x-ees-dispatch-payload', JSON.stringify({type:'unscheduled_wo', wo_id, project_id, duration_minutes}))`. **DispatchModule** rewritten: title → "Dispatch Console", new state for territories/certs/unscheduled list, second useEffect fetching them on `refreshNonce` change, derived `crewOptions` and `territoryNamesById` map, `filteredLanes` applying `laneInScope`. **Drop handler** `handleDrop(payload, lane, day, yWithinCell, cellHeight)`: refuses drops on Unassigned (toast warning); for `unscheduled_wo` validates duration > 0 and calls `dispatchAssignWorkOrder` with the snapped start; for `sa` computes end from existing duration and calls `dispatch_reschedule_service_appointment(p_sa_id, p_new_start_iso, p_new_end_iso, p_new_team_lead_contact_id)`, branching on returned status (`ok`/`slot_taken`/`not_reschedulable`/`invalid_resource`/`invalid_slot`/`appointment_not_found`) with appropriate toasts. Y → time mapping snaps to 15 min, nudges drops in the 11:30–12:00 lunch band up to 12:00, clamps to the daily window. `DayCell` extended as a drop target — `dragover` prevents default, sets `dropEffect='move'`, fires `onDragOverCell(cellKey)` for emerald dashed-outline + wash overlay highlight. Absences set `pointerEvents:'none'` so they don't intercept drops. **`SABlock`** made `draggable={true}`, emits `{type:'sa', sa_id, duration_minutes}` payload with `cursor:grab` and a "Drag to reassign" tooltip line. Server-side certification gate, status gate, and slot collision remain authoritative — the client never bypasses them. **Help: HA-00019 upgraded in place** — title → "Dispatch Console — resource lanes, filters, and drag-to-assign", slug → `dispatch-console-overview`, body rewritten covering filter rail, palette, drag flows, snapping, and RPC error semantics. Anchor `dispatch-board` concept renamed → `dispatch-console`; 5 new anchors added: object=work_orders, concept=drag-to-assign, concept=drag-to-reassign, concept=resource-filter, concept=unscheduled-palette. Build verified clean (DispatchModule bundle 38.42 kB / 10.90 kB gzip, RecordDetail 347.03 kB / 81.64 kB gzip). Migration: `help_dispatch_console_v2_upgrade`.

---

## Recently completed (prior session — 2026-05-14)

- [~] **Project Scheduler — v2 and v3 iteration on Nicholas's feedback** (uncommitted at time of TASKS edit; covered by `cad0566` + `9f74452` + this commit) — Three rounds of dispatcher-led tightening of the Phase 1 wizard. **v2 (`cad0566`):** added `work_types.work_type_post_buffer_minutes` (default 5) and rewrote `bulk_schedule_work_orders` to read per-WO buffer with same-unit-zero-buffer override (peek-ahead at next not-placed WO's unit_id; same unit → 0 buffer, different unit → previous WO's post_buffer); backfilled realistic durations on direct-install measures (Showerhead 15→3, Aerator-K/B + LED-9W 5→2). Result: 3 WOs in one apartment pack into ~7 contiguous minutes instead of ~50. Renamed wizard's primary trigger "Schedule" → "Schedule Work Orders" (Nicholas: was ambiguous whether it scheduled the project or its WOs). Wizard empty state when 0 WOs in 'To Be Scheduled' replaced with a green-check "Nothing to schedule" panel. **v3 (`9f74452`):** dropped the preview step entirely (Nicholas: never saw preview render correctly, just wanted to go); bumped `RELATED_LIST_MAX_ROWS` 5→10; wired View All footer link to actually navigate via new `getTableListUrl()` helper in urlNav.js (maps table → module → section, handles the workorders/timesheets/absences section-name exceptions). **This commit:** RELATED_LIST_MAX_ROWS 10→7 (Nicholas: 7 is plenty per object); restored the preview as a **Gantt chart** Step 3 (Nicholas: he does want a preview, just not the day-grouped table — a Gantt that shows the day's shape per assignee). Wizard now 3-step: Select & order → Crew & window (button: Preview placement →) → Preview & schedule (button: Confirm — schedule N). After commit the wizard auto-closes and a toast confirms the result; no separate Done step. Selection step gained ▲▼ arrows in an Order column so the dispatcher can sequence placement (the engine walks input order, so order = placement priority). Gantt swimlane per day: 7am–3:30pm time axis, hour gridlines, hatched lunch block 11:30–12:00, WO blocks colored by `unitColor(unit_name)` (HSL hash) so same-apartment WOs visually group as one contiguous color band. Block ≥20 min shows the WO number inline; smaller blocks rely on hover tooltip with full record number / work type / location / time range / duration. Unit-color legend below the Gantt. Unplaced WOs render in a separate amber-banded list below the Gantt with each row's placement_error reason. Build verified clean (RecordDetail bundle 309.74 kB → 73.30 kB gzip).

- [x] **Project Scheduler — Gantt drag-to-reorder + zoom + fullscreen** (this commit) — Round 4 on Nicholas's feedback. **Drag-to-reorder:** every WO block on the Gantt is now a pointer-event drag target. Mouse/touch down on a block → starts drag (cursor:grabbing, block fades to 0.35 opacity). Drag over another block → that target gets a green outline + a vertical insertion bar on the left edge (drop = before) or right edge (drop = after) depending on which half of the target the pointer is in. Drop → `commitDragReorder` splices the dragged WO id into woOrder at the new position and immediately re-runs the preview RPC with the new ordering. The Gantt re-renders with the new placement (kept on-screen during the refetch — only initial preview clears state to show "Computing…"). Pointer listeners are document-level so the drag works even when the pointer leaves the source block. Refs (`dragWoRef`, `dragOverRef`) bridge state changes into the listener handlers without resubscribing. Toolbar hint updated: "Drag a block to reorder. Drop before/after another WO to change its placement priority — the engine re-runs automatically." A "Refreshing…" indicator appears next to the hint while the re-preview is in flight. **Drag is bounded to reorder only** — dragging horizontally does NOT yet move a WO to a specific time pin; the engine still owns time placement based on input order + per-WO buffer + same-unit packing. **Zoom + fullscreen** (shipped same round): toolbar 1×/2×/4×/8× controls horizontal scroll multiplier; min block width and label-visibility threshold scale with zoom so 2-min direct-installs become readable at 4× and above. Fullscreen Expand mounts a viewport-filling overlay with its own zoom state (default 2×, max 16×) and Esc-to-close. Modal width on Step 3 bumped 760→1200. Build clean (RecordDetail bundle 316.10 kB → 75.01 kB gzip).

- [x] **Project Scheduler — time-pinned placement (drag to specific time)** (this commit, 2 migrations) — Drag-to-reorder was incomplete: dragging only changed sequence priority, not the actual clock time. Dispatcher feedback: "What if I want to pull to a new time slot entirely that does not currently have an appointment and booking?" Built engine + UI support for hard time pins. **Engine v3:** new optional `p_pinned_placements jsonb` param on `bulk_schedule_work_orders` accepting `[{work_order_id, start_ts}, ...]`. Pinned WOs are placed at their exact start time *before* the greedy loop runs, then carved out of every day's working-period array alongside SAs and resource_absences — so non-pinned WOs flow around them as if the pins were calendar absences on the lead. Validation: pin must be inside `[p_start_date, p_end_date]`, on a weekday, fully inside the daily window, not crossing lunch, not crossing midnight, not overlapping another pinned WO on the same lead. Failures return the WO unplaced with explicit `placement_error` codes (`pin_outside_working_hours`, `pin_overlaps_existing`). Same-unit zero-buffer peek-ahead skips pinned WOs when deciding buffer for the next non-pinned WO. **Frontend:** wizard now keeps a `pins` map `{wo_id → ISO start_ts}`. The drag pointer-move handler distinguishes two zones: (a) over another `[data-wo-id]` block → reorder (existing behavior); (b) over a `[data-gantt-day]` track but no block → compute time from pointer X-coordinate (snap to nearest 5 min), record `dragDropEmptyRef = {day, minuteOfDay}`. On pointer-up the commit handler picks reorder OR pin based on which ref is set. Pinned blocks render with an amber 2px border + amber shadow + a 14×14 amber 📌 badge in the top-right corner; clicking the badge unpins (and re-runs preview). Toolbar hint updated. New error labels in `describePlacementError`. **Smoke test** (db side): pinned WO-00017 to 10:30 AM May 18 with 16 other WOs in input; verified WO-00017 placed exactly at 10:30:00–10:33:00 and other 16 placed before it ending 10:25 with no overlap. Build clean (RecordDetail bundle 318.48 kB → 75.91 kB gzip).

- [ ] **Project Scheduler Phase 2 — drag-to-reorder unit-level grouping** — Today dragging operates on a single WO. In practice the dispatcher wants to say "do all of building B first, then all of building A" — i.e. move a whole unit/building's worth of WOs together. Option: detect that a dragged WO has same-unit siblings, and offer "move whole unit" vs "move just this WO" on drop. Or hold a modifier key (Shift+drag) to move group. Defer until Nicholas reports the per-WO drag isn't enough.

- [ ] **Project Scheduler Phase 2 — multi-tech parallel placement** — Nicholas flagged during v3 review: "shows how these are assigned to one or two people and what the day looks like." Today the bulk scheduler takes ONE Team Lead; Phase 2 lets the dispatcher allocate WOs across multiple Team Leads on the same project window. Engine work: separate placement runs per lead with cross-lead awareness (don't double-book a tech, balance load). Frontend: multi-lead swimlanes on the Gantt, one row per assignee. Question of which WOs go to which lead: auto-split round-robin? Auto-split by unit (lead 1 takes building A, lead 2 takes building B)? Manual assignment? Defer until Nicholas signals which model he wants.

- [ ] **Project Scheduler Phase 2 — tenant-window scheduling constraints** — Nicholas flagged during v3 review: "rearrange because of a specific tenant schedule." Some apartments have access windows (tenant home only after 5pm, or only Tues/Thurs mornings). Today the engine is window-blind. Phase 2 adds a per-unit (or per-WO) allowed-window field; engine skips slots outside the unit's window when placing that WO. Schema: `units.unit_access_windows jsonb` or a `unit_access_windows` table. Validation/UX for staff to capture the constraint up-front.

- [x] **Project Scheduler — bulk-schedule work orders for a project** (`9601da3`, 4 migrations) — Phase 1 of the dispatcher console. Replaces the not-real "manual one-by-one" booking flow with a real FSL-style allocator: select a Team Lead, give a date range, and the engine lays unscheduled work orders onto the crew's calendar. **Engine** lives in plpgsql at `public.bulk_schedule_work_orders(project, wo_ids[], team_lead, start, end, ...working-hours overrides..., commit)` — single SECURITY DEFINER function with dual mode via `p_commit`: false returns the placement plan; true re-runs the same algorithm under `pg_advisory_xact_lock` keyed on the team lead's contact id and writes Service Appointments + Assignments + flips WO status to 'Scheduled'. Same code runs both modes — preview and commit can't drift. Algorithm: greedy first-fit, in-record-number order, across [p_start_date, p_end_date] skipping weekends. Each working day is `[07:00, 11:30] ∪ [12:00, 15:30]` Chicago time minus existing SAs on this team lead (joined via SAA, excluding 'Canceled') and minus active `resource_absences` rows — both carved out of the per-day period array before placement. Effective WO duration = `COALESCE(work_orders.work_order_duration_minutes, work_types.work_type_duration_minutes)`. NULL → row returned as unplaced with `placement_error='duration_not_set'`. WOs that don't fit anywhere in the window get `placement_error='no_capacity_in_window'`. Inter-WO buffer defaults to 15 min. Validation enforces: project exists & not deleted, every WO id belongs to project & is in 'To Be Scheduled' status & not deleted, team lead exists & has 'Team Lead' in title, valid date order, valid daily/lunch boundary order. **Frontend wizard** at `src/components/scheduler/ProjectSchedulerWizard.jsx` — 4-step modal (Select WOs → Pick Team Lead + window → Preview → Done) opened from a new Schedule toolbar button on Project RecordDetail (calendar icon, next to Reports). Step 1 lists every 'To Be Scheduled' WO with effective duration; rows with missing duration highlighted amber and uncheckable. Step 2 picks Team Lead (contacts with `contact_title ILIKE '%Team Lead%'`, parsed crew label shown after em-dash) and date range (defaults to next Monday + Friday). Step 3 calls the RPC with `commit:false`, groups results by day chronologically with unplaced rows segregated at the bottom, and shows total/placed/unplaced summary cards. Step 4 calls the RPC with `commit:true`, shows success card with placed count and remaining-unplaced count if any. Partial-commit by design — places what fits, leaves the rest in 'To Be Scheduled' for the dispatcher to retry with an extended window. **Data layer** at `src/data/projectScheduler.js`: `fetchUnscheduledWorkOrdersForProject` (joins building/unit/work_type, resolves effective duration), `fetchTeamLeads` (parses crew label), `bulkScheduleWorkOrders` (RPC wrapper, only sends optional working-hours params when explicitly set so RPC defaults apply), `summarizeWorkOrderDurations` + `describePlacementError` helpers for the UI. **Help article HA-00018** `project-scheduler-overview` (Scheduling category, internal audience) covers when to use it, the 4-step UI, the algorithm in plain English, and the explicit limits (one team lead per batch, no inter-property travel time, no global optimization, same-project only). Anchored to `route=/m/field`, `object=projects`, `object=work_orders`, `concept=project-scheduling`, `concept=bulk-scheduling` (5 anchors). **Backfilled work_type_duration_minutes** on 8 common direct-install measures so the scheduler has real durations: Showerhead-Handheld (WT-00030)=15min, Aerator-Kitchen (WT-00008)/Aerator-Bath (WT-00013)/LED-9W (WT-00043)=5min, Thermostat-Advanced (WT-00007)/Thermostat-Programmable (WT-00047)=30min, PTAC Install (WT-00004)=90min, Blower Door Diagnostic (WT-00005)=60min. **Smoke test** seeded 20 WOs on PROJ-00001 (North Willow MF) across 5 units × 4 measures, ran preview against Javier May 18-22: all 20 placed correctly across 2 days with proper 15-min buffers and lunch carve. Added test resource_absence 9:00-10:00 May 18 → algorithm carved the gap, pushed WO-00018 (30min) to 10:00, fit WO-00019 (5min) in the 8:55-9:00 sliver. Test absence soft-deleted (RA-00001). Commit test scheduled WO-00014/15/16 (3 of the 20) → SA-00014/15/16 created with status=Scheduled, 1 SAA each linking Javier, WO statuses flipped to Scheduled with scheduled_start_date/time populated. 17 WOs remain in To Be Scheduled for the UI demo. Build verified clean (19.85s, RecordDetail bundle 304.24 kB → 71.83 kB gzip with the wizard added). Migrations: `bulk_schedule_work_orders_rpc`, `work_type_duration_backfill_common_measures`, `test_bulk_schedule_seed_north_willow_wos`, `help_article_project_scheduler`.

- [x] **Help articles — scheduling system** (`214072c`, 1 migration) — Four new help articles authored to cover everything that shipped in the EES scheduling slice through `ea585ca`. (1) `customer-scheduling-overview` (HA-00014) — what customers see at `/sa/*`, the four publicly schedulable work types and their durations, the full Property→Building→Unit→Opp→Project→WO→SA→SAA→Token cascade, who gets assigned per work type (SF/TH = Javier+Kenji, MF = Kenji solo), and where staff find the appointment after it's booked. (2) `service-appointments-inbox` (HA-00015) — the Field → Service Appointments console: status meanings (Scheduled / Dispatched / In Progress / Completed / Cannot Complete / Canceled), what RecordDetail enables on a row, how Out of Office silently removes slots from customer view, and how customer-initiated changes flow back. (3) `out-of-office-overview` (HA-00016) — Field → Out of Office: required fields, what happens on save (overlap-check in compute-availability), the existing-appointment caveat (not auto-canceled), best practices for half-days / recurring absences, and the soft-delete restore-availability behavior. (4) `manage-link-overview` (HA-00017) — `/sa/manage/<token>`: what customers can do, token lifecycle (valid until appointment start), what changes on customer-initiated reschedule vs cancel, troubleshooting steps for "my link doesn't work" calls. All four articles share a `route=/m/field` anchor (surface via topbar Help button anywhere in Field), plus relevant `object` anchors (`service_appointments`, `resource_absences`) for record-detail surfacing, plus concept anchors for inline HelpIcon placement. HelpIcon added inline next to the Service Appointments inbox h2 — clicking surfaces inbox + customer-scheduling + manage-link articles in one panel. Lookup smoke-tested against all four anchor surfaces: object/SA → 3 articles, route/field → 4 articles, object/absences → 1 article, concept/inbox → 1 article. Build verified clean. Migration: help_articles_scheduling.
- [x] **Scheduling — Customer self-serve reschedule + cancel (manage page)** (this commit, 2 migrations) — `/sa/manage/<token>` is now a real management page, not a stub. Customers can view their appointment, reschedule to a new time, or cancel — without emailing staff. Token validation in each RPC: shape (32-hex), expiry (`sat_expires_at < now()`), consumed-state (`sat_consumed_at` set). Tokens are NOT consumed on lookup or reschedule (customer may reschedule again); they ARE consumed on cancel (terminal). 3 new SECURITY DEFINER RPCs in scheduling_manage_appointment_rpcs migration (+ scheduling_lookup_appointment_fix_columns hotfix for stale FK names): `lookup_service_appointment_by_token(text)` → returns full appointment detail (work_type, sa times, address, customer, auditor, territory, token state); `cancel_appointment(text)` → validates token, refuses if SA already past-start ('too_late'), updates sa_status='canceled', consumes token; `reschedule_appointment(text, timestamptz, timestamptz, uuid)` → validates token + new slot + resource, takes pg_advisory_xact_lock on (resource, Chicago-day) to serialize against concurrent service appointments, overlap-checks against same resource's existing SAs (excluding self), updates SA times, soft-deletes old SAA, inserts new SAA. All three granted to anon + authenticated. Schema fix scheduling_saa_unique_constraint_respects_soft_delete: replaced strict UNIQUE(service_appointment_id, contact_id) with a partial unique index `WHERE saa_is_deleted=false` — the old constraint counted soft-deleted rows, breaking any auditor-reassignment workflow. ManagePage.jsx rewritten: state machine with 11 states (loading | error | view | confirm_cancel | canceling | canceled | loading_slots | slots | confirm_reschedule | rescheduling | back-to-view). Lookup happens on mount; failure paths surface specific messages (invalid_token / expired_token / appointment_not_found / generic). View state shows full appointment with Reschedule + Cancel actions. Cancel shows a confirmation dialog ("can't be undone") then calls the RPC. Reschedule calls compute-availability with the stored address, reuses the same day-grouped + deduped slot grid pattern from ServiceAppointmentFlow, shows a Confirm Reschedule summary with old vs new times, then calls the RPC. slot_taken race handling: refetch availability and bounce back to picker with inline error banner. Refreshes the lookup after successful reschedule so view shows new times. serviceAppointmentService.js extended with lookupAppointment / cancelAppointment / rescheduleAppointment — direct supabase.rpc() calls (the RPCs are SECURITY DEFINER + granted to anon, so no edge function wrapper needed). End-to-end smoke-tested against production: lookup → reschedule (Lisa's SA-00012 moved May 16 14:00 UTC → May 17 14:30 UTC) → lookup confirms new times → cancel succeeds → idempotency check (double-cancel returns already_consumed). Build verified clean. Live at https://ees-ops.netlify.app/sa/manage/&lt;token&gt; after Netlify auto-deploy.
- [x] **Scheduling — Staff Service Appointments inbox + Schedule view fallback** (`3f64b79`) — Customer-self-scheduled appointments now have a dispatcher console. Two changes: (1) `fetchSchedule` (Schedule day-view Gantt) extended with a single-Tech fallback — when an appointment has no Team Lead in its SAA but exactly one assignee, that Tech becomes the bucket key with the row labeled by their full name (e.g. "Javier Martinez"), not "Unassigned" and not "Martinez Crew". Customer-self-scheduled assessments group correctly under the auditor's name. Multi-member SAAs with no Team Lead still bucket under Unassigned (a real data issue worth surfacing). (2) New `fetchUpcomingServiceAppointments(days=14)` in fieldService.js: pulls service_appointments with sa_status='scheduled' in the upcoming N-day window, joins opportunity→account (customer name/phone/email), property (service address), work_type (label + slug), service_territory (label), and the single SAA assignee (auditor). Returns one row per appointment, sorted by scheduled start. (3) New "Service Appointments" section in FieldModule (placed second after Home, before Projects/Work Orders/Schedule). `ServiceAppointmentsInbox` component renders results grouped by Chicago calendar day with a card per day. Each row shows: scheduled time range, customer name + phone + email, service address (street + city/state/zip), work type + territory, assigned auditor + SA record number. Click a row → opens the SA in the existing RecordDetail. Window selector (7/14/30/60 days) + Refresh button. Empty state links to /book so dispatchers know where the URL goes. Mobile responsive (responsive grid). Picklist lookup for 'scheduled' status uses a direct picklist_values query (outreachService's loadPicklists exposes {byId, valueById, byField} but no value→id helper). Build verified clean. Live at https://ees-ops.netlify.app/m/field/service_appointments after Netlify auto-deploy.
- [x] **Scheduling — Customer-facing React scheduling pages** (`0d87fdd`) — Public `/sa/*` UI shipped. main.jsx path-routing extended: any path matching `/sa` or `/sa/<rest>` renders ServiceAppointmentRoot, which bypasses the staff AuthGate + sidebar/topbar (same pattern as /sign/* for the e-signature portal). 8 new files under `src/serviceAppointments/`: ServiceAppointmentRoot (path dispatcher + body-overflow override since the staff app sets overflow:hidden), ServiceAppointmentHeader (navy brand bar with EES mark), ServiceAppointmentFooter (contact + jurisdiction), SlugIndex (landing at `/sa` listing the 4 services), ServiceAppointmentFlow (4-step state machine: intake → loading → slots → confirming → scheduling → success), ManagePage (stub at `/sa/manage/<token>`, full reschedule/cancel deferred), serviceAppointmentService.js (POST wrapper for compute-availability + create-service-appointment edge functions), styles.js (design-token mirror with Inter/JetBrains Mono fonts, emerald primary, navy chrome, mobile-first card layouts, 8px radius). IntakeStep validates client-side (10-digit phone, 5-digit ZIP, 2-char state, email regex) before submission; per-slug intake fields are config-driven (multifamily slugs add `number_of_buildings`). SlotsStep groups results by Chicago calendar day and dedupes by start_iso (multiple Techs at the same time collapse to one tap target; edge function's day-fill ordering puts the preferred resource first). ConfirmStep shows summary; Confirm button calls create-service-appointment. SuccessStep shows confirmation checkmark, full appointment details, manage URL in monospace. Race-condition handling: slot_taken response refreshes availability and bounces back to slot picker with the inline error. Mobile-responsive throughout — single-column on narrow viewports, touch-targets ≥ 44px, no input zoom on iOS (16px font on mobile inputs via existing index.css). Build verified clean via vite (15s, no warnings). Live at https://ees-ops.netlify.app/sa.
- [x] **Scheduling — `create-service-appointment` edge function + `create_service_appointment` RPC** (`a2ed092`, 8 migrations) — Customer-facing scheduling endpoint deployed at `https://flyjigrijjjtcsvpgzvk.supabase.co/functions/v1/create-service-appointment` (public, `verify_jwt=false`). Takes a slot chosen from a compute-availability response plus customer details, atomically creates the full 10-record cascade via the `create_service_appointment` PG function: Account → Contact → Property → Building → Unit → Opportunity → Project → Work Order → Service Appointment → SAA → Service Appointment Token. Concurrency: `pg_advisory_xact_lock` on (resource_id, calendar_day) serializes simultaneous service appointments against the same Tech; post-lock conflict check rejects duplicate slots with `status='slot_taken'`. Input validation: phone normalized to 10 digits (drops formatting and leading "1" country code), ZIP to 5 digits, state to 2-char code — all matching schema CHECK constraints. Dedup: Contact lookup by phone; if found, reuses the Account. Token: 32 hex chars from `gen_random_uuid()`, used in customer manage URL `<APP_BASE_URL>/sa/manage/<token>`, expires 7 days after appointment start. Multifamily Energy Assessment skill requirement also updated to BPI Multifamily Building Analyst (previously BPI Building Analyst — single-family-focused auditor isn't qualified for 4-plex). Final qualification matrix: Single-Family/Townhome → Javier + Kenji (BPI BA); MF Energy + MF Diagnostic → Kenji alone (BPI MFBA). Smoke test verified: HTTP POST through edge function creates full cascade with `manage_url`; duplicate attempt returns `slot_taken`. Schema gotchas discovered: `contacts.contact_name` is NOT NULL alongside first/last (set = `first||' '||last`); `gen_random_bytes` requires pgcrypto (not enabled), used `gen_random_uuid` instead; `service_appointment_tokens` is a lean table with no record_number/owner/created_by. Notification firing deferred — Twilio A2P 10DLC and Mail.Send permission still pending. Migrations: scheduling_create_service_appointment_rpc + 6 iterative fixes (lock pattern, created_at prefix, contact_name, phone/zip/state validation, UUID token, service_appointment_tokens schema) + scheduling_mf_energy_assessment_requires_mfba. Final RPC consolidated in `supabase/migrations/20260515020000_scheduling_create_service_appointment_rpc.sql`.
- [x] **Scheduling — `compute-availability` edge function v2** (`1cfc74f`, 4 migrations) — Customer-facing slot-generation engine deployed at `https://flyjigrijjjtcsvpgzvk.supabase.co/functions/v1/compute-availability` (public, `verify_jwt=false`). Inputs: work_type slug, address, optional intake fields, optional date range. Outputs: territory-resolved slot list over 14 days (configurable up to 28) with per-resource attribution. Honors work_type_skill_requirements (only qualifying Technicians eligible), Service Territory containment (ZIP-list now, polygon when geocoder lands), operating_hours per territory per day-of-week, existing service_appointments on the resource's calendar, resource_absences, 15-min minimum buffer between appointments, 45-min lunch block that must START in [11:30, 12:15], 7:00 AM workday start, day-fill resource preference. 4 supporting RPCs added: `resolve_territory_by_point` (PostGIS ST_Contains with deepest-match-wins), `technicians_in_territory` (union of denormalized primary FK + service_territory_members), `appointments_for_resource_in_window` (joins service_appointment_assignments + status filter), `count_appts_for_resource_in_territory_day` (day-fill ranking). Mockable transport layer: Google Address Validation + Routes both passthrough/haversine fallback in v1, real APIs swap in when env keys land. Timezone-correct for America/Chicago via `Intl.DateTimeFormat`-based helpers (Deno Edge runtime defaults to UTC; server-local setHours would shift slots by 5-6 hours). 45 Wisconsin ZIPs seeded into service_territory_zips (23 SW WI, 22 SE WI). SW/SE Wisconsin polygon seam shifted from -89.7 to -88.7 longitude so Madison resolves correctly. Smoke test verified: Madison 53703 → SW WI, Javier + Kenji return 90 slots over 3-day window with lunch math correct. Migrations: scheduling_seed_wisconsin_zip_codes, scheduling_compute_availability_rpcs, scheduling_fix_wisconsin_subterritory_boundaries.
- [x] **Scheduling — BPI MFBA skill + corrected MF Diagnostic requirement** (`1cfc74f`) — Added `BPI Multifamily Building Analyst` skill (distinct from `BPI Building Analyst`, which is single-family). Multifamily Diagnostic Assessment now requires only BPI MFBA (replaces previous BPI BA + BPI EP requirement, which was soft-deleted). Kenji Chen assigned BPI MFBA as the senior multifamily auditor. Qualification matrix: Single-Family/Townhome/Multifamily Energy Assessment → Javier + Kenji (BPI BA); Multifamily Diagnostic → Kenji alone (BPI MFBA). Migration: scheduling_add_bpi_mfba_and_correct_mf_diagnostic_requirement. (Open question for next slice: should Multifamily Energy Assessment also require BPI MFBA instead of BPI BA?)
- [x] **Scheduling — slot-engine test data ready** (`91c1584`, 2 migrations) — Skill requirements added on all 4 customer-schedulable work_types: Single-Family / Townhome / Multifamily Energy Assessment require BPI Building Analyst; Multifamily Diagnostic Assessment requires BPI Building Analyst + BPI Envelope Professional. Test-data fix: Kenji Chen gained BPI Envelope Professional so at least one Technician qualifies for Multifamily Diagnostic. Final qualification matrix: Javier Martinez + Kenji Chen qualify for Single-Family/Townhome/Multifamily Energy Assessment; Kenji Chen alone qualifies for Multifamily Diagnostic. Daniel Okonkwo (trainee, OSHA 10 only) qualifies for none, which is correct. Territory assignments: all 4 Technicians set with Southwestern Wisconsin primary (denormalized on `contacts.contact_service_territory_id`) plus matching `service_territory_members` row with `stm_is_primary=true`; all 4 also non-primary members of Southeastern Wisconsin. Migrations: scheduling_skill_requirements_and_technician_territories, scheduling_kenji_bpi_envelope_professional.
- [x] **Scheduling Platform — schema slice** (`903a534`, 7 migrations) — FSL-equivalent customer-facing scheduling foundation. PostGIS enabled. `service_territories` gained `service_territory_polygon geography(MultiPolygon,4326)` with GIST index. `work_types` extended with `work_type_is_publicly_bookable`, `work_type_public_slug`, `work_type_customer_facing_description`, `work_type_duration_per_unit_minutes`, `work_type_unit_count_intake_field` (unique slug index where publicly schedulable + not deleted). Eight new tables: `service_territory_zips` (ZIP-list fallback), `service_territory_members` (multi-territory junction with `stm_is_primary` mirror; primary remains `contacts.contact_service_territory_id`), `operating_hours` (per-territory day-of-week × time windows), `resource_absences` (PTO/Training/Sick/Other absence blocks per contact), `service_appointment_tokens` (single-use customer manage-appointment tokens, edge-function-only writes), `drive_time_cache` (Google Routes results keyed on SHA-256 origin/destination hashes, edge-function-only), `notification_templates` (outbound SMS/email templates keyed on trigger event with per-work_type variants), `notification_logs` (append-only audit of every send, keyed on Twilio SID / Graph message ID). All new tables: standard EES audit columns, record-number triggers, RLS enabled with role-aware policies via `app_user_can`. Picklist seeding: 8 service_appointments.status values (scheduled, en_route, arrived, in_progress, completed, cannot_complete, canceled, no_show), 6 net-new projects.record_type values (single_family_energy_assessment, townhome_energy_assessment, multifamily_energy_assessment, multifamily_diagnostic_assessment, hvac_quote, customer_consultation — not in SF export, EES-native), 4 resource_absences.absence_type values. `role_object_access` seeded for all 8 new tables across 10 internal roles (external roles locked out). Operational territories seeded: 4 new sub-territories (Southwestern Wisconsin + Southeastern Wisconsin under Wisconsin parent; Michigan Lower Peninsula + Michigan Upper Peninsula under Michigan parent) plus North Carolina polygon. All 5 operational territories have default operating hours: Mon-Fri 07:30 first-slot / 18:00 last-slot, Sat 07:30 / 11:00, Sun closed (no row). Coarse bounding-box polygons placeholder until admin draws real shapes.

- [x] **Help frontend — context-aware ? button + /help full center** (`345cc34`) — added `HelpTopbarButton` in topbar right corner (every page), opens HelpPanel with anchors derived from current page via `useCurrentPageAnchors` hook. HelpPanel upgraded with top search box, footer "Browse all help articles" link, and search-mode swap. New `HelpCenterPage` at `/help` and `/help/<slug>` — sidebar by category, search, reading pane, audience filter. `urlNav.js` routes `/help/*` outside module switch.
- [x] **HA-00013 "Finding Help — The Help Center and the Help Button"** — documents both entry points for users. Audience='all'. Anchored to module:home, help-system concepts, and /help route.
- [x] **Help articles for Project Reports** (`df6ea44`) — HA-00009/00010/00011/00012 (Generate / Authoring / Publish Workflow / Photo Variants).
- [x] **Help articles for permission sweep** (`fc625bd`) — HA-00006/00007/00008 (changelog / per-role defaults / troubleshooting runbook).
- [x] **Permission sweep — role-based RLS enforced on all business tables** (`af31b82`, migrations `20260514204537` + `20260514204558`) — seeded `role_object_access` with 65 readable tables × 9 internal roles × 4 actions matrix; dropped 51 `internal_staff_*` permissive overrides. Net: every authenticated request flows through `app_user_can()`. Admin short-circuits true; external roles fully locked out until portal ships.
- [x] **PRG Phase 3 — documents FK columns** (`13d7560`, PRG v16) — `documents.project_report_template_id` + `documents.project_report_template_snapshot_id` FK columns (both nullable, ON DELETE SET NULL), 2 partial indexes, check constraint `documents_prtsn_implies_prt_chk`. Edge fn populates both on insert. Verified live with PROJ-00001 against PRT-00001 v2.

## Completed (chronological, most recent first)

- `bd646ba` Conversations + messages threading — twilio-inbound webhook + send-notification-sms v2 + related lists on contact/account/project/SA
- `fd2242b` Scheduling Phase 4b — send-notification-email + send-notification-sms
- `0e82866` Scheduling Phase 4a — validate-address + compute-route-matrix edge fns
- `560403c` chore: backfill TASKS.md commit hash for 9e77625
- `9e77625` Single-WO Schedule modal + Dispatch Console v2 — filter rail, unscheduled palette, drag-to-assign/reassign
- `8c47b67` dispatcher v10 — row-scope calculated field support
- `542bc82` chore: backfill TASKS.md commit hash for 05f582f
- `05f582f` dispatcher v9 — cross-filter support
- `11151ab` chore: backfill TASKS.md commit hash for 6857310
- `6857310` dispatcher v8 — related-field filters/sorts support
- `2afad5b` chore: backfill TASKS.md commit hash for 9f69b48
- `9f69b48` dispatcher v7 — custom filter logic support
- `8034885` dispatcher v6 — multi-hop via_path support
- `8af706f` admin_health_summary recycle-bin total aligned with 29-table dropdown
- `6b4d0d2` chore: backfill TASKS.md commit hash for ea766b0
- `ea766b0` Recycle Bin cross-table 'All tables' mode
- `39632a3` chore: backfill TASKS.md commit hash for c4da7be
- `c4da7be` audit triggers on folder + folder-share + help tables (9 tables)
- `eb07ab6` add TASKS.md — live working task list
- `0f6e53c` field-history tracking on PRT + envelope columns
- `05a903b` audit triggers on envelope family (3 tables)
- `fbb199f` Setup Home health strip + clickable Most Visited cards
- `5dc957c` audit triggers on PRT family (4 tables)
- `718c37d` Recycle Bin Phase 2 — permanent purge with admin gate
- `4ff648a` field-history tracking on permission/scope/portal columns
- `293e435` Recycle Bin Phase 1 — view + restore
- `52b453d` AuditLogPane with object/record/action filters
- `1339bbf` fetchAuditLog filter params + performer name resolution
- `b0f0f5d` audit triggers on permission/scope/portal tables (6 tables)
- `a64932b` clone_permission_set + Clone button
- `41d3c0a` clone_scheduled_report + Save As button
- `b2549fc` clone_dashboard + Save As button
- `f9fb5ab` audit triggers on Reports + Permission Builder tables (11 tables)
- `4b9cb5b` XLSX attachment support in dispatcher
